/** Framework interview scenarios FW-S-01 .. FW-S-20 */
export const SCENARIOS = [
  { id: 'FW-S-01', q: 'Walk me through initializing a Playwright TypeScript project from an empty repo.',
    think: ['What belongs in day-one config vs later?', 'Strict TypeScript — yes or later?', 'What is the runner vs your conventions?'],
    ideal: 'Add `@playwright/test`, enable strict tsconfig, create root `playwright.config.ts` with testDir, list+html reporters, trace on-first-retry, and forbidOnly in CI. Document folder conventions before the tenth test. Do not wrap Playwright in a custom runner.',
    stuck: 'They describe copying Selenium patterns or disabling strict mode "temporarily."',
    whyAsked: 'Junior signal: have they actually bootstrapped a repo or only joined an existing one?',
    wrong: '"We built a custom CLI so QAs never touch Playwright" — adds maintenance without value.',
    example: 'FinTech startup: init with strict TS + eslint-plugin-playwright; first PR added fixtures/ before pages/ grew to 40 files — zero retrofit cost.',
    followups: ['What trace default for CI?', 'When add a second browser project?', 'What goes in package.json scripts vs config?'] },
  { id: 'FW-S-02', q: 'Draw the folder structure for a 200-test suite with three engineers.',
    think: ['Feature vs layer grouping?', 'Where page objects and fixtures?', 'What is the junk drawer to avoid?'],
    ideal: '`tests/<feature>/*.spec.ts`, shared `pages/`, `fixtures/base.ts` exporting extended test, `data/factories/`, root config. Tests import test from fixtures. No flat numbered specs.',
    stuck: 'Mirror entire app src tree or one giant helpers/ folder.',
    whyAsked: 'Tests whether conventions scale with headcount.',
    wrong: 'Every engineer picks their own pattern per feature — review chaos.',
    example: 'Marketplace team grouped by checkout/orders/account; onboarding doc one page; new hire added auth test in correct folder day two.',
    followups: ['When colocate page object with feature?', 'Where do API clients live?', 'How enforce with CODEOWNERS?'] },
  { id: 'FW-S-03', q: 'What locator strategy would you standardize for the team?',
    think: ['Role vs test id vs CSS?', 'How enforce?', 'XPath ever?'],
    ideal: 'Priority: getByRole/getByLabel → getByTestId (contract with devs) → CSS. Ban XPath in new tests. ESLint + review checklist. Locators as readonly fields on page objects.',
    stuck: 'Codegen CSS copied everywhere; no written policy.',
    whyAsked: 'Locator policy is the longest-lived framework decision.',
    wrong: '"We use whatever codegen outputs" — suite rots on first redesign.',
    example: 'SaaS dashboard: pushed devs for accessible names; role locators survived React refactor without test edits.',
    followups: ['When is test id acceptable?', 'How handle duplicate button names?', 'Component scoping?'] },
  { id: 'FW-S-04', q: 'Review this thick Page Object with verify* methods full of expects. What do you change?',
    think: ['Assertions in tests or POM?', 'What stays in POM?', 'Component extraction?'],
    ideal: 'Thin POM: locators + actions. Move business expects to spec. Optional waitForLoaded() for readiness. Extract repeated widgets to component objects.',
    stuck: 'Defend thick POM as "DRY" without seeing hidden assertions hurt readability.',
    whyAsked: 'Mid-level code review scenario — very common in real PRs.',
    wrong: 'Add BasePage with 50 shared verify methods — inheritance tar pit.',
    example: '600-line OrdersPage split into OrdersPage + DataGridComponent + ConfirmModal; spec assertions visible in failure output.',
    followups: ['POM vs Screenplay?', 'When allow assertion in POM?', 'Fixture inject page object how?'] },
  { id: 'FW-S-05', q: 'Replace this beforeEach that constructs page objects and mutates shared state with fixtures.',
    think: ['Parallel safety?', 'Teardown where?', 'Import path for test?'],
    ideal: '`test.extend` with typed fixtures; module-level mutable state removed; export test/expect from fixtures/base; specs depend on `{ loginPage }` in signature.',
    stuck: 'Keep beforeEach and add `--workers=1` as "fix."',
    whyAsked: 'Core Playwright framework mechanism at mid level.',
    wrong: 'Singleton page object cache shared across tests.',
    example: 'Parallel failures traced to module-level `currentUser`; fixture per test fixed overnight.',
    followups: ['Worker vs test scope?', 'mergeTests when split fixtures?', 'Auto fixture cost?'] },
  { id: 'FW-S-06', q: 'Design auth so 2,000 tests do not UI-login every time.',
    think: ['storageState?', 'Setup project?', 'API login in setup?'],
    ideal: 'Setup project runs auth.setup.ts, saves gitignored `.auth/user.json`, consumer projects use dependencies + storageState. Prefer API/token in setup when available.',
    stuck: 'Login in beforeEach; hit rate limits; 40-minute suites.',
    whyAsked: 'Standard senior efficiency question.',
    wrong: 'Commit storageState JSON to repo for "speed."',
    example: 'B2B app: setup project + API login; suite login time 38 min → 4 min.',
    followups: ['Multi-role auth?', 'Session expiry?', 'Security on artifacts?'] },
  { id: 'FW-S-07', q: 'How test admin vs member RBAC in parallel without one shared user?',
    think: ['storageState per role?', 'Projects matrix?', 'PR vs nightly scope?'],
    ideal: 'Separate setup tests write `admin.json` / `member.json`; projects or grep tiers map roles; member tests assert forbidden on admin routes.',
    stuck: 'One admin cookie for everything; skip negative tests.',
    whyAsked: 'RBAC is where auth architecture shows depth.',
    wrong: 'Logout/login switch role mid-test for every case.',
    example: 'Healthcare portal: member project expects 403 on `/admin/billing`; parallel-safe.',
    followups: ['Matrix explosion control?', 'Dynamic role fixture?', 'MFA in setup?'] },
  { id: 'FW-S-08', q: 'Same tests must run against local, staging, and CI — config design?',
    think: ['Env vars?', 'Multiple config files?', 'Secrets?'],
    ideal: 'Single config; `use.baseURL` from `process.env.BASE_URL`; secrets in CI store; `.env.example` documents vars; smoke project on PR against staging.',
    stuck: 'Duplicate spec folders per environment.',
    whyAsked: 'Environment layering is architect interview staple.',
    wrong: 'Hardcode staging URL in 200 tests.',
    example: 'GitHub Actions injects BASE_URL; developers use localhost via `.env.local` — same specs.',
    followups: ['When split config files?', 'Per-env project metadata?', 'Feature flags per env?'] },
  { id: 'FW-S-09', q: 'Tests collide on email unique constraint when run with 4 workers. Fix?',
    think: ['Factories?', 'Static data?', 'DB isolation?'],
    ideal: 'Factory with UUID email suffix; API create user; optional per-worker prefix; never shared static `test@example.com`.',
    stuck: 'Run `--workers=1` permanently.',
    whyAsked: 'Parallel data isolation — senior must-have.',
    wrong: 'Serial mode as policy — hides design bug.',
    example: 'CRM signup tests used `createUser()` factory; parallel green after months of serial runs.',
    followups: ['Sweeper job?', 'Shared tenant DB?', 'Teardown failure?'] },
  { id: 'FW-S-10', q: 'When API-seed vs full UI journey for checkout test?',
    think: ['What is under test?', 'Speed vs coverage?', 'Hybrid?'],
    ideal: 'If asserting checkout total, API seed cart and open checkout URL. If testing "add to cart" journey, drive UI from catalog. Document decision tree in README.',
    stuck: 'Always UI from login; or always API skipping UI under test.',
    whyAsked: 'Efficiency without false confidence.',
    wrong: 'API-mock your own checkout API in smoke — misses integration.',
    example: 'Retail: 200 regression tests API-arrange; 15 smoke tests full UI happy path.',
    followups: ['Hybrid fixture teardown?', 'Network mock boundaries?', 'Contract tests where?'] },
  { id: 'FW-S-11', q: 'Implement network mocking for flaky payment provider without mocking your own API.',
    think: ['page.route scope?', 'Option fixture?', 'Unroute?'],
    ideal: 'Option fixture registers Stripe routes only when test opts in; fulfill JSON; unroute after test; own checkout API stays real in regression.',
    stuck: 'Mock everything including internal APIs.',
    whyAsked: 'Boundary judgment on mocks.',
    wrong: 'Global route in beforeAll affecting all tests.',
    example: 'Subscription flow: `@external-pay` tests use route fixture; core billing API unmocked nightly.',
    followups: ['HAR vs handler?', 'Third-party SLA tests?', 'Route order conflicts?'] },
  { id: 'FW-S-12', q: 'Teardown failed and orphaned e2e users accumulated for months. Permanent fix?',
    think: ['Idempotent delete?', 'Prefix convention?', 'Scheduled sweeper?'],
    ideal: 'Fixture teardown tolerates 404; all test data prefixed `e2e-`; nightly sweeper deletes aged records; alert on count growth.',
    stuck: 'Only happy-path afterEach; no backstop.',
    whyAsked: 'D19-style production hygiene at framework level.',
    wrong: 'Manual DB cleanup script run "when someone remembers."',
    example: 'Shared staging DB: sweeper + prefix reduced orphans 12k → 0 in one week.',
    followups: ['CI kill mid-test?', 'Shared env isolation?', 'Legal/data retention?'] },
  { id: 'FW-S-13', q: 'Design PR smoke vs nightly regression tiers for 3,000 tests.',
    think: ['Tags?', 'Projects?', 'Quarantine?'],
    ideal: '@smoke grep on PR (~10–15 min); full regression nightly with sharding; @quarantine excluded from merge gate with ticket+owner+expiry.',
    stuck: 'Full suite on every PR or no tests on PR.',
    whyAsked: 'Org-scale CI strategy.',
    wrong: 'Retry until green on PR with 5 retries — masks debt.',
    example: 'PR: chromium smoke 180 tests; nightly: 4 shards × 3 browsers, merge-reports html.',
    followups: ['Who owns tag list?', 'New test default tier?', 'Quarantine metrics?'] },
  { id: 'FW-S-14', q: 'Nightly suite still 45 minutes after workers maxed on one machine. Next step?',
    think: ['Workers vs shards?', 'Blob merge?', 'Isolation prerequisite?'],
    ideal: 'Verify parallel-safe tests; horizontal sharding with blob reporter; merge-reports; tune slow files; smoke on PR. Consider timing-aware orchestrator at 10k+.',
    stuck: 'Conflate workers with shards; shard before fixing shared data.',
    whyAsked: 'D3 scaling execution — architect favorite.',
    wrong: 'Buy bigger machine only — vertical limit hits.',
    example: '4-shard GitHub matrix + merge; 45 min → 14 min wall clock.',
    followups: ['Unbalanced shards?', 'Trace storage cost?', 'Test impact analysis later?'] },
  { id: 'FW-S-15', q: 'Flaky tests break every team\'s merge. Quarantine policy?',
    think: ['Threshold?', 'Owner?', 'Retries vs quarantine?'],
    ideal: 'Define flaky threshold; quarantine with ticket ID, owner, expiry; exclude from gate but run nightly; CI retries 1–2 max with trace on-first-retry; visible quarantine count.',
    stuck: 'Increase retries to 5; no ownership.',
    whyAsked: 'D5 governance — lead level.',
    wrong: 'Delete flaky tests without triage.',
    example: 'Quarantine dashboard in CI summary; count dropped 28 → 6 in one quarter with owners.',
    followups: ['Retry vs product bug?', 'Trace-first workflow?', 'Re-entry criteria?'] },
  { id: 'FW-S-16', q: 'You join with zero automation. Design framework to thousands of tests — what do you NOT build?',
    think: ['Year one restraint?', 'Playwright already provides?', 'Checkpoints for scale?'],
    ideal: 'No custom runner, BaseTest inheritance, hand-rolled parallelism, plugin architecture, or Screenplay without org buy-in. DO build: folders, fixtures, thin POM/components, storageState auth, env config, tags, lint.',
    stuck: 'Over-abstract day one; cannot name deferred items.',
    whyAsked: 'D1 architect question — restraint separates seniors.',
    wrong: '"We\'ll build our own Selenium-compatible layer for portability."',
    example: 'ADR-001 listed deferred items; revisited at 500 and 5k tests — avoided 6-month framework rewrite.',
    followups: ['When add shared npm package?', 'Component objects threshold?', 'BDD layer?'] },
  { id: 'FW-S-17', q: '100 teams depend on your shared framework. Ship breaking fixture API change how?',
    think: ['Semver?', 'Deprecation window?', 'Codemod?'],
    ideal: 'Major version bump; old+new coexist with deprecation warning; migration guide; teams opt-in schedule; no flag-day across 100 teams.',
    stuck: 'Silent breaking change merged Friday.',
    whyAsked: 'D25 internal product thinking.',
    wrong: 'Email "please update" without version or timeline.',
    example: '@corp/playwright-fixtures v3 removed `login()` fixture — v2 supported 90 days; codemod for rename.',
    followups: ['RFC process?', 'Compatibility test suite?', 'D27 blast radius?'] },
  { id: 'FW-S-18', q: '15 teams use POM, Screenplay, and raw tests in one shared repo. Fix without rewrite.',
    think: ['Convergence?', 'Lint?', 'Reference impl?'],
    ideal: 'Platform picks one pattern (thin POM + fixtures); reference implementation; lint/review flags deviation; incremental migration when teams touch code — not stop-the-world.',
    stuck: 'Build adapters supporting all three forever.',
    whyAsked: 'D26 pattern anarchy.',
    wrong: 'Mandate rewrite freeze for 6 months.',
    example: 'Screenplay team migrated hottest 20 specs first; lint blocked new Screenplay imports.',
    followups: ['Extension points?', 'Team autonomy limits?', 'mergeTests split?'] },
  { id: 'FW-S-19', q: 'Security bans persisting auth cookies to disk. Redesign suite auth.',
    think: ['storageState alternative?', 'API token in memory?', 'Talk to security?'],
    ideal: 'API auth per worker in memory; short-lived test credentials; or security-approved encrypted store — explicit conversation; never ignore requirement.',
    stuck: 'Insist storageState is the only way.',
    whyAsked: 'D23 real constraint.',
    wrong: 'Write storageState to /tmp and claim compliance.',
    example: 'Worker fixture obtains JWT via API; injects via addInitScript; no disk persistence; security signed off.',
    followups: ['MFA bypass?', 'Token refresh?', 'Trace leakage?'] },
  { id: 'FW-S-20', q: 'CI test bill tripled. Cut cost without dropping coverage.',
    think: ['Tiering?', 'Matrix scope?', 'Cache?', 'Shard efficiency?'],
    ideal: 'Audit: full cross-browser on every commit → move to nightly; cache browsers; right-size workers; smoke on PR; track cost per run; avoid over-sharding small suites.',
    stuck: 'More parallelism everywhere — same bill driver.',
    whyAsked: 'D28 platform economics.',
    wrong: 'Delete regression tier entirely.',
    example: 'Moved firefox/webkit to weekly; PR chromium smoke only; bill −55%, coverage unchanged on schedule.',
    followups: ['Visual tests tier?', 'Cloud vs self-host?', 'Test impact analysis ROI?'] },
];

# Portfolio samples

- **CI artifacts:** failing bank-demo runs upload `playwright-report/` and `test-results/` (traces on retry).
- **Trace viewer:** open any `trace.zip` with `npx playwright show-trace` or https://trace.playwright.dev
- **HTML report:** `cd practice-suite && npx playwright show-report` after a local run

Governed AI demo for interviews: walk `#agents-mcp` + D36, then show a bank-demo PR run that stays green when external hosts are down.

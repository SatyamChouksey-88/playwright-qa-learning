/* Generated by tools/content/build-content.mjs — do not edit by hand.
 * SSOT: interview-qa/stuck/*.md — see interview-qa/_templates.md (Template 1)
 * Regenerate: npm run build:content
 */
window.STUCK_ENTRIES = [
  {
    id: "stuck-files-data-date-locale-mismatch",
    title: "A date renders differently in US vs EU locale and breaks the assertion",
    category: "files-data",
    severity: "common",
    symptom: "A test asserting `toContainText('03/04/2026')` passes in one environment and fails in another where the exact same underlying date renders as `04/03/2026` — because one means March 4th and the other April 3rd, depending on locale.",
    why: "Date formatting (`MM/DD/YYYY` in the US vs `DD/MM/YYYY` in most of the rest of the world) depends on the browser's or app's configured locale, which can differ between a developer's local machine, a CI runner's default OS locale, and whatever locale a real user's browser reports — a hardcoded ambiguous-format date string bakes in an assumption that isn't actually guaranteed anywhere in the stack.",
    debug: "1. Check what locale the CI runner/browser context is actually using (`Intl.DateTimeFormat().resolvedOptions().locale` via `page.evaluate()`) versus what the developer's local machine defaults to.\n2. Confirm the app itself is locale-aware (renders dates via `Intl.DateTimeFormat` respecting a `locale`) rather than always hardcoding one format regardless of context.\n3. Reproduce locally by explicitly launching a browser context with a different `locale` option to match CI's.",
    fix: "```ts\n// Pin the locale explicitly so the test's expectations are unambiguous\n// regardless of the runner's OS default.\ntest.use({ locale: 'en-US' });\n\ntest('order date renders in US format', async ({ page }) => {\n  await page.goto('/orders/1042');\n  await expect(page.getByTestId('order-date')).toHaveText('3/4/2026');\n});\n\n// Or, avoid ambiguous strings entirely — assert on an unambiguous, parsed value:\ntest('order date is the 4th of March', async ({ page }) => {\n  const text = await page.getByTestId('order-date').textContent();\n  const parsed = new Date(await page.evaluate((t) => Date.parse(t!), text));\n  expect(parsed.getUTCMonth()).toBe(2); // March, 0-indexed\n  expect(parsed.getUTCDate()).toBe(4);\n});\n```",
    bestPractice: "Always pin `locale` (and, where relevant, `timezoneId`) explicitly via `test.use()` rather than depending on the runner's ambient OS/browser default, and prefer asserting on parsed date components or ISO-formatted values over locale-ambiguous display strings wherever the exact rendered format isn't itself the thing under test.",
    wrongFixes: "1. Hardcoding the CI runner's current default locale into the assertion without pinning it in the test config — the runner's default can silently change with an OS/image update.\n2. Writing separate assertions per suspected locale and skipping the ones that fail — masks the real fix (pin the locale) with a maintenance burden.\n3. Using string `.includes()` checks for just the year to \"avoid the ambiguity\" — passes trivially without verifying the date is actually correct.",
    interviewAngle: "\"A date-based assertion passes locally but fails in CI with what looks like the same date — what's really going on?\" — senior answer: very likely a locale-driven month/day ordering difference (MM/DD vs DD/MM); pin `locale` explicitly via `test.use()` and prefer unambiguous, parsed-value assertions over raw locale-formatted strings.",
    related: "stuck-parallel-ci-docker-font-rendering, stuck-waits-timing-clock-dependent-otp",
  },
  {
    id: "stuck-files-data-download-capture",
    title: "Verifying a downloaded file's name and size",
    category: "files-data",
    severity: "common",
    symptom: "A test clicks a \"Download report\" button and needs to verify the download actually happened with the right filename and a non-trivial size, but the browser's native download UI/dialog isn't something the test can see or interact with directly.",
    why: "Downloads triggered by the browser aren't part of the page's DOM — they're a browser-chrome-level event. Without explicitly listening for Playwright's `download` event, a test has no visibility into whether a download happened at all, let alone its filename or content.",
    debug: "1. Confirm a `download` event listener is registered (via `page.waitForEvent('download')`) *before* the triggering click, following the same before-not-after ordering rule as popups and responses.\n2. Check `download.suggestedFilename()` and save the file (`download.saveAs(...)` or read `download.path()`) to inspect its actual content/size if the failure is about content correctness, not just \"did a download happen.\"\n3. Verify the download isn't actually being blocked by browser download-permission settings in the test's launch configuration.",
    fix: "```ts\nimport fs from 'node:fs';\n\ntest('downloads a report with the expected filename and content', async ({ page }) => {\n  const [download] = await Promise.all([\n    page.waitForEvent('download'),\n    page.getByRole('button', { name: 'Download report' }).click(),\n  ]);\n  expect(download.suggestedFilename()).toBe('annual-report.pdf');\n  const savePath = 'test-results/annual-report.pdf';\n  await download.saveAs(savePath);\n  const { size } = fs.statSync(savePath);\n  expect(size).toBeGreaterThan(1000); // sanity check it's not an empty/error file\n});\n```",
    bestPractice: "Always assert on more than \"a download happened\" — check the filename matches what's expected and that the file's size (or, for structured formats, its parsed content) is sane; treat a 0-byte or suspiciously tiny download as a real failure, not a pass.",
    wrongFixes: "1. Only checking that `page.waitForEvent('download')` resolved without checking the filename or size — a download of a corrupted/empty file still \"resolves\" the event and would incorrectly pass.\n2. Registering the download listener after the click — same ordering race as any other `waitForEvent` usage; the event may already have fired.\n3. Trying to interact with the OS's native \"Save As\" dialog — Playwright deliberately auto-accepts downloads to a managed location instead of surfacing an OS dialog to automate.",
    interviewAngle: "\"How do you verify a file download in Playwright, given there's no visible download dialog?\" — senior answer: register `page.waitForEvent('download')` before the triggering click, then assert on `suggestedFilename()` and the saved file's actual size/content — not just that the event fired.",
    related: "stuck-frames-windows-popup-handling, stuck-waits-timing-response-race",
  },
  {
    id: "stuck-files-data-dynamic-table-order",
    title: "A data table's row order keeps changing between runs",
    category: "files-data",
    severity: "common",
    symptom: "An assertion like \"the first row should be Order #1042\" passes sometimes and fails other times, even though the underlying data hasn't actually changed in a way the test cares about.",
    why: "Tables backed by data with no strong, stable default sort order (e.g., sorted by a timestamp with sub-second collisions, or unsorted and reliant on database-returned order which isn't guaranteed stable across queries) can legitimately return rows in a different order on different runs without any bug at all — this is often correct backend behavior, not something to \"fix\" on the API side, just something the test needs to account for.",
    debug: "1. Check whether the API/backend documents any *guaranteed* sort order at all — if not, position-based assertions were never valid to begin with.\n2. Compare two consecutive real API responses for the same query to confirm the order genuinely varies.\n3. Identify a stable, unique identifier per row (an order ID, not a display position) that the test could key off of instead.",
    fix: "```ts\n// Before: assumes a specific row position that isn't actually guaranteed\nawait expect(page.getByRole('row').nth(1)).toContainText('Order #1042');\n\n// After: locate by stable content, not position\nawait expect(page.getByRole('row', { name: /Order #1042/ })).toBeVisible();\n\n// If order truly matters for the feature under test (e.g., \"sorted by date desc\"),\n// assert the ordering property itself, not a fixed absolute position:\nconst dates = await page.getByTestId('order-date').allTextContents();\nconst sorted = [...dates].sort().reverse();\nexpect(dates).toEqual(sorted);\n```",
    bestPractice: "Assert on content by stable identity (an ID, a unique label) rather than row position whenever the underlying order isn't a documented guarantee; when order *is* a feature (e.g., a sortable column), test the ordering property itself rather than one specific expected sequence.",
    wrongFixes: "1. Adding a fixed \"sort by ID\" query param to the test's own requests, papering over the fact that the *feature under test* (default table order) was never actually verified as intended.\n2. Retrying until the row happens to land in the expected position — non-deterministic and doesn't actually validate anything about correct behavior.\n3. Hardcoding a `.nth(n)` with a comment \"this is usually first\" — documents the flakiness instead of fixing it.",
    interviewAngle: "\"A table-order assertion is flaky even though nothing about the data changed — what's the likely issue?\" — senior answer: the underlying order was probably never a guaranteed contract; locate by stable content/ID instead of position, and if order genuinely matters, assert the ordering property rather than one fixed sequence.",
    related: "stuck-locators-virtual-scroll, stuck-flaky-debug-green-nine-of-ten",
  },
  {
    id: "stuck-files-data-upload-hidden-input",
    title: "Uploading a file when the `<input>` is visually hidden",
    category: "files-data",
    severity: "common",
    symptom: "A file-upload flow driven by a custom-styled \"Choose file\" button fails when the test tries to interact with the real `<input type=\"file\">` — either it can't be found, or a `click()` on it times out.",
    why: "Most custom file-upload UI deliberately hides the native `<input type=\"file\">` (it's visually unstylable) behind a styled button that triggers it via `label`/`click()` delegation or JS. A real user never directly clicks the hidden input — but Playwright's `setInputFiles()` is specifically designed to work around this: it doesn't require the input to be visible or clickable at all, because it operates directly on the file input's value, the same way a user's OS file-picker dialog would set it.",
    debug: "1. Confirm the actual `<input type=\"file\">` element exists in the DOM (even if hidden) — search the page source for `type=\"file\"`.\n2. Check whether the test is trying to `.click()` the hidden input directly (wrong) instead of using `setInputFiles()` on it (right).\n3. If the input only mounts dynamically after clicking a trigger button, make sure the trigger is clicked (or the input located) at the right point in the flow.",
    fix: "```ts\ntest('uploads a file via a custom-styled hidden input', async ({ page }) => {\n  await page.goto('/documents/upload');\n  // No need to click the hidden input or make it visible first.\n  await page.getByLabel('Upload document').setInputFiles('tests/fixtures/sample.pdf');\n  await expect(page.getByText('sample.pdf')).toBeVisible();\n});\n\ntest('uploads multiple files at once', async ({ page }) => {\n  await page.goto('/documents/upload');\n  await page.locator('input[type=\"file\"]').setInputFiles([\n    'tests/fixtures/sample.pdf',\n    'tests/fixtures/photo.png',\n  ]);\n});\n```",
    bestPractice: "Always use `locator.setInputFiles()` targeting the real `<input type=\"file\">` (via `getByLabel`, a `data-testid`, or a stable selector) regardless of whether it's visually hidden — never try to force a click through a `display:none`/`visibility:hidden` input, since `setInputFiles()` doesn't need the element to be actionable at all.",
    wrongFixes: "1. Using `page.evaluate()` to remove the `hidden`/`display:none` styling before clicking — unnecessary DOM surgery when `setInputFiles()` already handles hidden inputs natively.\n2. Using `{ force: true }` on a `.click()` against the hidden input — even if it \"works,\" it doesn't actually set the file the way `setInputFiles()` does, and can behave inconsistently across browsers.\n3. Trying to drive the OS-level native file picker dialog directly — Playwright intentionally doesn't (and can't reliably) automate OS-native dialogs; `setInputFiles()` exists precisely so you never need to.",
    interviewAngle: "\"How do you test a file upload when the actual `<input type=\"file\">` is hidden behind custom styling?\" — senior answer: `setInputFiles()` targets the real input directly and doesn't require it to be visible or clickable — never try to force-click a hidden native file input.",
    related: "stuck-locators-hidden-vs-visible",
  },
  {
    id: "stuck-files-data-worker-data-collision",
    title: "Parallel workers collide on shared test data",
    category: "files-data",
    severity: "tricky",
    symptom: "Two tests that each create, rename, or delete \"the same\" resource (an account named \"Test User,\" an order with a fixed ID) pass individually but fail unpredictably when run in parallel — one worker's cleanup deletes data another worker is mid-way through using.",
    why: "Playwright workers run genuinely in parallel by default; any test data keyed by a fixed, shared identifier (a literal string like `\"test-user\"` or a fixed numeric ID) is not actually isolated between workers, even though each worker believes it \"owns\" that data for the duration of its own test. This is a test-data-design bug, not a Playwright or app bug.",
    debug: "1. Check whether failing tests reference a literal, non-unique identifier for data they create or mutate.\n2. Confirm failures correlate with the number of workers (fewer workers = fewer/no collisions, consistent with a real race for shared data) rather than a specific test change.\n3. Look for setup/teardown (`beforeEach`/`afterEach`) that deletes or resets data by a fixed name shared across spec files.",
    fix: "```ts\nimport { test as base } from '@playwright/test';\nimport { randomUUID } from 'node:crypto';\n\nconst test = base.extend<{ uniqueAccountName: string }>({\n  uniqueAccountName: async ({}, use, testInfo) => {\n    // Unique per test AND per worker/retry — safe under full parallelism.\n    await use(`qa-${testInfo.workerIndex}-${randomUUID().slice(0, 8)}`);\n  },\n});\n\ntest('creates and deletes its own isolated account', async ({ page, uniqueAccountName }) => {\n  await page.goto('/accounts/new');\n  await page.getByLabel('Account name').fill(uniqueAccountName);\n  await page.getByRole('button', { name: 'Create' }).click();\n  await expect(page.getByText(uniqueAccountName)).toBeVisible();\n});\n```",
    bestPractice: "Generate unique, per-test/per-worker identifiers for any data a test creates or mutates (via `testInfo.workerIndex`, a UUID, or a timestamp), and scope cleanup to exactly the data that specific test created — never delete/reset by a fixed shared name that other parallel tests might also be using.",
    wrongFixes: "1. Reducing `workers` to 1 to eliminate the race — hides the underlying test-data-isolation bug and makes the whole suite far slower.\n2. Adding a global mutex/lock file tests wait on before touching \"the\" shared resource — reintroduces serialization by hand instead of just making the data unique per test.\n3. Adding retries so the test passes if it happens to win the race on a later attempt — non-deterministic, and can still fail unpredictably at higher parallelism.",
    interviewAngle: "\"Tests that each manage 'the same' named resource fail intermittently only under parallel execution — diagnose.\" — senior answer: shared, non-unique test data identifiers racing across workers; generate a unique identifier per test (worker index + UUID) and scope cleanup to exactly what that test created, rather than reducing parallelism or adding retries.",
    related: "stuck-login-auth-parallel-logout, stuck-parallel-ci-worker-race-shared-user",
  },
  {
    id: "stuck-flaky-debug-baseline-mac-vs-linux",
    title: "Visual baselines captured on a Mac don't match Linux CI",
    category: "flaky-debug",
    severity: "common",
    symptom: "`toHaveScreenshot()` fails in CI immediately after a developer captures/updates a baseline locally, even though nothing about the app's actual appearance changed.",
    why: "Screenshot rendering is OS- and font-dependent at the pixel level — the exact same HTML/CSS renders with different anti-aliasing, subpixel positioning, and (if the exact fonts aren't installed identically) potentially different fallback fonts entirely between macOS/Windows and a Linux CI container. Playwright's visual comparisons are pixel-based, so baselines are inherently tied to the environment they were captured in.",
    debug: "1. Confirm the baseline file's naming convention includes a platform suffix (Playwright does this automatically per-OS/browser unless overridden) and check whether CI is comparing against the right platform-specific file.\n2. Compare a screenshot taken locally against one taken inside the same Docker image CI uses — do they match each other, confirming the CI-vs-local gap is the actual issue?\n3. Check whether `maxDiffPixelRatio`/`maxDiffPixels` is configured at all, or left at a zero-tolerance default that fails on any anti-aliasing noise.",
    fix: "```bash\n# Always generate/update baselines from inside the same image CI uses to compare.\ndocker run --rm -v $PWD:/work -w /work mcr.microsoft.com/playwright:v1.55.0-jammy \\\n  npx playwright test --update-snapshots\n```\n```ts\n// playwright.config.ts\nexport default defineConfig({\n  expect: { toHaveScreenshot: { maxDiffPixelRatio: 0.02 } },\n});\n```",
    bestPractice: "Standardize on \"baselines are only ever generated inside the CI Docker image\" as a team rule (document it in the README, as this repo does), never accepting a locally-generated baseline into the repo — pair this with a small, explicit diff tolerance for legitimate anti-aliasing noise.",
    wrongFixes: "1. Committing a locally-captured baseline \"just this once\" to unblock a PR — guarantees the very next unrelated CI run fails on the same platform mismatch.\n2. Disabling visual tests for the affected component permanently — a much larger loss of coverage than the actual problem (a baseline-generation process gap) warrants.\n3. Setting pixel tolerance extremely high to force it green — hides real visual regressions along with the platform noise.",
    interviewAngle: "\"A visual test fails in CI right after a baseline update — what's the most likely explanation?\" — senior answer: the baseline was almost certainly generated on a different OS (developer's machine) than the one CI compares against (Linux Docker) — baselines must always be generated inside the same image used for comparison.",
    related: "stuck-parallel-ci-docker-font-rendering, stuck-flaky-debug-visual-flaky-animation",
  },
  {
    id: "stuck-flaky-debug-console-errors-before-failure",
    title: "Console errors appear right before a failure, but nobody's looking at them",
    category: "flaky-debug",
    severity: "common",
    symptom: "A test fails on a UI assertion, and separately (unnoticed until someone specifically checks) the browser console logged a JavaScript error a moment earlier — but the test's own failure message never mentioned it.",
    why: "Playwright doesn't fail a test automatically just because the page logged a console error — by design, since some apps intentionally log warnings/errors that aren't fatal to the flow under test. But that means a genuinely fatal client-side exception (a broken render, an unhandled promise rejection that stopped a component from mounting) can be the *real* root cause of a UI assertion failure while looking, from the test's failure message alone, like \"the element just wasn't there.\"",
    debug: "1. Open the trace's Console tab and scan for any error-level log in the moments before the failing assertion.\n2. Cross-reference the error's stack trace against the component/feature the failing assertion was targeting.\n3. Consider adding a standing `page.on('console', ...)` / `page.on('pageerror', ...)` listener that surfaces (or fails the test on) unexpected errors, so future occurrences are loud instead of buried.",
    fix: "```ts\ntest.beforeEach(async ({ page }) => {\n  const errors: string[] = [];\n  page.on('pageerror', (err) => errors.push(err.message));\n  page.on('console', (msg) => { if (msg.type() === 'error') errors.push(msg.text()); });\n  // Attach for inspection in an afterEach, or assert none occurred for\n  // sensitive flows where any client-side error is inherently a bug.\n  (page as unknown as { __errors: string[] }).__errors = errors;\n});\n\ntest.afterEach(async ({ page }) => {\n  const errors = (page as unknown as { __errors: string[] }).__errors;\n  expect(errors, `Unexpected console errors: ${errors.join('; ')}`).toEqual([]);\n});\n```",
    bestPractice: "Wire a standing console/page-error listener into shared fixtures so every test surfaces unexpected client-side errors as part of its own failure output, rather than requiring someone to manually think to check the console tab after the fact.",
    wrongFixes: "1. Ignoring console output entirely as \"just noise\" without ever checking whether it correlates with failures — misses a frequently-available root-cause signal for free.\n2. Failing every test on *any* console error without an allowlist for expected/benign ones — creates noisy false failures for apps that intentionally log non-fatal warnings.\n3. Suppressing console error logging in the browser via launch flags to \"clean up\" test output — removes a diagnostic signal instead of using it.",
    interviewAngle: "\"How do console logs help debug a UI assertion failure that seems unrelated to logging?\" — senior answer: a fatal client-side JS error can silently prevent the exact element the assertion checks for from ever rendering; wire a standing console/pageerror listener into shared fixtures so this signal is always available, not just checked when someone remembers to look.",
    related: "stuck-network-api-mock-json-correctly, stuck-flaky-debug-trace-viewer-timeout",
  },
  {
    id: "stuck-flaky-debug-green-nine-of-ten",
    title: "A test is green 9 times out of 10 — is that \"acceptable flake\"?",
    category: "flaky-debug",
    severity: "common",
    symptom: "A specific test fails roughly 1 in 10 runs with no obvious pattern, and the team is debating whether to just add a retry and move on.",
    why: "A test that fails 10% of the time essentially never has \"no cause\" — it has a real, deterministic trigger condition (a race, an unhandled edge case, a resource limit) that simply isn't hit on every run because the timing/ordering that exposes it doesn't align every time. \"Flaky\" is a description of the symptom's *frequency*, not evidence that the cause is unknowable or unfixable.",
    debug: "1. Collect artifacts from several *failed* runs (not just one) — look for what's common across the failures specifically, not just what's true in general.\n2. Check whether failure rate correlates with any factor: time of day (shared CI load), specific worker index, specific browser, or specific preceding test in execution order.\n3. Run the suspect test in a tight local loop (`--repeat-each=50`) to reproduce faster than waiting for CI's natural 1-in-10 cadence.",
    fix: "```bash\n# Reproduce faster than waiting for CI's natural flake cadence.\nnpx playwright test tests/checkout.spec.ts --repeat-each=50 --workers=4\n```\n```ts\n// Once reproduced, add the missing precondition wait instead of a retry —\n// e.g., a race between an optimistic UI update and a real API confirmation.\nawait expect(page.getByTestId('cart-count')).toHaveText('2'); // wait for the real state\nawait page.getByRole('button', { name: 'Checkout' }).click();\n```",
    bestPractice: "Treat any failure rate above zero as a real bug with an unknown-but-discoverable trigger, and invest in reproducing it locally with `--repeat-each` before deciding retries are the answer — retries are a legitimate mitigation for truly rare, understood externalities (a third-party outage), not a substitute for root-causing a 10% failure rate.",
    wrongFixes: "1. Adding `retries: 1` specifically to this test and calling it resolved — the underlying race is still there, now just less visible, and can worsen as the app changes.\n2. Deprioritizing investigation because \"it's only 10%, not that bad\" — a 10% single-test flake rate compounds badly across a suite with many similarly-flaky tests, eroding trust in CI overall.\n3. Quarantining the test out of the required check without a tracked follow-up — a common way flaky tests are \"handled\" by never actually being fixed.",
    interviewAngle: "\"A test fails 1 in 10 times — do you just add a retry?\" — senior answer: no — a 10% failure rate has a real, reproducible trigger; use `--repeat-each` to reproduce it locally faster than CI's natural cadence, root-cause it, and reserve retries for truly rare, understood external flakiness.",
    related: "stuck-parallel-ci-retries-hiding-bugs, stuck-flaky-debug-race-assertion-rerender",
  },
  {
    id: "stuck-flaky-debug-memory-crash-long-suite",
    title: "The browser crashes or the runner runs out of memory on a long suite",
    category: "flaky-debug",
    severity: "rare",
    symptom: "A large suite reliably makes it most of the way through before a browser process crashes (or the whole CI job is OOM-killed), and the failure point drifts around rather than being tied to one specific test.",
    why: "Long-running suites that reuse a single browser/context across many tests (rather than Playwright's default of isolating contexts) can accumulate memory over hundreds of tests — leaked event listeners, uncollected DOM references, growing trace/video buffers — until the process exceeds available memory, especially on CI runners with tighter memory limits than a developer's local machine.",
    debug: "1. Check whether the config uses a persistent/reused browser context across many tests (an anti-pattern for isolation *and* memory) versus Playwright's default per-test context isolation.\n2. Monitor memory usage across the run (`docker stats`, or a CI resource-monitoring step) to confirm a genuine upward trend rather than a single-test spike.\n3. Check whether the failure point moves around between runs (consistent with gradual memory accumulation) versus always happening at the same test (which would point to something specific to that test instead).",
    fix: "```ts\n// playwright.config.ts — let Playwright manage per-test isolation and browser\n// lifecycle rather than manually sharing one long-lived context across the\n// entire suite, and constrain worker memory footprint via sharding.\nexport default defineConfig({\n  workers: process.env.CI ? 4 : undefined, // don't over-parallelize on constrained runners\n  use: { trace: 'on-first-retry' }, // avoid 'on' for every test, which grows storage/memory\n});\n```",
    bestPractice: "Default to Playwright's per-test isolated contexts (don't hand-roll a shared browser/context \"for speed\" — the isolation is there for exactly this reason among others), size CI worker counts to the runner's actual available memory rather than maximizing parallelism blindly, and avoid `trace: 'on'` for the whole suite (prefer `'on-first-retry'`) to bound memory/storage growth.",
    wrongFixes: "1. Restarting the CI job and hoping it completes before crashing again — doesn't fix an accumulating resource leak, just gambles on timing.\n2. Splitting the suite into more shards purely to make each individual run shorter, without ever finding the actual leak — reduces symptom visibility without fixing the cause, and increases total CI resource usage.\n3. Reusing a single browser instance across the entire suite specifically to \"improve performance,\" inadvertently causing the very accumulation this entry describes — a well-intentioned optimization that trades away isolation and stability.",
    interviewAngle: "\"A long CI suite crashes with an OOM error partway through, at no consistent test — what's your hypothesis?\" — senior answer: likely a shared/reused browser context accumulating memory across many tests rather than Playwright's default per-test isolation, or overly broad `trace: 'on'` capture — check context-reuse patterns and trace config, not just add more shards.",
    related: "stuck-parallel-ci-sharding-uneven, stuck-parallel-ci-artifact-upload-failed-runs",
  },
  {
    id: "stuck-flaky-debug-race-assertion-rerender",
    title: "A race between an assertion and a re-render makes the test nondeterministic",
    category: "flaky-debug",
    severity: "common",
    symptom: "An assertion immediately following a state-changing action sometimes sees the *old* value and sometimes the *new* one — the test isn't wrong about what the end state should be, it's just sometimes checking before the UI has actually re-rendered to reflect it.",
    why: "Playwright's web-first assertions (`expect(locator).toHaveText(...)`) already auto-retry until the condition is true or a timeout elapses — but a bug in test code that instead reads a value once (`await locator.textContent()`) and compares it with a plain `expect(value).toBe(...)` performs exactly one snapshot read with zero retrying, so it's purely a coin flip whether the re-render has completed by that exact microtask.",
    debug: "1. Find the specific assertion and check whether it's a web-first assertion (auto-retrying, e.g. `toHaveText`) or a manual read-then-compare pattern (`const t = await locator.textContent(); expect(t).toBe(...)`) — the latter is the classic signature of this bug.\n2. Confirm in the trace that the failing run's DOM snapshot at the assertion's timestamp still shows the old value, and a snapshot a moment later shows the new one — proving it's purely a timing race, not a real bug in app behavior.\n3. Grep the codebase for this manual-read pattern broadly — it's rarely just one occurrence once it's identified as a pattern.",
    fix: "```ts\n// Before: single read, zero retrying — a coin flip against the re-render\nconst text = await page.getByTestId('cart-count').textContent();\nexpect(text).toBe('2');\n\n// After: web-first assertion retries automatically until true or timeout\nawait expect(page.getByTestId('cart-count')).toHaveText('2');\n```",
    bestPractice: "Default to web-first assertions (`toHaveText`, `toBeVisible`, `toHaveValue`, etc.) for anything that reflects async/re-rendered state, and treat any `const x = await locator.textContent()` followed by a plain `expect(x).toBe(...)` as a code-review red flag worth converting on sight.",
    wrongFixes: "1. Adding a `waitForTimeout` before the manual read instead of switching to a retrying assertion — sometimes \"works\" by luck, and adds fixed latency to every run regardless of whether a race would have occurred that time.\n2. Wrapping the whole check in a manual polling loop reimplementing retry logic Playwright's assertions already provide — more code, same or worse reliability.\n3. Adding `retries` at the test level to paper over the race — the exact same race can still lose on a retry attempt too, just less often.",
    interviewAngle: "\"An assertion right after a UI-updating action is nondeterministic — what pattern usually causes this, and what's the fix?\" — senior answer: a manual single read (`textContent()` + plain `expect`) racing against React/Vue's re-render, with zero retrying; replace it with a web-first assertion (`toHaveText`), which retries automatically until the condition is true or times out.",
    related: "stuck-locators-element-detached, stuck-flaky-debug-green-nine-of-ten",
  },
  {
    id: "stuck-flaky-debug-trace-viewer-timeout",
    title: "Walking a timeout failure through the trace viewer",
    category: "flaky-debug",
    severity: "common",
    symptom: "A test fails with a timeout and a trace file exists, but it's not obvious which part of the trace actually explains the failure.",
    why: "The trace viewer contains several independent signal sources (a DOM snapshot per action, screenshots, console logs, network requests, and a timeline) — the skill isn't knowing that a trace exists, it's knowing which panel to check first and in what order to get from \"it timed out\" to \"here's why\" efficiently.",
    debug: "1. Open the Actions panel and find the last action that started but never completed — its \"before\"/\"after\" DOM snapshot tab shows exactly what the page looked like right before Playwright gave up.\n2. Switch to the Console tab filtered around that timestamp — a JS exception here often explains why an expected element never rendered.\n3. Switch to the Network tab — a failed, slow, or never-completing request around the same timestamp often explains a missing precondition the UI was waiting on.\n4. Use the timeline scrubber at the very top to visually confirm what was happening across all panels simultaneously, not just one in isolation.",
    fix: "```bash\nnpx playwright show-trace test-results/checkout-should-complete/trace.zip\n```\n```ts\n// Once the trace reveals the real cause (e.g., a network call that never\n// resolved because a route mock never called route.continue()):\nawait page.route('**/api/shipping-options', (route) => route.continue()); // was missing\n```",
    bestPractice: "Build a personal habit/checklist for trace investigation (Actions → DOM snapshot at failure → Console → Network → timeline) so every timeout investigation follows the same efficient path instead of ad hoc clicking around; configure `trace: 'on-first-retry'` (or `'retain-on-failure'`) so a trace always exists to look at in the first place.",
    wrongFixes: "1. Only looking at the final screenshot and guessing from a static image — misses console/network context that usually explains *why*, not just *what*.\n2. Re-running the test repeatedly hoping to \"see it happen live\" instead of reading the trace that already captured it once — much slower than reading the artifact you already have.\n3. Assuming a timeout with a trace present is unexplainable and reaching straight for a longer timeout or a retry — the trace almost always contains the actual answer.",
    interviewAngle: "\"Walk me through how you'd use the trace viewer to debug a timeout.\" — senior answer: find the last action that started but didn't complete, check its DOM snapshot, then cross-reference the Console and Network panels at that exact timestamp — the combination almost always reveals the real precondition that never became true.",
    related: "stuck-waits-timing-timeout-30000-triage, stuck-parallel-ci-artifact-upload-failed-runs",
  },
  {
    id: "stuck-flaky-debug-video-wrong-element-click",
    title: "The trace video shows a click landing on the wrong element",
    category: "flaky-debug",
    severity: "tricky",
    symptom: "Watching the recorded video of a failing run, a click visibly lands on the wrong element — slightly off from the intended target, or on an entirely different control nearby — even though the locator used looks correct in the code.",
    why: "This usually means the page's layout shifted between when Playwright computed the click coordinates and when the actual click event fired — a late-loading image without reserved dimensions, a banner that appeared, or web fonts swapping in (FOUT/FOIT) can all shift layout by just enough to move the target out from under the computed click point. This is functionally a specific case of Cumulative Layout Shift causing a real interaction bug, not just a Lighthouse metric.",
    debug: "1. Watch the video frame by frame around the click moment — is there a visible layout jump immediately before or during the click?\n2. Check for images or ads without explicit `width`/`height` attributes, or a web font loading without `font-display` reserving space, near the clicked area.\n3. Check the DOM snapshot immediately before the click for the target's actual bounding box versus where the video shows the click landing.",
    fix: "```ts\n// Playwright's own actionability checks already guard against this in most\n// cases by re-verifying stability — but if the app has a genuine CLS bug,\n// fix layout stability at the source (reserve space for async content):\n// <img src=\"promo.png\" width=\"320\" height=\"180\" />  <!-- reserves space -->\n\n// If you can't fix the app immediately, wait for the specific late-loading\n// element to settle before interacting with anything near it:\nawait expect(page.getByTestId('promo-banner')).toBeVisible();\nawait page.getByRole('button', { name: 'Continue' }).click();\n```",
    bestPractice: "Treat a \"click landed on the wrong element\" trace as a real Cumulative-Layout-Shift bug in the app worth reporting (it affects real users identically, just less visibly than a failed test makes it), not merely a test-timing nuisance to route around.",
    wrongFixes: "1. Adding a `waitForTimeout` before every click near dynamic content, hoping layout has \"settled\" by then — a fragile guess at a duration, and doesn't fix the underlying CLS bug for real users.\n2. Switching to coordinate-based `page.mouse.click(x, y)` calls with hardcoded coordinates — even more fragile to any layout change, and abandons Playwright's actionability/re-resolution guarantees entirely.\n3. Wrapping the click in a retry loop — may eventually \"work\" by luck once layout happens to have settled, without ever fixing or even reporting the real CLS bug.",
    interviewAngle: "\"A test's recorded video shows a click landing on the wrong element — what's your hypothesis?\" — senior answer: very likely a Cumulative Layout Shift — content shifted between when Playwright computed the click point and when it fired; this is a real bug affecting actual users too, worth fixing/reporting at the source, not just waiting out in the test.",
    related: "stuck-waits-timing-animation-covered-click, stuck-locators-element-detached",
  },
  {
    id: "stuck-flaky-debug-visual-flaky-animation",
    title: "A visual regression test is flaky specifically around an animation",
    category: "flaky-debug",
    severity: "tricky",
    symptom: "`toHaveScreenshot()` on a page containing a CSS/JS animation (a loading spinner, a subtle pulse, a carousel) fails intermittently with diffs concentrated on the animated element, even when the \"real\" content is identical every time.",
    why: "A screenshot is captured at one exact instant — if an element is mid-animation at that instant, the captured frame depends on precise timing that will legitimately differ run to run (a spinner rotated to a slightly different angle, a carousel transitioned by a different amount), producing a real pixel difference that has nothing to do with an actual regression.",
    debug: "1. Confirm the diff region in the failure report is specifically the animated element, not the surrounding static content.\n2. Check whether Playwright's built-in CSS-animation-disabling for screenshots (`animations: 'disabled'`, the modern default in most setups) is actually enabled for this test/config.\n3. Check for JS-driven (not CSS) animations, which Playwright's `animations: 'disabled'` option does not automatically pause — these need masking or a different strategy.",
    fix: "```ts\n// Freeze CSS animations/transitions at their final frame before capturing.\nawait expect(page).toHaveScreenshot('dashboard.png', { animations: 'disabled' });\n\n// For JS-driven animation that CSS-freezing can't reach, mask the region instead.\nawait expect(page).toHaveScreenshot('dashboard.png', {\n  mask: [page.getByTestId('live-ticker')],\n});\n```",
    bestPractice: "Default every visual-regression test to `animations: 'disabled'`, and explicitly `mask()` any JS-driven or inherently non-deterministic region (live tickers, carousels, video thumbnails) that freezing CSS animations can't stabilize — never leave an animated region uncontrolled in a pixel-diff test.",
    wrongFixes: "1. Adding a `waitForTimeout` timed to land \"between\" animation frames — fragile to any future duration/easing change, and never fully eliminates the timing dependency.\n2. Retrying the visual comparison until it happens to land on a matching frame by luck — non-deterministic, and can still fail unpredictably.\n3. Excluding the whole page/component from visual testing because \"it has an animation\" — loses coverage of everything else on the page that isn't actually animated.",
    interviewAngle: "\"A visual regression test is flaky, and the diffs are always around one animated element — what's the fix?\" — senior answer: use `animations: 'disabled'` to freeze CSS animations at their final frame for the screenshot, and `mask()` any JS-driven or inherently nondeterministic region that freezing can't stabilize — don't rely on timing to avoid mid-animation captures.",
    related: "stuck-flaky-debug-baseline-mac-vs-linux, stuck-waits-timing-animation-covered-click",
  },
  {
    id: "stuck-frames-windows-cross-origin-iframe",
    title: "Cross-origin iframe blocks reading its content",
    category: "frames-windows",
    severity: "tricky",
    symptom: "A `frameLocator()` targeting a third-party embed (payment provider, video player from another domain) can click visible buttons but any attempt to read certain content, or a `page.evaluate()` reaching into that frame's document, fails or returns limited data.",
    why: "Cross-origin iframes are subject to the browser's same-origin policy, same as any other cross-origin access — Playwright's `frameLocator` still works for interacting with the frame's *rendered* elements (it operates at the automation-protocol level, not through JS `document.getElementById`), but page-context JavaScript (`page.evaluate`) genuinely cannot read across origins, and some providers deliberately restrict what's exposed inside their iframe for security (e.g., masking card numbers).",
    debug: "1. Confirm whether the operation that's failing is a Playwright locator action (should work fine cross-origin) versus a `page.evaluate()`/JS-based reach into the frame (subject to same-origin policy).\n2. Check the provider's documentation — many payment/embed providers intentionally limit what's readable from their iframe as a security feature, not a bug.\n3. Test with a best-effort try/catch fallback if partial verification is acceptable (e.g., can't read card digits, but can confirm a success postMessage fired).",
    fix: "```ts\ntest('cross-origin iframe is reachable for interaction', async ({ page }) => {\n  const frame = page.frameLocator('iframe[title=\"Payment\"]');\n  await frame.getByLabel('Card number').fill('4242424242424242'); // interaction: fine\n  // Reading is fine when it's Playwright locating rendered elements...\n  await expect(frame.getByText('Card accepted')).toBeVisible();\n  // ...but reaching in via page.evaluate() across origins will not work — don't rely on it.\n});\n```",
    bestPractice: "Rely on `frameLocator()` for interaction and on visible UI state (success/error text rendered inside the frame) for verification, rather than trying to read internal frame data via `page.evaluate()`; if the provider offers a `postMessage`-based event on success/failure, listen for that on the parent page instead.",
    wrongFixes: "1. Launching the browser with disabled web security flags to \"unblock\" cross-origin access — changes browser behavior in a way that doesn't match real users and can hide genuine cross-origin bugs.\n2. Trying to proxy/inline the iframe's content same-origin just for testing — tests a materially different setup than production.\n3. Giving up on any verification of the embedded frame's outcome — silently drops coverage of a critical payment/checkout step.",
    interviewAngle: "\"You can interact with a cross-origin iframe but can't read its internals via page.evaluate() — is that a Playwright bug?\" — senior answer: no, it's the browser's same-origin policy, which Playwright correctly respects; verify via the frame's own rendered UI state (`frameLocator` + visible text) or a `postMessage` event instead.",
    related: "stuck-frames-windows-nested-iframes, stuck-locators-shadow-root",
  },
  {
    id: "stuck-frames-windows-dialog-auto-dismissed",
    title: "A native dialog gets auto-dismissed before the test can assert on it",
    category: "frames-windows",
    severity: "common",
    symptom: "An action that should trigger a native `confirm()`/`alert()`/`prompt()` dialog seems to just... proceed, with the test never seeing the dialog it expected to assert on or respond to.",
    why: "Playwright auto-dismisses native JavaScript dialogs by default (`confirm()` returns `false`, `alert()` is dismissed) unless you register a `page.on('dialog', ...)` handler *before* the triggering action — without a handler, the dialog is gone before your test code gets a chance to look at it, which explains why a \"confirm before deleting\" flow silently behaves as if the user clicked \"Cancel.\"",
    debug: "1. Check whether a `page.on('dialog', ...)` listener exists anywhere in the test, and — critically — whether it was registered before the action that triggers the dialog.\n2. Confirm the behavior matches \"as if Cancel was clicked\" (e.g., a delete that should have happened didn't) — that's the signature of the default auto-dismiss.\n3. If the app is moving to a custom (non-native) confirm dialog instead of `window.confirm()`, verify which one it actually uses now — the fix is different for a real DOM modal.",
    fix: "```ts\ntest('confirm dialog is accepted before delete proceeds', async ({ page }) => {\n  page.once('dialog', (dialog) => dialog.accept()); // register BEFORE the click\n  await page.getByRole('button', { name: 'Delete item' }).click();\n  await expect(page.getByText('Item deleted')).toBeVisible();\n});\n\ntest('confirm dialog cancel path leaves item intact', async ({ page }) => {\n  page.once('dialog', (dialog) => dialog.dismiss());\n  await page.getByRole('button', { name: 'Delete item' }).click();\n  await expect(page.getByText('Item deleted')).toHaveCount(0);\n});\n```",
    bestPractice: "Always register the `page.on('dialog', ...)` handler before triggering the action, and explicitly test both the accept and dismiss paths for anything destructive gated by a confirm dialog — don't rely on the default auto-dismiss behavior implicitly.",
    wrongFixes: "1. Assuming the dialog \"just works\" and never registering a handler — silently gets the default-dismiss behavior, which for a delete-confirmation flow means the delete never actually gets tested.\n2. Registering the handler with `page.on()` (not `.once()`) at file scope and never removing it — can leak into unrelated tests in the same file if dialogs appear unexpectedly elsewhere.\n3. Trying to interact with the dialog as if it were a DOM element (`page.getByRole('alertdialog')`) — native `window.confirm()`/`alert()` dialogs are OS-level chrome, not part of the page's DOM, and require the `dialog` event API specifically.",
    interviewAngle: "\"A delete action with a confirm() dialog seems to silently do nothing in your test — why?\" — senior answer: Playwright auto-dismisses native dialogs by default; without a `page.on('dialog', ...)` handler registered before the triggering action, the confirm is effectively always 'Cancel.'",
    related: "stuck-frames-windows-popup-handling, stuck-waits-timing-animation-covered-click",
  },
  {
    id: "stuck-frames-windows-iframe-not-found",
    title: "Element inside an iframe is never found",
    category: "frames-windows",
    severity: "common",
    symptom: "`page.getByRole(...)` or `page.locator(...)` times out with zero matches for an element that's clearly visible inside an embedded widget (payment form, chat widget, video embed) on the page.",
    why: "`page` locators only ever search the top-level document. Anything inside an `<iframe>` lives in a separate document that has to be addressed explicitly — Playwright doesn't automatically reach into frames the way a human eye does when looking at the rendered page.",
    debug: "1. View the page source or DevTools Elements panel and confirm the target element's closest ancestor is an `<iframe>`.\n2. Check the trace's frame list (the trace viewer shows a frame tree) to find the iframe's name/URL.\n3. Confirm you're not confusing this with a shadow-root case — iframes and shadow roots look similar in the DOM tree but need entirely different Playwright APIs.",
    fix: "```ts\n// Before: searches only the top document, never finds it\nawait page.getByLabel('Card number').fill('4242424242424242');\n\n// After: explicitly address the frame\nconst cardFrame = page.frameLocator('iframe[title=\"Secure card payment frame\"]');\nawait cardFrame.getByLabel('Card number').fill('4242424242424242');\n```",
    bestPractice: "Use `frameLocator()` with a stable selector (a `title`, `name`, or unique `src` fragment) rather than positional (`iframe >> nth=0`) — frame order can change if the page adds another embed later.",
    wrongFixes: "1. Calling `page.frame({ url: '...' })` and then using old-style `ElementHandle` APIs on it — works but loses Playwright's auto-retrying locator behavior that `frameLocator()` gives you.\n2. Using `page.evaluate()` to reach into the iframe's `contentDocument` — bypasses actionability checks entirely and will throw on cross-origin iframes due to browser security.\n3. Giving up and asserting only on the outer page state — silently drops real coverage of the embedded widget's behavior.",
    interviewAngle: "\"An element you can see on the page is never found by a locator — what's the first thing you check?\" — senior answer: whether it's inside an iframe, since `page` locators never implicitly search into frames — use `frameLocator()` addressed by a stable attribute.",
    related: "stuck-frames-windows-nested-iframes, stuck-frames-windows-cross-origin-iframe",
  },
  {
    id: "stuck-frames-windows-nested-iframes",
    title: "Nested iframes make the target unreachable",
    category: "frames-windows",
    severity: "tricky",
    symptom: "`frameLocator('iframe').getByRole(...)` finds nothing even though the element is visibly inside *an* iframe — because it's actually inside a second iframe nested within the first one.",
    why: "`frameLocator()` addresses exactly one level of frame; if the target markup is two (or more) levels deep — an outer widget iframe that itself embeds another iframe (common with third-party payment/ad widgets composing sub-widgets) — you need to chain `frameLocator()` calls, one per level, matching the actual nesting.",
    debug: "1. In DevTools, use the frame-context dropdown in the console to walk down through each nested frame and confirm how many levels deep the target actually sits.\n2. In the Playwright trace viewer, expand the frame tree fully — nested frames are shown as children of their parent frame.\n3. Try locating just the outer frame's container first, then progressively add one more `.frameLocator()` level until the target resolves.",
    fix: "```ts\n// Two levels deep: outer widget iframe → inner payment iframe\nconst outer = page.frameLocator('iframe[name=\"widget-outer\"]');\nconst inner = outer.frameLocator('iframe[name=\"widget-inner\"]');\nawait inner.getByLabel('CVC').fill('123');\n```",
    bestPractice: "Chain `frameLocator()` calls to match the real nesting exactly rather than guessing at a single level; if the nesting is deep and fragile (third-party composing third-party), consider whether an API-level test of the underlying integration is more valuable than a brittle multi-level UI test.",
    wrongFixes: "1. Using `page.locator('iframe iframe').contentFrame()` chains — mixes an older, non-retrying pattern with the newer API and is easy to get subtly wrong about which level you're addressing.\n2. Trying `page.frameLocator('iframe >> iframe')` string-chaining — not valid syntax for nested frame addressing and easy to misdiagnose as \"iframes are broken.\"\n3. Flattening the assertion to only check the outer frame loaded (`toBeVisible()` on the iframe element itself) — proves far less than actually verifying the inner widget's content.",
    interviewAngle: "\"How do you locate an element inside an iframe that's nested inside another iframe?\" — senior answer: chain `frameLocator()` once per nesting level, matching the real DOM structure — there's no single call that reaches through multiple levels automatically.",
    related: "stuck-frames-windows-iframe-not-found, stuck-frames-windows-cross-origin-iframe",
  },
  {
    id: "stuck-frames-windows-new-tab-reload",
    title: "New tab loses previously-found locators after a reload",
    category: "frames-windows",
    severity: "tricky",
    symptom: "A test opens content in a new tab, successfully interacts with it, the tab reloads (or navigates) as part of the flow, and every subsequent locator captured *before* the reload now times out — even ones that look identical to before.",
    why: "This is the same root cause as a stale `ElementHandle` (see the locators category) but shows up specifically around navigation: any handle-based reference (or, less commonly, a `Locator` bound to a frame object that itself got replaced by the navigation) can no longer resolve against the new document. `Locator` objects bound to `page` re-resolve fine across reloads; the issue is almost always a captured `Frame`/`ElementHandle` reference or a `frameLocator` built from a frame handle that no longer exists.",
    debug: "1. Check whether the code captured a `Frame` object (e.g., via `page.frame({ name: ... })`) before the reload and kept using that same reference afterward.\n2. Confirm in the trace whether a full navigation (not just a re-render) actually occurred at the failure point.\n3. Re-derive any frame/locator references *after* the reload rather than reusing pre-reload ones.",
    fix: "```ts\ntest('new tab survives an internal reload', async ({ page, context }) => {\n  const [tab] = await Promise.all([\n    context.waitForEvent('page'),\n    page.getByRole('link', { name: 'Open report' }).click(),\n  ]);\n  await tab.waitForLoadState();\n  await tab.getByRole('button', { name: 'Refresh data' }).click();\n  await tab.waitForLoadState(); // re-navigation happened — re-derive locators after this\n  await expect(tab.getByTestId('report-updated-at')).toBeVisible();\n});\n```",
    bestPractice: "Re-derive locators (call `page.locator(...)`/`getByRole(...)` again) after any navigation rather than holding onto references captured beforehand; `Locator` objects themselves are cheap and safe to recreate — there's no need to cache them across a reload.",
    wrongFixes: "1. Wrapping the post-reload action in a retry loop hoping the stale reference \"catches up\" — a stale frame/handle reference never becomes valid again, no amount of retrying helps.\n2. Adding `page.waitForTimeout()` after the reload before continuing — doesn't address a stale-reference bug, which isn't a timing issue.\n3. Closing and reopening the tab entirely to \"reset\" — works around the symptom but doesn't test the actual reload behavior the scenario was meant to cover.",
    interviewAngle: "\"Locators that worked before a reload suddenly fail after it — what's the likely cause?\" — senior answer: a captured `Frame`/`ElementHandle` reference from before the navigation is now stale; re-derive locators fresh after any reload rather than reusing pre-navigation references.",
    related: "stuck-locators-element-detached, stuck-frames-windows-popup-handling",
  },
  {
    id: "stuck-frames-windows-popup-handling",
    title: "Popup window handling breaks the rest of the test",
    category: "frames-windows",
    severity: "common",
    symptom: "A flow that opens a popup (share dialog, print preview, third-party embed) either hangs waiting for the popup, or continues driving actions against the *original* page while the user's real next action needed to happen in the popup.",
    why: "A popup is a distinct `Page` object that must be captured explicitly the moment it's about to open — if the listener is registered *after* the triggering click (a common ordering mistake), there's a race where the popup may already exist and the event was missed, or the code proceeds against the wrong page entirely.",
    debug: "1. Check the code order: is `context.waitForEvent('page')` (or `page.waitForEvent('popup')`) started *before* the action that triggers the popup, inside the same `Promise.all`?\n2. In the trace, confirm how many pages exist in the trace's page list and at what timestamp the second one appears relative to the triggering click.\n3. If the popup is expected to close itself, confirm the test isn't still trying to interact with it after that.",
    fix: "```ts\ntest('share dialog opens in a popup and completes', async ({ page }) => {\n  const [popup] = await Promise.all([\n    page.waitForEvent('popup'),\n    page.getByRole('button', { name: 'Share' }).click(),\n  ]);\n  await popup.waitForLoadState();\n  await popup.getByRole('button', { name: 'Post' }).click();\n  await popup.waitForEvent('close').catch(() => {}); // some providers auto-close\n  await expect(page.getByText('Shared successfully')).toBeVisible();\n});\n```",
    bestPractice: "Always register the popup-wait *before* triggering the action that opens it (`Promise.all` pattern), never after; treat the popup as its own page object with its own lifecycle, and explicitly decide (and assert) whether it's expected to close itself or needs to be closed by the test.",
    wrongFixes: "1. Polling `context.pages().length` in a loop with sleeps to \"wait for the popup to appear\" — reimplements what `waitForEvent('page')` already does, with worse timing precision.\n2. Ignoring the popup entirely and asserting success based only on the opener page's state — may pass even if the popup flow itself silently failed.\n3. Using `page.on('popup', ...)` as a global listener registered once at test-file scope shared across tests — can leak popup pages between tests if not closed, polluting later tests' `context.pages()`.",
    interviewAngle: "\"A test triggers a popup — what's the pattern to reliably capture and drive it?\" — senior answer: `Promise.all([page.waitForEvent('popup'), triggerAction()])` so the listener is registered before the race can be lost, then treat the popup as its own `Page` with its own waits and assertions.",
    related: "stuck-login-auth-oauth-popup, stuck-frames-windows-new-tab-reload",
  },
  {
    id: "stuck-locators-devtools-vs-test",
    title: "Locator works in DevTools console but fails in the actual test",
    category: "locators",
    severity: "common",
    symptom: "`document.querySelector('.submit-btn')` finds the element instantly when pasted into the browser's DevTools console, but the equivalent Playwright locator in the test times out with zero matches.",
    why: "The most common cause: the element genuinely doesn't exist yet at the moment the test tries to locate it (DevTools is being run *after* the page has already fully loaded and possibly after the exact user action that created the element, while the test runs the check earlier in its own timeline). A second common cause: the element lives inside an `<iframe>` — DevTools' default console context is the top document, so a raw `querySelector` there silently returns null for cross-frame content unless you've switched context, while a Playwright `page.locator()` also only searches the top document (you'd need `frameLocator`).",
    debug: "1. Confirm you're comparing like-for-like: reproduce the DevTools check at the *exact same point in the flow* the test is at (before any subsequent user action), not after manually finishing the flow by hand.\n2. Check whether the element is inside an iframe — DevTools' context selector dropdown will reveal this immediately.\n3. Add a screenshot immediately before the failing locate to see the actual page state at that moment.",
    fix: "```ts\n// If it's an iframe issue:\nconst frame = page.frameLocator('#payment-iframe');\nawait frame.getByRole('button', { name: 'Submit' }).click();\n\n// If it's a timing issue, wait for the real precondition, not a sleep:\nawait expect(page.getByRole('status')).toHaveText('Ready');\nawait page.getByRole('button', { name: 'Submit' }).click();\n```",
    bestPractice: "Always reproduce a \"works in DevTools\" claim at the exact point in the flow the automated test is at — pasting a selector into a fully-loaded page after manually finishing the flow is not the same test as the one that's failing, and iframe context is the single most common gap between the two.",
    wrongFixes: "1. Adding a long fixed sleep before the locate \"to match how long it took to check manually\" — an unreliable proxy for the actual readiness condition.\n2. Switching to `page.evaluate(() => document.querySelector(...))` because \"it worked in the console\" — reimplements a non-retrying, non-actionability-checked lookup and reintroduces exactly the flakiness Playwright locators exist to prevent.\n3. Assuming Playwright is \"buggy\" and adding a global retry wrapper around all locate calls — treats a reproducible logic bug (iframe/timing) as random flakiness.",
    interviewAngle: "\"A selector works instantly in the DevTools console but times out in Playwright — what are your first two hypotheses?\" — senior answer: either the element genuinely doesn't exist yet at that point in the test's timeline (a timing/precondition gap vs. the manual check), or it's inside an iframe that a plain `querySelector` in the top-frame console context wouldn't have reached either.",
    related: "stuck-frames-windows-iframe-not-found, stuck-waits-timing-timeout-30000-triage",
  },
  {
    id: "stuck-locators-dynamic-class-names",
    title: "Locator can't find an element with an auto-generated class name",
    category: "locators",
    severity: "common",
    symptom: "A selector like `.css-1a2b3c4` works today and breaks on the next deploy with no visible UI change — the class name itself changed even though nothing about the element's appearance or behavior did.",
    why: "CSS-in-JS libraries (styled-components, Emotion, CSS Modules) commonly generate hashed class names at build time that are not guaranteed stable across builds — they're an implementation detail of the styling system, not a stable identifier, even though they look like one.",
    debug: "1. Diff the class name between the failing build and the last passing one — if it's a hash-looking string that changed with no visual difference, that confirms the theory.\n2. Check whether the element has any stable, semantic attribute available instead: role, accessible name, `data-testid`, or a stable non-generated class.\n3. If nothing stable exists, that's a signal to request a `data-testid` from engineering rather than keep chasing the moving target.",
    fix: "```ts\n// Before: brittle, build-generated class name\nawait page.locator('.css-1a2b3c4').click();\n\n// After: stable, semantic locator\nawait page.getByRole('button', { name: 'Add to cart' }).click();\n// or, if no accessible role/name exists yet, request a data-testid:\nawait page.getByTestId('add-to-cart').click();\n```",
    bestPractice: "Default to Playwright's role/label/text-based locators, which target the *accessible* representation of the UI (a public contract) rather than implementation details; reserve `data-testid` for cases where no accessible name reasonably exists, and never depend on generated/hashed class names.",
    wrongFixes: "1. Pinning the exact current hash and re-updating it every time it changes — turns every unrelated deploy into a test-maintenance chore.\n2. Matching with a partial-class regex like `[class*=\"css-\"]` — often matches unrelated elements sharing the same CSS-in-JS prefix.\n3. Falling back to absolute XPath position (`/html/body/div[3]/div[2]/button`) — even more brittle than the class name it replaced.",
    interviewAngle: "\"A CSS class-based locator breaks on every deploy — what's wrong and how do you fix it for good?\" — senior answer: CSS-in-JS-generated class names are implementation details, not a stable contract; migrate to role/label/testid-based locators, which target the accessible UI instead.",
    related: "stuck-locators-strict-mode-two-matches, stuck-locators-shadow-root",
  },
  {
    id: "stuck-locators-element-detached",
    title: "\"Element is not attached to the DOM\" right after a click",
    category: "locators",
    severity: "common",
    symptom: "`Error: element is not attached to the DOM` fires on the *second* action against a locator you already successfully interacted with once in the same test, usually right after a re-render.",
    why: "A `Locator` in Playwright is a lazy, re-resolving query — but if you captured a raw `ElementHandle` earlier (via `elementHandle()` or an older API pattern) and the framework re-rendered that DOM node (React/Vue re-mounting rather than mutating in place), the handle you're holding points at a node the browser already discarded. Locators re-query on every action specifically to avoid this; handles do not.",
    debug: "1. Search the test for `.elementHandle()`, `$eval`, or any pattern that captures a handle once and reuses it across multiple awaits.\n2. Check the trace's DOM snapshots immediately before and after the re-render to confirm the element was actually replaced, not just mutated.\n3. Confirm whether the re-render is expected app behavior (e.g., a full list re-mount on filter change) versus an unnecessary re-render worth flagging separately.",
    fix: "```ts\n// Before: captures a handle once, reused after a re-render invalidates it\nconst el = await page.$('.item-row');\nawait el?.click();\n// ...state changes, row re-renders...\nawait el?.click(); // stale handle — throws \"not attached to the DOM\"\n\n// After: a Locator re-resolves on every action, surviving re-renders\nconst row = page.locator('.item-row');\nawait row.click();\n// ...state changes...\nawait row.click(); // re-queries fresh, works\n```",
    bestPractice: "Avoid `ElementHandle`/`$`/`$$` entirely in new code — use `Locator` for everything, since it re-resolves lazily on each action and is immune to this whole class of bug by design.",
    wrongFixes: "1. Wrapping the second action in a retry loop that re-fetches the handle manually — reimplements what `Locator` already gives you for free, worse and by hand.\n2. Adding a `waitForTimeout` before the second action hoping the re-render \"settles\" first — doesn't fix a stale-reference bug, which isn't a timing issue at all.\n3. Wrapping in try/catch and ignoring the error — silently skips a real interaction the test was supposed to perform.",
    interviewAngle: "\"What's the difference between an ElementHandle and a Locator, and why does it matter for a re-rendering UI?\" — senior answer: a Locator is a lazy query re-resolved on every action; a handle is a live reference to one specific DOM node that becomes stale the moment that node is replaced — always prefer Locator for anything that might re-render.",
    related: "stuck-flaky-debug-race-assertion-rerender, stuck-locators-strict-mode-two-matches",
  },
  {
    id: "stuck-locators-hidden-vs-visible",
    title: "Locator times out because the element is \"visible\" but not really",
    category: "locators",
    severity: "tricky",
    symptom: "`toBeVisible()` (or an action requiring visibility) times out on an element that clearly appears on screen in a screenshot taken at the same moment.",
    why: "\"Visible\" in Playwright's actionability model means more than \"has non-zero opacity\" — it requires a non-empty bounding box, `visibility` not `hidden`, `display` not `none`, and — the part people miss — the element must not be covered by another element at the point Playwright would interact with it. An overlay, sticky header, or cookie-consent banner positioned above it (even semi-transparent) fails this even though your eyes read the underlying element as \"visible.\"",
    debug: "1. Read Playwright's own timeout error text closely — recent versions name the specific actionability check that's failing (e.g., \"element is outside of the viewport\" or \"element is covered by another element\").\n2. Open the trace's DOM snapshot at the failure moment and check `elementFromPoint` at the target's center — is a different element actually on top there?\n3. Check CSS: a `position: sticky` header, a toast, or a cookie banner are the most common real-world culprits.",
    fix: "```ts\n// Diagnose: is something else covering the target?\nconst box = await page.getByRole('button', { name: 'Continue' }).boundingBox();\nconsole.log(await page.evaluate(({ x, y }) => document.elementFromPoint(x, y)?.outerHTML, {\n  x: box!.x + box!.width / 2, y: box!.y + box!.height / 2,\n}));\n\n// Fix at the source — dismiss the real blocker, don't force through it\nawait page.getByRole('button', { name: 'Accept cookies' }).click();\nawait page.getByRole('button', { name: 'Continue' }).click();\n```",
    bestPractice: "Treat an actionability timeout as diagnostic information about a real interaction problem a user would also hit — dismiss the actual blocking element (banner, overlay) as part of the test flow, rather than bypassing the check that caught it.",
    wrongFixes: "1. Adding `{ force: true }` to punch through the check — a real user's click would also hit the cookie banner, so this hides an authentic UX bug (or a test that forgot a precondition) rather than fixing it.\n2. Hiding the blocking element via `page.evaluate()` DOM surgery — diverges the tested DOM from what real users see.\n3. Increasing the action timeout hoping the overlay disappears eventually — works only if the overlay is temporary, and hides the real cause even when it does.",
    interviewAngle: "\"toBeVisible() times out on an element you can see in a screenshot — what does that actually mean?\" — senior answer: Playwright's visibility check includes 'not covered by another element at the click point,' so the real cause is almost always something layered on top (banner/overlay) — check `elementFromPoint`, don't reach for `force: true`.",
    related: "stuck-waits-timing-animation-covered-click, stuck-locators-strict-mode-two-matches",
  },
  {
    id: "stuck-locators-shadow-root",
    title: "Locator can't reach content inside a closed shadow root",
    category: "locators",
    severity: "rare",
    symptom: "A component clearly renders in the browser (visible in DevTools, visible on screen), but every Playwright locator targeting its internals returns zero matches, even simple ones like `text=Submit`.",
    why: "Playwright automatically pierces **open** shadow roots as if they were regular DOM — but it cannot pierce **closed** shadow roots at all, by browser design (that's the entire point of \"closed\" mode: even JavaScript running on the page can't reach in). Some third-party design-system web components default to closed mode.",
    debug: "1. In DevTools, inspect the component and check whether its shadow root shows as `#shadow-root (open)` or `#shadow-root (closed)`.\n2. If closed, confirm there's genuinely no other way in — no exposed slot, no light-DOM projection you could target instead.\n3. Ask the component's maintainers (internal or third-party) whether an open-mode \"test build\" flag exists.",
    fix: "```ts\n// If the component is internal: request/ship open shadow mode for test builds\n// customElements.define('my-widget', class extends HTMLElement {\n//   constructor() { super(); this.attachShadow({ mode: 'open' }); } // was 'closed'\n// });\n\n// Once open, Playwright pierces it like normal DOM:\nawait page.locator('my-widget').getByRole('button', { name: 'Submit' }).click();\n```",
    bestPractice: "For internal components, standardize on `open` shadow mode (it doesn't meaningfully weaken encapsulation for typical apps and unlocks testability); for third-party closed-shadow components you don't control, ask for a host-level `data-testid` or an exposed public API/event to drive the interaction instead of trying to reach inside.",
    wrongFixes: "1. Trying browser-devtools-only tricks (`$0.shadowRoot`) inside `page.evaluate()` — closed shadow roots are inaccessible to page-context JavaScript too, this doesn't work.\n2. Switching to a full CDP-level workaround to force access — fragile, version-specific, and fights the platform's explicit privacy boundary instead of working with the component's public surface.\n3. Abandoning automated coverage of that component entirely with no escalation — silently drops real coverage instead of flagging a testability gap.",
    interviewAngle: "\"Playwright can't find anything inside a web component — why, and what do you do?\" — senior answer: check whether it uses closed-mode shadow DOM, which Playwright (correctly) cannot pierce by design; the fix is requesting open mode or a host-level test hook, not a workaround that fights the browser's privacy boundary.",
    related: "stuck-locators-dynamic-class-names, stuck-frames-windows-cross-origin-iframe",
  },
  {
    id: "stuck-locators-strict-mode-two-matches",
    title: "Strict mode violation: locator resolved to 2 elements",
    category: "locators",
    severity: "common",
    symptom: "`Error: strict mode violation: locator('...') resolved to 2 elements` on a click/fill that used to work, often right after an unrelated UI change added a second, visually hidden or off-screen copy of the same element.",
    why: "Playwright's strict mode (on by default for locator actions) intentionally refuses to guess which of several matches you meant — a real ambiguity that a non-strict tool would silently resolve to \"whichever was first,\" hiding a genuine test-authoring or DOM-duplication problem. Common causes: a duplicated component for mobile/desktop layouts both present in the DOM, a modal and its trigger sharing similar text, or a list item template rendering more rows than expected.",
    debug: "1. Read the error message fully — Playwright lists both/all matched elements' outer HTML, which usually makes the duplication obvious immediately.\n2. Open the DOM snapshot in the trace and search for the locator's selector to see both matches side by side.\n3. Ask whether one match is actually meant to be hidden (`display:none`/off-screen) — if so, scope the locator to visible elements or a more specific container.",
    fix: "```ts\n// Before: ambiguous across desktop + mobile nav copies\nawait page.getByRole('link', { name: 'Settings' }).click();\n\n// After: scope to the currently-visible nav\nawait page.locator('nav[aria-hidden=\"false\"]').getByRole('link', { name: 'Settings' }).click();\n// or, if truly duplicated content is a bug, fix the app instead of the locator\n```",
    bestPractice: "Treat a strict-mode violation as a signal worth investigating before reaching for `.first()` — sometimes the fix belongs in the app (remove genuine duplication), not the test. When duplication is intentional (responsive layouts), scope locators to the relevant container rather than relying on ordinal position.",
    wrongFixes: "1. Appending `.first()` reflexively without checking which element is actually first — silently locks the test to DOM order, which can flip with unrelated markup changes.\n2. Wrapping the click in a try/catch that ignores the strict-mode error — hides a real ambiguity instead of resolving it.\n3. Switching to an XPath that \"just picks one\" via `(//a[text()=\"Settings\"])[1]` — same problem as `.first()`, just less visible in the diff.",
    interviewAngle: "\"Your locator suddenly throws a strict-mode error — how do you approach it?\" — senior answer: read which two elements matched from the error/DOM snapshot first, decide whether the duplication is a real app bug or expected (responsive layout), and scope the locator to the right container — don't reach for `.first()` by reflex.",
    related: "stuck-locators-dynamic-class-names, stuck-table-nth-child (see also code-review-lab CR3)",
  },
  {
    id: "stuck-locators-text-matches-script",
    title: "Text locator accidentally matches a `<script>` tag's contents",
    category: "locators",
    severity: "rare",
    symptom: "A broad text-based locator like `page.locator('text=userId')` matches more elements than expected, including one that turns out to be a `<script>` block containing inline JSON with that string inside it.",
    why: "A plain CSS/text selector without a role or tag constraint searches the rendered text content broadly; if the app inlines JSON state (`<script>window.__DATA__ = {...}</script>`) or analytics snippets containing your search string as a literal substring, a loose locator can't distinguish \"text a user reads\" from \"text that happens to be inside script/style tags.\"",
    debug: "1. Reproduce the match count issue and inspect exactly which elements matched — Playwright's strict-mode error (if triggered) will show you the offending `<script>` node directly.\n2. Check the page source for inlined JSON/analytics blobs near the search string.\n3. Confirm whether a role- or tag-scoped locator would have avoided the match entirely.",
    fix: "```ts\n// Before: too broad, can match inline <script> content\nawait expect(page.locator('text=userId')).toBeVisible();\n\n// After: scope by role/tag so non-visual nodes are never candidates\nawait expect(page.getByRole('heading', { name: /userId/i })).toBeVisible();\n// or, for prose content specifically:\nawait expect(page.locator('main').getByText('userId')).toBeVisible();\n```",
    bestPractice: "Default to role-based locators (`getByRole`, `getByLabel`, `getByText` scoped to a real container) instead of a bare `text=` selector with no structural constraint — role-based queries only ever match the accessibility tree, which never includes script/style content.",
    wrongFixes: "1. Adding `:not(script)` CSS exclusions ad hoc every time a new false match appears — a whack-a-mole approach instead of switching to a locator strategy immune to the whole class of bug.\n2. Asserting `nth(1)` because \"the real one is always second\" — brittle to any future addition of more inlined blobs.\n3. Suppressing the assertion failure with a broad try/catch — masks a real locator-quality issue instead of fixing it.",
    interviewAngle: "\"A text-based locator matched something inside a script tag — what locator strategy prevents this entirely?\" — senior answer: role-based locators (`getByRole`, `getByLabel`) only query the accessibility tree, which never includes non-visual nodes like `<script>`/`<style>` — prefer them over bare text selectors.",
    related: "stuck-locators-strict-mode-two-matches, stuck-locators-dynamic-class-names",
  },
  {
    id: "stuck-locators-virtual-scroll",
    title: "Locators go stale scrolling through a virtualized list",
    category: "locators",
    severity: "tricky",
    symptom: "A test that scrolls to \"row 200\" of a long list and clicks it fails intermittently, or clicks the wrong row — even though the same test reliably worked for \"row 5.\"",
    why: "Virtualized lists (react-window, react-virtualized, similar) only render a window of DOM nodes near the current scroll position and recycle/reuse those DOM nodes for different data as you scroll — a `Locator` matched by visible text can point at a *different* logical row than intended if the recycled node hasn't updated yet, or the item you want simply isn't in the DOM at all until you scroll to it.",
    debug: "1. Confirm the list is virtualized (inspect DOM child count vs. total data length — virtualized lists render far fewer nodes than total items).\n2. Check whether the test located the target row by index/position rather than by its actual content, which is unreliable once recycling is involved.\n3. Verify scroll-then-locate is happening in the right order — locating before scrolling into view will find nothing or the wrong recycled node.",
    fix: "```ts\ntest('finds a specific row deep in a virtualized list', async ({ page }) => {\n  await page.goto('/orders');\n  const target = page.getByRole('row', { name: /ORD-10042/ });\n  // Scroll incrementally until the target actually mounts, using the app's own\n  // scroll container — not the window — since virtualization tracks that container.\n  await target.scrollIntoViewIfNeeded();\n  await expect(target).toBeVisible();\n  await target.getByRole('button', { name: 'View' }).click();\n});\n```",
    bestPractice: "Locate virtualized rows by their real content (an order ID, a name), never by numeric position, and prefer `scrollIntoViewIfNeeded()` (which Playwright retries) over manual `mouse.wheel` loops; if the app exposes a search/filter to jump directly to the row, prefer that over scrolling at all.",
    wrongFixes: "1. Scrolling by a fixed pixel amount in a loop with `waitForTimeout` between each step — slow, and fragile to row-height or viewport changes.\n2. Locating by DOM index (`nth(37)`) assuming it maps to data index 37 — recycling breaks this assumption entirely.\n3. Disabling virtualization \"for tests only\" via a special test-mode flag — tests a materially different DOM than what real users experience, undermining the point of the test.",
    interviewAngle: "\"How do you reliably click a specific row deep inside a virtualized/infinite-scroll list?\" — senior answer: locate by real content (not index or position), use `scrollIntoViewIfNeeded()`, and understand that DOM nodes are recycled — position-based locators are fundamentally unreliable there.",
    related: "stuck-locators-strict-mode-two-matches, stuck-files-data-dynamic-table-order",
  },
  {
    id: "stuck-login-auth-jwt-refresh",
    title: "JWT expires mid-test on a long-running scenario",
    category: "login-auth",
    severity: "tricky",
    symptom: "A long end-to-end test (multi-step checkout, multi-page wizard) works fine for the first several minutes, then suddenly every subsequent API call returns 401 and the UI shows stale data or silently fails.",
    why: "Access tokens are usually short-lived by design (5–15 minutes) and rely on a refresh-token flow the *app* handles silently in the background. If the app's silent-refresh logic is broken, slow, or blocked by something the test does (e.g., intercepting requests in a way that swallows the refresh call), the token expires with nothing renewing it.",
    debug: "1. Check the network panel for a refresh-token request (`/oauth/token` or similar) near the failure time — is it firing at all, and what does it return?\n2. If you're using `page.route()` elsewhere in the test, confirm your route handler isn't accidentally intercepting the refresh endpoint and never calling `route.continue()`.\n3. Time-box the test: does the failure always happen at roughly the access-token's TTL, confirming it's a refresh problem rather than a random flake?",
    fix: "```ts\ntest('long checkout flow survives a token refresh mid-way', async ({ page }) => {\n  // Ensure any route mocks explicitly pass through auth endpoints\n  await page.route('**/oauth/token', (route) => route.continue());\n  await page.goto('/checkout/start');\n  // ... multi-step flow that legitimately takes several minutes ...\n  await expect(page.getByTestId('order-confirmation')).toBeVisible({ timeout: 5 * 60_000 });\n});\n```",
    bestPractice: "When mocking network requests broadly (e.g., `page.route('**/api/**')`), always explicitly allowlist or pass through auth/refresh endpoints rather than relying on a catch-all handler to happen to let them through; for very long flows, prefer breaking the test into smaller, independently-verifiable steps that don't require a single unbroken 10-minute token lifetime.",
    wrongFixes: "1. Extending the access-token TTL specifically for test environments — masks a genuine refresh-flow bug that will also affect real long-lived user sessions.\n2. Manually re-authenticating mid-test by navigating back to `/login` — breaks the realism of the scenario you're actually trying to verify (does refresh work?).\n3. Wrapping the assertion in a retry loop — the 401 is a real failure, not a timing race; retrying just delays discovering the refresh bug.",
    interviewAngle: "\"A long-running test starts failing with 401s exactly at your access-token's TTL — what's your hypothesis?\" — senior answer: check whether the silent refresh-token flow is firing and succeeding, and whether any test-side route mocking is accidentally blocking it — don't just extend the token lifetime.",
    related: "stuck-network-api-route-not-intercepting, stuck-login-auth-storage-expired",
  },
  {
    id: "stuck-login-auth-mfa-otp",
    title: "MFA/OTP step blocks every test that logs in",
    category: "login-auth",
    severity: "common",
    symptom: "Every login-dependent test now has to click through a 6-digit OTP screen, and there's no real SMS/authenticator app available in CI to read the code from.",
    why: "MFA is, by design, meant to require a second factor a script can't produce on its own — so it can't be automated \"for real\" without either a deterministic test-mode OTP or a way to read the code programmatically (a seeded/predictable code, an API endpoint that returns the current code for test accounts, or a mocked clock so a fixed code never expires).",
    debug: "1. Ask (or check the codebase) whether non-prod environments already expose a deterministic OTP for designated test accounts — many apps do, precisely for this reason (the Bank Demo app in this repo exposes its OTP directly in the DOM for exactly this purpose).\n2. If not, check whether there's a test-only API endpoint that returns the current valid code for a given test user.\n3. If truly nothing exists, that's a product gap to flag, not a testing problem to work around with a hack.",
    fix: "```ts\n// Using a deterministic, DOM-exposed test OTP (see Bank Demo's #bank-demo section)\ntest('2FA login succeeds with the known test OTP', async ({ page }) => {\n  await page.goto('/index.html#bank-demo');\n  await page.getByTestId('bank-username').fill('apex_2fa');\n  await page.getByTestId('bank-password').fill(process.env.APEX_2FA_PASSWORD!);\n  await page.getByTestId('bank-login').click();\n  const code = await page.getByTestId('otp-hint').textContent();\n  const inputs = page.locator('.otp-input');\n  for (let i = 0; i < code!.length; i++) await inputs.nth(i).fill(code![i]);\n  await page.getByTestId('verify-2fa').click();\n  await expect(page.getByTestId('welcome-banner')).toBeVisible();\n});\n```",
    bestPractice: "Push for a documented, audited \"test OTP\" mode gated behind an environment flag (never enabled in production) rather than every team reinventing an ad-hoc workaround; treat the OTP-entry UI itself as testable in isolation (wrong code, expired code) separately from \"can I get logged in for an unrelated test.\"",
    wrongFixes: "1. Reading a real SMS via a third-party SMS-relay API in every test — slow, flaky, and couples your whole suite's reliability to an external vendor's uptime.\n2. Hardcoding \"123456\" as a magic universal bypass with no environment gating — a serious security risk if it ever leaks into a build that reaches a non-test environment.\n3. Skipping MFA-account tests entirely and only testing non-MFA accounts — silently drops real coverage of a security-critical path.",
    interviewAngle: "\"How do you automate a login flow that requires SMS-based 2FA?\" — senior answer: get the app to expose a deterministic, environment-gated test OTP (or an API to fetch the current code for test accounts) rather than trying to intercept a real SMS; treat \"can't automate a real SMS\" as expected and design around it, not as a blocker.",
    related: "stuck-waits-timing-clock-dependent-otp, stuck-login-auth-oauth-popup",
  },
  {
    id: "stuck-login-auth-oauth-popup",
    title: "OAuth popup login never completes in the test",
    category: "login-auth",
    severity: "common",
    symptom: "Clicking \"Sign in with Google/GitHub\" opens a popup window, the test hangs until timeout, and the main page never reflects a logged-in state — even though the same flow works fine when you click through it by hand.",
    why: "OAuth popups are a genuinely separate `Page` in Playwright's model. If you don't explicitly capture the popup via `page.waitForEvent('popup')`, your test keeps driving the original `page` while the actual form (email/password/consent) lives in a second page object you never touched. The popup usually also needs its own `waitForLoadState` before you can interact with its fields.",
    debug: "1. Run with `--headed` once and watch whether a second window actually opens — if a popup blocker or `noopener` policy silently prevented it, you'll see that immediately.\n2. Open the trace: check whether a second page/tab appears in the trace viewer's page list at all.\n3. If the popup exists but selectors fail, screenshot the popup specifically (not the opener) to confirm you're looking at the right document.",
    fix: "```ts\ntest('OAuth login via Google popup', async ({ page, context }) => {\n  await page.goto('/login');\n  const [popup] = await Promise.all([\n    context.waitForEvent('page'),\n    page.getByRole('button', { name: 'Sign in with Google' }).click(),\n  ]);\n  await popup.waitForLoadState();\n  await popup.getByLabel('Email').fill(process.env.OAUTH_TEST_EMAIL!);\n  await popup.getByRole('button', { name: 'Next' }).click();\n  await popup.getByLabel('Password').fill(process.env.OAUTH_TEST_PASSWORD!);\n  await popup.getByRole('button', { name: 'Sign in' }).click();\n  // popup closes itself after redirecting the opener\n  await expect(page.getByTestId('welcome-banner')).toBeVisible();\n});\n```",
    bestPractice: "Prefer bypassing the real OAuth provider entirely for most tests: seed a `storageState` once via a real login (in a setup project) and reuse it, or stub the provider's callback endpoint. Reserve the full popup flow for one canary test that actually proves OAuth still works end-to-end.",
    wrongFixes: "1. Adding `page.waitForTimeout(5000)` hoping the popup finishes on its own — it may still be waiting on the *original* page's context.\n2. Calling `context.pages()[1]` immediately after the click — the popup may not exist yet; always await the `page` event first.\n3. Disabling popup blocking in browser launch args as the \"fix\" when the real issue was never registering the event listener.",
    interviewAngle: "\"How do you test a third-party OAuth login flow?\" — senior answer: capture the popup via `context.waitForEvent('page')` registered *before* the triggering click, drive it as its own page object, and prefer a stubbed/seeded `storageState` for every test except one real-provider canary.",
    related: "stuck-login-auth-storage-expired, stuck-frames-windows-popup-handling",
  },
  {
    id: "stuck-login-auth-parallel-logout",
    title: "Users get logged out randomly, only when tests run in parallel",
    category: "login-auth",
    severity: "tricky",
    symptom: "Individually, every login test passes. Run the full suite with multiple workers, and some fraction of tests using the *same* test account suddenly see a logged-out state partway through, with no pattern tied to any specific test.",
    why: "If multiple parallel workers log in as the *same* shared test account, most auth systems treat a new login as invalidating the previous session (single-session-per-account enforcement) — worker B's login silently kills worker A's session mid-test. This is a data-isolation bug in the test suite, not a flake in the app.",
    debug: "1. Check whether the failing tests share a hardcoded username across multiple spec files.\n2. Search CI logs for two near-simultaneous login requests for the same account around the failure time.\n3. Confirm with the backend team whether the app enforces single-session-per-user (many banking/security apps do, intentionally).",
    fix: "```ts\n// fixtures/personas.ts — one account per worker, not one shared account for everyone\nexport const PERSONAS = {\n  apex_user: { password: process.env.APEX_USER_PASS! },\n  apex_2fa: { password: process.env.APEX_2FA_PASS! },\n  // ...one distinct persona per parallel test lane\n};\n\n// playwright.config.ts\nexport default defineConfig({ workers: Object.keys(PERSONAS).length });\n```",
    bestPractice: "Provision one test account per parallel worker (or generate ephemeral accounts via an API/seed script per test), and treat \"shared login account across parallel tests\" as a design smell to fix immediately, not something retries can paper over.",
    wrongFixes: "1. Reducing `workers` to 1 to \"make it stable\" — hides the real isolation bug and makes the whole suite far slower.\n2. Adding retries so the \"logged out\" test passes on a second attempt (which may kill a *different* worker's session instead) — just moves the failure around.\n3. Re-logging in inside the test whenever a logout is detected — treats a data-isolation bug as an expected, normal occurrence.",
    interviewAngle: "\"Login tests pass alone but fail intermittently under parallel execution — what's your first hypothesis?\" — senior answer: check whether parallel workers share the same test account, since single-session-per-user enforcement means one worker's login can silently invalidate another's — fix with per-worker accounts, not retries.",
    related: "stuck-parallel-ci-worker-race-shared-user, stuck-parallel-ci-sharding-uneven",
  },
  {
    id: "stuck-login-auth-rate-limited-ci",
    title: "Login gets rate-limited only in CI",
    category: "login-auth",
    severity: "common",
    symptom: "Local runs never hit it, but CI occasionally returns a 429 (\"Too many attempts\") on the login request, failing otherwise-correct tests — especially on days with more CI activity (more PRs, nightly + PR runs overlapping).",
    why: "Login endpoints are commonly rate-limited by IP to slow down credential-stuffing attacks. CI runners frequently share IP ranges (cloud provider egress pools), so many unrelated CI jobs — sometimes from many different repos on a shared CI provider — can collectively exceed a per-IP threshold that a single developer's home/office IP never approaches.",
    debug: "1. Confirm the response is actually 429 with a rate-limit-specific body/header (`Retry-After`), not a generic 500 that looks similar.\n2. Check whether failures cluster around times of high overall CI load (e.g., many PRs merging around the same hour).\n3. Ask whether the auth provider supports allowlisting known CI egress IP ranges, or issuing a rate-limit-exempt test API key.",
    fix: "```ts\ntest('login succeeds even under real (or simulated) rate-limit conditions', async ({ page }) => {\n  await page.goto('/login');\n  await page.getByLabel('Username').fill('apex_user');\n  await page.getByLabel('Password').fill(process.env.APP_PASSWORD!);\n  await page.getByRole('button', { name: 'Sign in' }).click();\n  // If truly rate-limited, this is an environment/provisioning issue — assert clearly\n  // instead of silently retrying past a real 429.\n  await expect(page.getByTestId('welcome-banner').or(page.getByText(/too many attempts/i)))\n    .toBeVisible();\n});\n```",
    bestPractice: "Get CI's egress IP range allowlisted with the auth provider, or use a dedicated test-tier API key/bypass header exempt from the production rate limiter — both are provisioning fixes, and both are far more durable than anything the test code itself can do.",
    wrongFixes: "1. Adding exponential-backoff retry loops around the login call inside the test — hides a real infrastructure constraint and slows every CI run down waiting out rate-limit windows.\n2. Reducing the production rate limit specifically to stop CI from tripping it — weakens a real security control for everyone to solve a test-environment problem.\n3. Randomly staggering test start times with `waitForTimeout` hoping to dodge the threshold — non-deterministic and doesn't fix the underlying shared-IP capacity problem.",
    interviewAngle: "\"CI intermittently gets 429 on login, but never locally — diagnose.\" — senior answer: shared CI egress IPs collectively tripping a per-IP rate limit meant for credential-stuffing protection; fix via IP allowlisting or a test-tier bypass key, not retries or weakening the production limiter.",
    related: "stuck-login-auth-oauth-popup, stuck-parallel-ci-ci-provider-pipeline-failure",
  },
  {
    id: "stuck-login-auth-session-expired-midform",
    title: "Session expires while filling a long multi-step form",
    category: "login-auth",
    severity: "tricky",
    symptom: "A test that fills a long form (many fields, maybe a file upload, maybe waiting on an async validation) submits successfully most of the time, but occasionally lands back on the login screen with the form data lost.",
    why: "Session/idle timeouts are usually based on elapsed time since the *last request*, not since the form was opened — if a test fills fields slowly (many small `fill()` calls with real network validation between them) with no server request in between for longer than the idle window, the session can expire before submit, even though a real user typing at a normal pace with occasional \"still there\" pings might not trigger it.",
    debug: "1. Time the gap between the form-open request and the submit request in the trace's network panel; compare it against the documented idle-session timeout.\n2. Check whether the app has a \"keep-alive\" heartbeat request while a form is open, and whether test conditions (mocked network, paused JS timers) are suppressing it.\n3. Reproduce with a deliberately slow fill and see if the timeout reproduces consistently at the same elapsed time.",
    fix: "```ts\ntest('long form submits without hitting session idle timeout', async ({ page }) => {\n  await page.goto('/forms/onboarding');\n  await page.getByLabel('Full name').fill('Apex User');\n  await page.getByLabel('Company').fill('Example Corp');\n  // If the app has a real heartbeat, don't suppress it — let real timers run.\n  await page.getByLabel('Notes').fill('A'.repeat(200));\n  await page.getByRole('button', { name: 'Submit' }).click();\n  await expect(page.getByTestId('onboarding-success')).toBeVisible();\n});\n```",
    bestPractice: "If the app relies on a client-side heartbeat to keep the session alive, verify that heartbeat is untouched by any test-level network mocking (`page.route`) or clock manipulation (`page.clock`) — those two features are the most common accidental cause of a heartbeat silently stopping in tests only.",
    wrongFixes: "1. Filling the form as fast as possible with no realistic pacing just to \"beat the clock\" — hides whether real users (who type slower) would hit the same bug.\n2. Catching the redirect-to-login and silently re-authenticating mid-test — masks a real UX bug where users could lose form progress.\n3. Globally disabling session idle timeout for the whole test environment — removes coverage of the idle-timeout behavior entirely, including for tests that specifically need to verify it.",
    interviewAngle: "\"A form-submission test flakes with 'session expired' only when the form has many fields — what do you check?\" — senior answer: whether the app's session heartbeat is still firing during the fill, and whether any test-side mocking/clock control accidentally suppressed it — not just add speed or retries.",
    related: "stuck-waits-timing-clock-dependent-otp, stuck-network-api-route-not-intercepting",
  },
  {
    id: "stuck-login-auth-sso-redirect-loop",
    title: "SSO redirect loop never lands back on the app",
    category: "login-auth",
    severity: "tricky",
    symptom: "After submitting SSO credentials on the identity provider's page, the browser bounces between the IdP and the app's `/callback` URL repeatedly until Playwright's navigation timeout fires, or it lands back on the login page instead of the dashboard.",
    why: "SSO redirect chains are sensitive to cookie attributes (`SameSite`, `Secure`) and to the exact final URL the app expects after the IdP hands back a token. A loop usually means the app's session cookie wasn't accepted on the redirect (wrong `SameSite` for a cross-site redirect, or the app is on `http://` locally while the IdP requires `https://`), so the app thinks the user is still unauthenticated and redirects to the IdP again.",
    debug: "1. Open the trace's network panel and read the full redirect chain in order — count how many hops and whether cookies are present on the request *after* the IdP handoff.\n2. Check `Set-Cookie` response headers on the callback response for `SameSite=None; Secure` when the flow is cross-site.\n3. Compare against a manual browser session: DevTools → Application → Cookies, same login, does the cookie actually get stored there either?",
    fix: "```ts\ntest('SSO login lands on dashboard, not a redirect loop', async ({ page }) => {\n  await page.goto('/login');\n  await page.getByRole('link', { name: 'Sign in with company SSO' }).click();\n  await page.getByLabel('Work email').fill('qa+sso@example.com');\n  await page.getByRole('button', { name: 'Continue' }).click();\n  // Wait for the FINAL app URL explicitly rather than a generic \"navigated\" signal.\n  await page.waitForURL('**/dashboard', { timeout: 15_000 });\n  await expect(page.getByTestId('welcome-banner')).toBeVisible();\n});\n```",
    bestPractice: "Run SSO tests against an environment whose cookie/HTTPS configuration matches production exactly — testing SSO on plain `http://localhost` when prod requires `Secure` cookies reproduces this bug reliably and wastes debugging time on a non-representative environment.",
    wrongFixes: "1. Increasing the navigation timeout to \"let it finish eventually\" — a real redirect loop never converges; a longer timeout just fails slower.\n2. Manually looping `page.goto()` calls to \"help it along\" — this fights the browser's own redirect handling and masks the cookie bug.\n3. Disabling `Secure`/`SameSite` cookie flags in the test environment only — fixes the symptom in tests while leaving prod's real (stricter) behavior unverified.",
    interviewAngle: "\"A cross-domain SSO login redirects forever in your test env only — where do you look first?\" — senior answer: the network panel's redirect chain and `Set-Cookie` headers, because a loop almost always means the session cookie from the callback isn't being accepted on the next request, not that the test is waiting wrong.",
    related: "stuck-network-api-cookies-not-sent, stuck-login-auth-oauth-popup",
  },
  {
    id: "stuck-login-auth-storage-expired",
    title: "storageState looks valid but the suite is logged out mid-run",
    category: "login-auth",
    severity: "common",
    symptom: "The first few tests using a saved `storageState.json` pass; partway through the suite, tests start seeing the login screen instead of the dashboard, with no code change to blame.",
    why: "`storageState` is a snapshot taken once (usually in a setup project) — if the session token it captured has a shorter TTL than your full suite's wall-clock runtime, later tests replay an already-expired cookie/token. This gets worse as the suite grows: what was \"plenty of time\" at 50 tests silently becomes \"not enough\" at 500.",
    debug: "1. Decode the token in the saved `storageState.json` (JWTs are base64 — check the `exp` claim) and compare it against your suite's typical wall-clock duration.\n2. Confirm the exact test index/time where failures start — if it's a consistent elapsed-time threshold rather than a consistent test name, that's the signature of a TTL problem.\n3. Check whether the setup project's login happens once per full run or is being cached across CI runs from a stale artifact.",
    fix: "```ts\n// setup/auth.setup.ts\nsetup('authenticate', async ({ page }) => {\n  await page.goto('/login');\n  await page.getByLabel('Username').fill('apex_user');\n  await page.getByLabel('Password').fill(process.env.APP_PASSWORD!);\n  await page.getByRole('button', { name: 'Sign in' }).click();\n  await expect(page.getByTestId('welcome-banner')).toBeVisible();\n  await page.context().storageState({ path: authFile });\n});\n\n// playwright.config.ts — re-run setup if the suite runs longer than the token TTL\nexport default defineConfig({\n  globalTimeout: 25 * 60 * 1000, // keep total runtime comfortably under token TTL\n  projects: [\n    { name: 'setup', testMatch: /auth\\.setup\\.ts/ },\n    { name: 'app', dependencies: ['setup'], use: { storageState: authFile } },\n  ],\n});\n```",
    bestPractice: "Either request a long-lived test-only token from the auth provider for CI accounts, or shard the suite so no single shard's runtime approaches the token TTL, and always assert freshness (re-run the setup project) rather than trusting a cached artifact from a previous CI run.",
    wrongFixes: "1. Adding a UI re-login inside a `beforeEach` \"just in case\" — reintroduces the slow, racy UI-login-per-test problem `storageState` exists to solve.\n2. Blindly raising `retries` so expired-session failures pass on the second attempt — the second attempt fails identically since the token is still expired.\n3. Increasing the token TTL in production to \"fix CI\" — a test infrastructure problem should not change a security control meant for real users.",
    interviewAngle: "\"Your storageState-based suite starts failing exactly 20 minutes into every run — diagnose.\" — senior answer: decode the token's `exp` claim, compare to suite runtime, and either get a longer-lived test token or shard the run so it never gets close to the TTL — not add retries or a UI-login fallback.",
    related: "stuck-parallel-ci-sharding-uneven, stuck-waits-timing-clock-dependent-otp",
  },
  {
    id: "stuck-network-api-401-only-ci",
    title: "An API call returns 401 only in CI, never locally",
    category: "network-api",
    severity: "common",
    symptom: "A test that calls an authenticated API endpoint (directly via `request` fixture, or indirectly via the app) works fine on every developer's machine but returns 401 consistently in CI.",
    why: "The most common cause is an environment-specific secret or config value (an API key, a base URL pointing at a different environment, a required header) that's set in every developer's local `.env` but missing or different in CI secrets — CI runs with its own, separately-configured environment, and a value \"just being there\" locally doesn't mean it's wired into CI.",
    debug: "1. Diff the exact request headers sent locally versus in CI (a temporary debug log of `request.headers()` in both environments) — look specifically for a missing/different `Authorization` header or API key.\n2. Check the CI workflow file for whether the relevant secret is actually declared and passed as an env var to the test step.\n3. Confirm the base URL/environment target is the same in both cases — a 401 can also mean \"successfully talking to the wrong environment,\" not just \"missing credential.\"",
    fix: "```yaml\n# .github/workflows/practice-suite.yml — ensure the secret is actually wired through\n- run: npx playwright test\n  env:\n    API_KEY: ${{ secrets.API_KEY }}\n    BASE_URL: ${{ vars.BASE_URL }}\n```\n```ts\ntest('authenticated API call succeeds', async ({ request }) => {\n  const res = await request.get('/api/accounts', {\n    headers: { Authorization: `Bearer ${process.env.API_KEY}` },\n  });\n  expect(res.status(), await res.text()).toBe(200);\n});\n```",
    bestPractice: "Print (or assert on) the presence — never the value — of required secrets as an early, fast-failing CI step, so a missing-secret misconfiguration fails in seconds with a clear message instead of surfacing as a confusing 401 deep inside a test run.",
    wrongFixes: "1. Hardcoding a real API key directly into the workflow YAML \"to make it work\" — a serious secret-leak risk the moment that file is public or the key is ever rotated.\n2. Adding retries around the 401 — a missing/invalid credential doesn't become valid on a second attempt.\n3. Weakening the endpoint's auth requirement for the CI environment specifically — tests a materially different security posture than production.",
    interviewAngle: "\"An authenticated API test returns 401 only in CI — what's your first check?\" — senior answer: diff the actual request headers/secrets between local and CI, since this is almost always a CI secrets/config wiring gap, not a code or timing bug — assert on secret presence as an early, fast-failing CI step going forward.",
    related: "stuck-login-auth-rate-limited-ci, stuck-parallel-ci-github-actions-cache",
  },
  {
    id: "stuck-network-api-500-only-staging",
    title: "An API returns 500 only in staging, never in production or locally",
    category: "network-api",
    severity: "tricky",
    symptom: "A specific endpoint reliably 500s when tests hit the staging environment, while the exact same request against production or a local dev server succeeds.",
    why: "Staging environments frequently diverge from production in ways that are easy to forget: a thinner database with missing seed/reference data a query silently depends on, a feature flag defaulted differently, a downstream service staging depends on being down or misconfigured, or simply less capacity leading to resource-exhaustion errors under the exact load the test suite generates.",
    debug: "1. Capture the 500 response body — many APIs include a stack trace or error code in non-production environments that names the actual failure (a missing row, a null dereference, a downstream timeout).\n2. Compare staging's feature-flag/config state against production for the endpoint in question.\n3. Check whether the failure only happens for specific test data (an account that doesn't exist in staging's thinner dataset) versus universally for the endpoint.",
    fix: "```ts\ntest('order lookup succeeds for a seeded staging account', async ({ request }) => {\n  // Ensure the test uses data known to exist in *this* environment's dataset,\n  // not an assumption carried over from production.\n  const res = await request.get('/api/orders/staging-seed-order-1');\n  expect(res.status(), await res.text()).toBe(200);\n});\n```",
    bestPractice: "Isolate whether a staging-only failure is a data problem, a config/flag problem, or a genuine capacity problem before touching the test — each has a completely different fix, and none of them are solved by changing test code. Seed staging with data your tests own and control, rather than assuming production-shaped data exists there.",
    wrongFixes: "1. Adding a retry loop around the 500 hoping it's \"just flaky\" — a consistent, reproducible 500 is a real bug, not flakiness, and retries just delay finding it.\n2. Skipping the staging-only failing tests permanently — silently loses coverage of a real staging-environment problem that may also predict a future production issue.\n3. Pointing the test at production instead \"since staging is broken\" — trades a real (if inconvenient) staging signal for tests running against live user data and systems.",
    interviewAngle: "\"An endpoint 500s reliably in staging but nowhere else — how do you narrow down the cause?\" — senior answer: read the actual error body/stack trace staging often exposes, then check for the three usual suspects — thinner seed data, different feature-flag state, or a downstream dependency issue specific to that environment — not a test-code problem.",
    related: "stuck-parallel-ci-docker-font-rendering, stuck-network-api-401-only-ci",
  },
  {
    id: "stuck-network-api-graphql-assertion",
    title: "Asserting on a GraphQL response is harder than a REST one",
    category: "network-api",
    severity: "tricky",
    symptom: "`page.route('**/api/data', ...)` or `page.waitForResponse('**/api/data')` either mocks/matches the *wrong* GraphQL operation, or matches all of them indiscriminately, because every GraphQL call hits the exact same URL.",
    why: "Unlike REST, where each operation typically has its own path, GraphQL APIs commonly expose a single endpoint (e.g., `/graphql`) for every query and mutation — the thing that actually distinguishes one call from another is the request body's `operationName` (or the query text itself), not the URL. A route/wait matched only by URL can't tell \"GetBalance\" apart from \"GetTransactions.\"",
    debug: "1. Inspect the request body of the calls hitting the shared endpoint — confirm the `operationName` field is present and reliably distinguishes the operations you care about.\n2. Check whether the client library always sends `operationName` (some configurations omit it) — if not, matching on a substring of the query text is a fallback.\n3. Confirm a broad URL-only route isn't accidentally intercepting/mocking an unrelated operation that happens to share the endpoint.",
    fix: "```ts\ntest('mocks only the GetBalance GraphQL operation', async ({ page }) => {\n  await page.route('**/graphql', async (route) => {\n    const body = route.request().postDataJSON();\n    if (body.operationName === 'GetBalance') {\n      return route.fulfill({ json: { data: { balance: { checking: 4250 } } } });\n    }\n    return route.continue(); // let every other operation through untouched\n  });\n  await page.goto('/dashboard');\n  await expect(page.getByTestId('checking-balance')).toContainText('4,250');\n});\n\nconst [response] = await Promise.all([\n  page.waitForResponse((r) => r.url().includes('/graphql')\n    && r.request().postDataJSON()?.operationName === 'GetBalance'),\n  page.getByRole('button', { name: 'Refresh' }).click(),\n]);\n```",
    bestPractice: "Always match GraphQL network activity on `operationName` (or a query-text fingerprint) from the parsed POST body, never on URL alone; validate mocked response shapes against the same generated TypeScript types the app uses (from GraphQL codegen) so a schema drift shows up as a type error, not a silent runtime mismatch.",
    wrongFixes: "1. Matching by URL only and assuming \"there's only one GraphQL call on this page anyway\" — breaks the moment a second query/mutation is added to the same page.\n2. Intercepting and mocking *every* GraphQL call broadly to avoid the matching problem entirely — makes the test brittle to unrelated schema changes on operations it never meant to care about.\n3. Falling back to string-matching the raw request body for a fragment of query text — fragile to query-text formatting/whitespace changes that don't affect behavior.",
    interviewAngle: "\"How do you mock a specific GraphQL query when every request hits the same `/graphql` URL?\" — senior answer: match on the request body's `operationName` (or a stable fingerprint of the query), not the URL, since URL alone can't distinguish GraphQL operations from each other.",
    related: "stuck-network-api-mock-json-correctly, stuck-contract-testing",
  },
  {
    id: "stuck-network-api-har-replay-drift",
    title: "A HAR replay drifts from what the real API now returns",
    category: "network-api",
    severity: "tricky",
    symptom: "Tests using `page.routeFromHAR()` for deterministic playback pass reliably for months, then start failing (or silently rendering stale-looking data) after the real API's response shape genuinely changed — but nobody updated the recorded HAR.",
    why: "A recorded HAR is a frozen snapshot of a real interaction at the moment it was captured — by design, it never re-verifies itself against the live API. As the real backend evolves (new required field, renamed field, different pagination shape), the HAR silently becomes an inaccurate stand-in, and the UI code changes that were built to consume the *new* shape then mismatch the *old* recorded response.",
    debug: "1. Check the HAR file's age/last-updated date against recent API changes (changelog, migration notes) for the endpoints it covers.\n2. Diff the HAR's recorded response body against a fresh live capture of the same endpoint/call.\n3. Confirm whether the failure is \"UI breaks because HAR has old shape\" (drift) versus \"route pattern in the HAR doesn't match the current request URL\" (a different, matching-only problem).",
    fix: "```ts\n// Treat HAR files as content that needs a refresh workflow, not a one-time capture.\n// tools/refresh-hars.mjs (conceptual):\n// 1. Hit the real staging API for each recorded interaction.\n// 2. Re-record via page.routeFromHAR({ ..., update: true }) against a live session.\n// 3. Diff the new HAR against the old one in the PR for reviewers to see the drift.\n\ntest('order history renders from a HAR fixture', async ({ page }) => {\n  await page.routeFromHAR('tests/fixtures/order-history.har', {\n    url: '**/api/orders**',\n    update: false, // set true only when intentionally refreshing the fixture\n  });\n  await page.goto('/orders');\n  await expect(page.getByTestId('order-row').first()).toBeVisible();\n});\n```",
    bestPractice: "Treat HAR fixtures like generated content with an explicit refresh workflow (a periodic job or a pre-release checklist item that re-captures and diffs them), not a \"record once and forget\" artifact — pair contract or schema tests against the real API so shape drift is caught independently of whether anyone remembers to refresh the HAR.",
    wrongFixes: "1. Hand-editing the HAR's JSON body to patch just the one field that's currently failing — HARs are meant to be captured, not hand-maintained, and manual edits drift further from reality over time.\n2. Turning off HAR-based tests and switching to purely manual `page.route()` mocks with no connection to real API shapes at all — loses the main benefit of HAR (realistic recorded traffic) without solving the staleness problem.\n3. Ignoring the failure and adding it to a permanent skip list — silently drops coverage instead of scheduling a fixture refresh.",
    interviewAngle: "\"A HAR-based test suite starts failing months after it was recorded, with no test code changes — why, and how do you prevent it long-term?\" — senior answer: the real API's response shape drifted from the frozen recording; prevent it with a scheduled HAR-refresh workflow and independent contract/schema checks against the live API, not by hand-patching the HAR.",
    related: "stuck-network-api-mock-json-correctly, stuck-contract-testing (see also `#contract-testing`)",
  },
  {
    id: "stuck-network-api-mock-json-correctly",
    title: "Mocking a JSON API response the right way",
    category: "network-api",
    severity: "common",
    symptom: "A `page.route()` mock either doesn't take effect (the real network call still happens), or takes effect but the app doesn't render the mocked data correctly even though the JSON \"looks right\" when logged.",
    why: "Two distinct failure modes hide behind this one symptom: (1) the route's URL glob/predicate doesn't actually match the real request (wrong path, missing query params, or a request method mismatch), so `route.continue()` effectively happens by default and the real network fires; or (2) the mock's shape doesn't match exactly what the app's client code expects (missing a field the UI reads, wrong nesting, wrong types like a string where a number is expected), so the app either crashes silently or renders an empty/error state.",
    debug: "1. Add a temporary `console.log` inside the route handler to confirm it's actually being invoked at all — if it never logs, the URL pattern doesn't match.\n2. Compare the mocked payload's exact shape against a real captured response (from a HAR or the Network tab) field by field.\n3. Check the browser console for a client-side error (a `TypeError: Cannot read properties of undefined`) that would explain a \"looks right in the mock\" payload still failing to render.",
    fix: "```ts\ntest('dashboard renders mocked account data', async ({ page }) => {\n  await page.route('**/api/accounts', (route) =>\n    route.fulfill({\n      status: 200,\n      contentType: 'application/json',\n      body: JSON.stringify({\n        accounts: [{ id: 'acc_1', type: 'checking', balance: 4250, currency: 'USD' }],\n      }),\n    }),\n  );\n  await page.goto('/dashboard');\n  await expect(page.getByTestId('checking-balance')).toContainText('4,250');\n});\n```",
    bestPractice: "Derive mock shapes from a real captured response (or a shared TypeScript type/schema both the app and tests import) rather than hand-typing JSON from memory, and always register the route before navigation so the very first request is intercepted.",
    wrongFixes: "1. Registering the route *after* `page.goto()` — the initial request may already have fired against the real API before the mock existed.\n2. Using `route.fulfill()` with a `body` that's already an object instead of a JSON string with the right `contentType` — some clients then fail to `JSON.parse()` it correctly.\n3. Matching the route with an overly broad glob (`**/api/**`) that also intercepts unrelated calls (auth refresh, analytics) — see the JWT-refresh-mid-test entry for what that breaks.",
    interviewAngle: "\"Your route mock doesn't seem to work — how do you diagnose whether it's a matching problem or a shape problem?\" — senior answer: log inside the handler to confirm it's invoked at all (matching problem if not), then diff the mocked payload against a real response and check the console for client-side errors (shape problem if it is invoked but rendering still fails).",
    related: "stuck-network-api-route-not-intercepting, stuck-network-api-graphql-assertion",
  },
  {
    id: "stuck-network-api-request-before-route",
    title: "A request fires before the route handler is registered",
    category: "network-api",
    severity: "common",
    symptom: "A `page.route()` mock is set up correctly and matches the right URL, but the *first* request after a navigation still goes to the real network — only requests *after* that one get intercepted.",
    why: "If `page.route()` is called after `page.goto()` (or after any action that immediately fires the request, like a component that calls an API on mount), the very first request has already been dispatched — sometimes before the route registration's own async setup completes — by the time the handler exists. Route registration must happen before the navigation/action that triggers the request it's meant to intercept, matching the same \"register before triggering\" rule as event listeners.",
    debug: "1. Check the literal code order: is `page.route()` called before or after `page.goto()`/the triggering action?\n2. Confirm in the network panel whether the first request of a given type actually reached the real server (a 200 from the real API, not the mocked shape) while subsequent identical requests were intercepted.\n3. If registering earlier isn't possible for some reason, consider whether `context.route()` (registered once, before any page exists) is more appropriate than `page.route()`.",
    fix: "```ts\ntest('dashboard mock intercepts from the very first load', async ({ page }) => {\n  await page.route('**/api/dashboard', (route) => route.fulfill({ json: { ready: true } }));\n  await page.goto('/dashboard'); // route registered BEFORE navigation\n  await expect(page.getByTestId('dashboard-ready')).toBeVisible();\n});\n```",
    bestPractice: "Always register `page.route()` (or `context.route()` for routes needed across multiple pages/navigations) before the navigation or action that triggers the request — treat this the same as the `waitForResponse`-before-click ordering rule, since it's the same underlying race.",
    wrongFixes: "1. Registering the route, then immediately re-navigating (`page.goto()` twice) so the \"second\" load is the one that gets mocked — works but is wasteful and confusing to read.\n2. Adding a `waitForTimeout` between route registration and navigation \"to let it register\" — route registration is synchronous from the test's perspective; the fix is ordering, not timing.\n3. Assuming Playwright's route API \"doesn't work reliably\" and switching to a browser-extension-based network mocking tool — usually an unnecessary complexity increase for an ordering bug in the test itself.",
    interviewAngle: "\"Your route mock intercepts the second request but not the first, right after navigation — why?\" — senior answer: the route was registered after `page.goto()` fired, so the first request had already left before the handler existed — always register routes before the navigation/action that triggers the request they're meant to catch.",
    related: "stuck-waits-timing-response-race, stuck-network-api-route-not-intercepting",
  },
  {
    id: "stuck-network-api-route-not-intercepting",
    title: "`page.route()` doesn't intercept anything — a service worker is in the way",
    category: "network-api",
    severity: "tricky",
    symptom: "A `page.route()` handler is registered correctly, matches the right URL pattern by all appearances, and simply never fires — the real network response comes through untouched.",
    why: "If the page under test registers its own Service Worker (for offline caching, as this very repo's learning site does), the service worker can intercept the `fetch` at a layer that sits *between* the page and Playwright's route interception in certain configurations, effectively serving a cached response before your route handler ever sees the request. This is a common, easy-to-miss interaction between PWA caching and network mocking.",
    debug: "1. Check whether the app under test registers a service worker (`navigator.serviceWorker`) and, if so, whether it's active for the origin/scope your test runs against.\n2. Try unregistering/bypassing the service worker for the test context and see if the route suddenly starts working — if so, that confirms the theory.\n3. Check Playwright's version/docs for current service-worker interception support (`serviceWorkers: 'block'` context option), since behavior here has evolved across versions.",
    fix: "```ts\n// playwright.config.ts (or per-test context option) — block service workers\n// entirely for tests that need full control over network responses.\nexport default defineConfig({\n  use: { serviceWorkers: 'block' },\n});\n```",
    bestPractice: "For any test relying on precise `page.route()` control, explicitly decide and document whether the service worker should be blocked, allowed-but-empty (fresh cache), or genuinely exercised (a dedicated offline/PWA-behavior test) — don't let it be accidental.",
    wrongFixes: "1. Adding cache-busting query params to try to dodge the service worker — treats the symptom, and can break app code that expects clean URLs.\n2. Clearing browser cache manually between steps hoping it also clears the service-worker cache — service worker caches are a separate storage mechanism from HTTP cache.\n3. Assuming Playwright's route interception is broken and filing it as a tool bug — usually it's working exactly as configured; the service worker is a genuine, separate interception layer.",
    interviewAngle: "\"A `page.route()` mock silently doesn't work on a PWA-style app — what's your first hypothesis?\" — senior answer: check whether a service worker is intercepting the fetch before Playwright's route layer sees it, and use the `serviceWorkers: 'block'` context option (or a dedicated cache-bypass strategy) for tests that need precise network control.",
    related: "stuck-network-api-mock-json-correctly, stuck-login-auth-jwt-refresh",
  },
  {
    id: "stuck-network-api-slow-3g-simulation",
    title: "Simulating a slow 3G connection doesn't behave as expected",
    category: "network-api",
    severity: "rare",
    symptom: "Using CDP-based network throttling to simulate a slow connection either has no visible effect on the test, or makes *everything* uniformly slow in a way that doesn't match how a real slow connection actually behaves (which affects different resources differently).",
    why: "Network throttling emulation (via `page.route()` with artificial delays, or CDP's `Network.emulateNetworkConditions`) approximates bandwidth/latency but doesn't perfectly replicate the connection-level behavior of a real cellular network (packet loss, jitter, variable latency per-connection) — and if throttling is applied at the wrong scope (only to the main document request, not to subsequent asset requests), the test won't see the effect it expects on the parts of the page that matter.",
    debug: "1. Confirm what layer the throttling is actually applied at — a single delayed `page.route()` handler for one resource type versus a full connection-level emulation covering everything.\n2. Check whether the thing you're trying to observe (a loading skeleton, a \"slow connection\" banner) is driven by a timing threshold that your simulated delay does or doesn't actually cross.\n3. Compare against real device/network testing (or a dedicated performance-testing tool) if you need to validate genuine perceived-performance behavior rather than a single test's pass/fail.",
    fix: "```ts\ntest('shows a loading skeleton under a simulated slow connection', async ({ page }) => {\n  await page.route('**/api/dashboard', async (route) => {\n    await new Promise((r) => setTimeout(r, 3000)); // simulate real latency, not a full block\n    await route.continue();\n  });\n  await page.goto('/dashboard');\n  await expect(page.getByTestId('dashboard-skeleton')).toBeVisible();\n  await expect(page.getByTestId('dashboard-ready')).toBeVisible({ timeout: 10_000 });\n});\n```",
    bestPractice: "Use targeted, per-route artificial delay for testing a specific loading-state UI behavior (this is deterministic and fast to run), and reserve full network-condition emulation or real device testing for genuine performance/Core-Web-Vitals investigations — don't expect one tool to answer both questions equally well.",
    wrongFixes: "1. Using `waitForTimeout` to \"wait out\" the simulated slowness instead of asserting on the loading-state UI the delay was meant to exercise — misses the actual point of the test.\n2. Applying a uniform multi-second delay to every single request indiscriminately — makes the test slow to run without meaningfully testing anything beyond \"everything is slow.\"\n3. Concluding throttling emulation is \"unreliable\" and abandoning slow-connection UI testing altogether — usually the scope/threshold, not the tool, was the actual problem.",
    interviewAngle: "\"How do you test that a 'slow connection' loading state actually appears?\" — senior answer: add a targeted artificial delay to the specific route via `page.route()` and assert on the loading-state UI directly, rather than relying on full network-condition emulation to reproduce an exact real-world scenario — that's a job better suited to dedicated performance testing.",
    related: "stuck-waits-timing-networkidle-trap, stuck-waits-timing-spinner-never-detaches",
  },
  {
    id: "stuck-parallel-ci-artifact-upload-failed-runs",
    title: "Failed CI runs don't leave behind anything useful to debug",
    category: "parallel-ci",
    severity: "common",
    symptom: "A test fails in CI, and all that's available afterward is a one-line error in the console log — no trace, no screenshot, no video — making the failure effectively undebuggable without trying to reproduce it blind.",
    why: "Playwright's trace/video/screenshot capture is opt-in via config (`trace: 'on-first-retry'`/`'retain-on-failure'`, similarly for `video`/`screenshot`), and even when captured, those artifacts only persist if the CI workflow explicitly uploads them — a common gap is capturing artifacts correctly but never adding the `actions/upload-artifact` step (or only uploading on success, or to a path that doesn't match where Playwright actually wrote them).",
    debug: "1. Check `playwright.config.ts` for whether `trace`, `screenshot`, and `video` are configured to capture on failure at all.\n2. Check the CI workflow for an artifact-upload step, and confirm its `path` matches the actual `playwright-report`/`test-results` output directory.\n3. Confirm the upload step doesn't have an `if: success()` condition (or equivalent) that skips it precisely when you need it most — on failure.",
    fix: "```ts\n// playwright.config.ts\nexport default defineConfig({\n  use: {\n    trace: 'on-first-retry',\n    screenshot: 'only-on-failure',\n    video: 'retain-on-failure',\n  },\n});\n```\n```yaml\n# .github/workflows/practice-suite.yml\n- run: npx playwright test\n- uses: actions/upload-artifact@v4\n  if: ${{ !cancelled() }} # upload on both pass AND fail, not only on success\n  with:\n    name: playwright-report\n    path: playwright-report/\n    retention-days: 14\n```",
    bestPractice: "Always upload trace/screenshot/video/HTML-report artifacts unconditionally (or explicitly `if: failure() || !cancelled()`), never gated behind a success condition — the entire value of these artifacts is debugging the runs that failed, so an upload step that only fires on green runs is worse than useless.",
    wrongFixes: "1. Only uploading the plain text console log — far less useful than a trace, which lets you replay the exact DOM/network/console state step by step.\n2. Setting `trace: 'off'` to save CI storage/time, planning to \"turn it on later if needed\" — by the time a real failure needs investigating, the evidence is already gone.\n3. Re-running the failed job repeatedly hoping to catch it live in a monitored session instead of fixing artifact capture once — wastes far more time than a five-line config/workflow fix.",
    interviewAngle: "\"A CI failure has nothing useful to debug from — what CI configuration is missing?\" — senior answer: trace/screenshot/video capture on failure needs to be configured in `playwright.config.ts` AND the CI workflow needs an unconditional (or failure-inclusive) artifact-upload step — both pieces are required, and either one missing leaves you with nothing to debug.",
    related: "stuck-flaky-debug-trace-viewer-timeout, stuck-parallel-ci-retries-hiding-bugs",
  },
  {
    id: "stuck-parallel-ci-docker-font-rendering",
    title: "A Docker CI image renders fonts (and therefore visual diffs) differently",
    category: "parallel-ci",
    severity: "common",
    symptom: "Visual-regression (`toHaveScreenshot()`) tests fail with small but consistent pixel differences purely from running inside a Docker-based CI runner, with no actual visual change in the app.",
    why: "Font rendering (anti-aliasing, hinting, even which font a generic family name like `sans-serif` resolves to) depends on which font packages are actually installed in the OS/container — a Linux Docker image almost never has the exact same font set as a developer's macOS or Windows machine, so text renders with different pixel-level anti-aliasing even when the \"same\" font family is requested.",
    debug: "1. Diff the failing screenshot against the baseline visually — is the difference concentrated around text glyphs specifically (a font-rendering signature) versus a layout-level difference (a real bug)?\n2. Check which fonts are actually installed in the CI Docker image versus what the app's CSS requests.\n3. Confirm baselines were originally captured on the same OS/image the CI comparison now runs against — a baseline captured on a developer's Mac will never exactly match Linux CI font rendering.",
    fix: "```ts\n// playwright.config.ts — generate baselines inside the same environment they'll\n// be compared against, and allow a small, deliberate pixel tolerance for\n// anti-aliasing noise that isn't a real visual regression.\nexport default defineConfig({\n  expect: {\n    toHaveScreenshot: { maxDiffPixelRatio: 0.02 },\n  },\n});\n```\n```bash\n# Always (re)generate baselines inside the CI image, never on a local machine.\ndocker run --rm -v $PWD:/work -w /work mcr.microsoft.com/playwright:v1.55.0-jammy \\\n  npx playwright test --update-snapshots\n```",
    bestPractice: "Generate and update visual baselines exclusively inside the same Docker image CI uses for comparison (never on a developer's local OS), and set a small, explicit `maxDiffPixelRatio`/`maxDiffPixels` tolerance to absorb genuine anti-aliasing noise without hiding real regressions — masking dynamic regions (timestamps, avatars) with `mask` where relevant.",
    wrongFixes: "1. Regenerating baselines on a developer's Mac/Windows machine and committing them — guarantees every CI run diffs against a baseline that was never rendered in the same environment.\n2. Setting `maxDiffPixelRatio` extremely high (e.g., 0.3) to \"make it stop failing\" — hides real visual regressions along with the font noise.\n3. Disabling visual tests in CI entirely and only running them locally — loses the actual point of automated visual regression coverage.",
    interviewAngle: "\"Visual regression tests fail only in CI with tiny pixel differences around text — what's the real cause?\" — senior answer: font rendering differs between the developer's OS and the CI Docker image's installed fonts; always generate baselines inside the same image used for comparison, and set a small, explicit diff tolerance rather than a local-machine baseline.",
    related: "stuck-parallel-ci-jenkins-provider-specific, stuck-files-data-date-locale-mismatch",
  },
  {
    id: "stuck-parallel-ci-github-actions-cache",
    title: "GitHub Actions browser cache causes version drift between runs",
    category: "parallel-ci",
    severity: "tricky",
    symptom: "A test suite suddenly starts failing (often visual-regression diffs, or a feature that depends on a specific browser version's behavior) after a Playwright version bump in `package.json` — but only in CI, and only sometimes, not on every run.",
    why: "If the workflow caches the Playwright browser binaries (a common performance optimization) keyed only on something too coarse (or not re-keyed on the Playwright version at all), a run can restore a *stale* cached browser binary that doesn't match the newly bumped `package.json` version — the npm package updated, but the actual browser executable being launched didn't.",
    debug: "1. Check the workflow's cache key — does it include the exact Playwright version (e.g., `hashFiles('package-lock.json')` covering the Playwright entry, or an explicit version string) or something too generic like just the OS name?\n2. Log the actual installed browser version at the start of the CI run (`npx playwright --version`, and the browser's own reported version) and compare it against what `package.json` expects.\n3. Check whether the failure correlates specifically with runs that hit a cache restore versus a cold cache miss (which would always install fresh).",
    fix: "```yaml\n# .github/workflows/practice-suite.yml\n- uses: actions/cache@v4\n  with:\n    path: ~/.cache/ms-playwright\n    key: playwright-${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}\n- run: npx playwright install --with-deps\n```",
    bestPractice: "Key browser-binary caches on something that changes whenever the Playwright version changes (a lockfile hash is the simplest reliable choice), and always run `npx playwright install --with-deps` even on a cache hit — Playwright's own install step is a no-op if the cached version already matches, so this is cheap insurance against a stale cache.",
    wrongFixes: "1. Removing browser caching entirely \"to be safe\" — fixes the staleness bug but makes every CI run noticeably slower re-downloading browsers from scratch.\n2. Manually bumping a cache-busting comment in the workflow file every time Playwright is updated — works but relies on someone remembering to do it every single version bump.\n3. Pinning the workflow to never update the Playwright version — avoids the caching bug by avoiding updates entirely, at the cost of falling behind on real fixes and features.",
    interviewAngle: "\"CI started failing right after a Playwright version bump, but only intermittently — what's your hypothesis?\" — senior answer: a browser-binary cache keyed too coarsely can restore a stale browser version that doesn't match the updated npm package; key the cache on a lockfile hash and always run the install step even on a cache hit.",
    related: "stuck-parallel-ci-passes-locally-fails-ci, stuck-network-api-401-only-ci",
  },
  {
    id: "stuck-parallel-ci-headless-vs-headed",
    title: "Headless mode behaves differently from headed mode",
    category: "parallel-ci",
    severity: "tricky",
    symptom: "A test passes reliably when run headed (`--headed`) locally for debugging, but fails in CI's headless mode — or, less commonly, the reverse.",
    why: "While Playwright's modern headless Chromium is much closer to headed than older \"headless mode\" implementations, real differences can still surface: GPU-accelerated rendering paths behaving differently without a real display, certain media/codec features, focus/window-activation behavior (a headless browser has no real OS window to receive focus events the same way), and timing differences since headed mode is often (slightly) slower due to actual rendering to a visible window.",
    debug: "1. Reproduce exactly: run the same command locally with `--headed` removed to force headless, confirming the difference is genuinely headless-vs-headed and not something else entirely (worker count, OS).\n2. Check whether the failing assertion relates to focus, media playback, or GPU-dependent rendering — the categories most likely to genuinely differ.\n3. Check the Playwright changelog/docs for the specific browser version in use — headless-vs-headed parity has improved significantly across versions, and an older pinned version may have gaps a newer one has closed.",
    fix: "```ts\n// playwright.config.ts — if a specific feature genuinely needs headed-mode\n// fidelity (e.g., certain GPU/media-dependent behavior), scope headed mode to\n// just that project rather than assuming headless is simply \"broken.\"\nexport default defineConfig({\n  projects: [\n    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },\n    {\n      name: 'media-tests',\n      testMatch: /media\\/.*\\.spec\\.ts/,\n      use: { ...devices['Desktop Chrome'], headless: false },\n    },\n  ],\n});\n```",
    bestPractice: "Default to headless everywhere for speed and CI-compatibility, and only carve out a specific, narrow project/test group that genuinely requires headed mode (documented with *why*) rather than assuming a headless failure means headless mode itself is untrustworthy.",
    wrongFixes: "1. Switching the entire CI suite to headed mode to \"match local\" — usually impossible or extremely awkward in a CI environment with no real display (requires a virtual framebuffer like Xvfb) and slower across the board for no benefit to most tests.\n2. Adding broad try/catch swallowing around the specific assertion that differs — hides a real, possibly user-facing rendering/focus difference instead of understanding it.\n3. Assuming \"headless is just less reliable\" as a blanket explanation without confirming the specific mechanism (focus, GPU, media) actually at play.",
    interviewAngle: "\"A test passes headed but fails headless in CI — what specific categories of difference would you check first?\" — senior answer: focus/window-activation behavior, GPU-accelerated rendering paths, and media/codec support are the main categories where headless and headed still meaningfully differ — check those specifically rather than assuming headless mode is generally unreliable.",
    related: "stuck-parallel-ci-passes-locally-fails-ci, stuck-parallel-ci-docker-font-rendering",
  },
  {
    id: "stuck-parallel-ci-jenkins-provider-specific",
    title: "A test only fails on the Jenkins pipeline, not on GitHub Actions or locally",
    category: "parallel-ci",
    severity: "tricky",
    symptom: "The exact same test suite, same commit, passes on GitHub Actions and locally but fails specifically when run through a Jenkins pipeline.",
    why: "Provider-specific failures usually trace back to how each CI system's agents are provisioned and how the pipeline script itself invokes the suite: different base Docker images (or none — a bare VM with whatever browser dependencies happen to be pre-installed), different default environment variables, different working-directory/checkout behavior, or a Jenkinsfile that runs steps in a different order or with different flags (e.g., missing `--ci`-equivalent flags, a different `NODE_ENV`) than the GitHub Actions workflow.",
    debug: "1. Diff the Jenkinsfile's actual invocation of the test command against the GitHub Actions workflow's — flags, env vars, working directory, and setup steps line by line.\n2. Check what base image/agent Jenkins uses and whether it matches the browser/OS dependency versions Playwright expects (or whether `npx playwright install --with-deps` even runs there).\n3. Capture Jenkins' own console output and any Playwright HTML/trace artifacts — configure the pipeline to archive them if it doesn't already, since a Jenkins-only failure you can't see artifacts for is far harder to diagnose than one you can.",
    fix: "```groovy\n// Jenkinsfile — mirror the GitHub Actions workflow's exact setup steps\nstage('Test') {\n  steps {\n    sh 'npx playwright install --with-deps'\n    sh 'npx playwright test --workers=4'\n  }\n  post {\n    always {\n      archiveArtifacts artifacts: 'playwright-report/**', allowEmptyArchive: true\n    }\n  }\n}\n```",
    bestPractice: "Keep exactly one canonical Docker image (ideally Playwright's official image, pinned to a specific tag) used by *every* CI provider running the suite, and treat any pipeline-specific setup script as a thin wrapper around the same install/run commands — never let two providers' environments drift independently.",
    wrongFixes: "1. Adding Jenkins-specific `sleep`/retry hacks directly in the Jenkinsfile to \"get it green\" — papers over an environment mismatch instead of aligning it with the known-working GitHub Actions setup.\n2. Skipping the failing tests specifically when `process.env.JENKINS_HOME` is set — actively hides a real environment gap rather than fixing it, and creates two different \"true\" states of the suite depending on which CI ran it.\n3. Abandoning Jenkins for that suite without root-causing the actual environment mismatch — may just relocate the same fragility to whatever replaces it.",
    interviewAngle: "\"The same suite passes on GitHub Actions but fails on Jenkins — how do you find the difference?\" — senior answer: diff the exact invocation (flags, env vars, base image) between the two pipelines line by line — provider-specific failures are almost always an environment/setup mismatch, not a genuinely different test result.",
    related: "stuck-parallel-ci-passes-locally-fails-ci, stuck-parallel-ci-docker-font-rendering",
  },
  {
    id: "stuck-parallel-ci-passes-locally-fails-ci",
    title: "\"It passes locally but fails in CI\" — the full triage tree",
    category: "parallel-ci",
    severity: "common",
    symptom: "A test is 100% reliable on a developer's machine and fails — consistently or intermittently — the moment it runs in the CI pipeline, with no code difference between the two runs.",
    why: "\"CI\" isn't one variable, it's a bundle of differences from local: headless vs. headed rendering, a different OS (often Linux containers vs. a developer's macOS/Windows), different fonts installed, more constrained CPU/memory causing everything to run slower, real parallelism across more workers than a developer typically runs locally, and a cold cache/cold start with no browser profile warm-up. Treating \"fails in CI\" as one bug category prevents diagnosing which of these several genuinely different causes is actually responsible.",
    debug: "1. Reproduce with the *same* conditions CI uses: `--workers=N` matching CI's worker count, headless mode, and ideally inside the same Docker image CI uses.\n2. Download the CI run's trace/video/screenshot artifacts first — don't start theorizing before looking at what CI actually saw.\n3. Bisect the difference systematically: run locally with just headless mode changed, then just worker count changed, then inside the CI Docker image, to isolate which single factor reproduces the failure.",
    fix: "```bash\n# Reproduce CI's exact conditions locally before touching any test code.\nnpx playwright test --workers=4 --project=chromium # match CI's worker count\ndocker run --rm -v $PWD:/work -w /work mcr.microsoft.com/playwright:v1.55.0-jammy \\\n  npx playwright test --workers=4\n```",
    bestPractice: "Make \"reproduce locally under CI's exact conditions\" (Docker image, worker count, headless) the very first triage step for any CI-only failure, before writing a single line of speculative test-code changes — most CI-only failures map cleanly onto one of: rendering/font differences, resource-constrained timing, or real parallel-execution races.",
    wrongFixes: "1. Adding `retries: 3` to the CI config as a blanket first response — sometimes genuinely appropriate for known-flaky externalities, but as a *first* response it hides which of the several real CI-specific causes is at play.\n2. Increasing all timeouts uniformly \"since CI is slower\" without confirming that's actually the specific cause here — masks other categories (parallelism races, font rendering) that a bigger timeout won't fix at all.\n3. Disabling the failing test in CI only — the fastest way to quietly lose real coverage without ever finding out what CI was telling you.",
    interviewAngle: "\"A test is 100% green locally and consistently red in CI — how do you approach it?\" — senior answer: reproduce CI's exact conditions locally first (Docker image, worker count, headless), then bisect one variable at a time — most \"CI-only\" failures are rendering differences, resource-constrained timing, or real parallelism races, not random flakiness.",
    related: "stuck-parallel-ci-docker-font-rendering, stuck-parallel-ci-headless-vs-headed",
  },
  {
    id: "stuck-parallel-ci-retries-hiding-bugs",
    title: "`retries: 2` is quietly hiding a real, reproducible bug",
    category: "parallel-ci",
    severity: "tricky",
    symptom: "The CI dashboard shows all-green, but the Playwright HTML report (or `flake-report.mjs`-style summary) shows a specific test consistently needing its second or third retry attempt to pass — meaning it's genuinely failing on attempt one, every single run, and nobody's looking at why.",
    why: "`retries` exists to absorb genuine, rare environmental flakiness (a one-in-a-hundred network blip) — but a test that reliably fails on attempt one and reliably passes on attempt two isn't flaky in that sense, it has a *deterministic* race or ordering bug that retries happen to dodge (e.g., attempt one always hits a cold-cache timing edge that's warmed up by the time attempt two runs). A blanket-green CI status without looking at retry counts hides this completely.",
    debug: "1. Check the HTML report's per-test retry count, or run a flake-summary tool like this repo's `tools/flake-report.mjs` across recent runs — look for any test with a consistent (not random) need for retry.\n2. Compare the trace of the *failed* first attempt against the *passed* second attempt for the same test run — what's actually different between them (usually: something warmed up, like a cache or a lazily-initialized service)?\n3. Treat \"always needs exactly one retry\" as more suspicious than \"fails randomly 1 in 50 runs\" — consistency is a signal of a deterministic bug, not luck.",
    fix: "```ts\n// tools/flake-report.mjs (already in this repo) — surface retry-needing tests\n// as an explicit, reviewed signal instead of letting a green CI badge hide them.\n```\n```bash\nnode tools/flake-report.mjs playwright-report/results.json\n# Treat any test appearing here with retries > 0 as a bug ticket, not a shrug.\n```",
    bestPractice: "Monitor retry counts as a first-class CI signal (fail the build, or at minimum flag it loudly, if a specific test needs a retry on every run) rather than only looking at final pass/fail — a consistently-retried test is telling you something reproducible is wrong on the first attempt.",
    wrongFixes: "1. Raising `retries` from 2 to 3 \"to be extra safe\" when a test still occasionally fails all attempts — treats a worsening deterministic bug as needing more chances instead of investigation.\n2. Ignoring retry-count data because \"the build is green anyway\" — the single most common way real, fixable bugs sit unaddressed for months.\n3. Removing retries entirely without first fixing the underlying causes they were masking — makes CI newly flaky/red for bugs that were always there, which is directionally right but disruptive if done without warning the team first.",
    interviewAngle: "\"CI is green, but you notice a test needs its second retry attempt on every single run — is that a problem?\" — senior answer: yes — a test that deterministically fails on attempt one and passes on attempt two has a reproducible race/ordering bug, not random flakiness, and a green badge alone hides it; monitor retry counts explicitly, not just final status.",
    related: "stuck-flaky-debug-green-nine-of-ten, stuck-parallel-ci-passes-locally-fails-ci",
  },
  {
    id: "stuck-parallel-ci-sharding-uneven",
    title: "Sharding leaves one shard taking far longer than the others",
    category: "parallel-ci",
    severity: "tricky",
    symptom: "A suite split across N shards for CI parallelism finishes N-1 shards quickly, while one shard consistently takes several times longer — turning \"parallel\" sharding into \"wait for the slowest one\" in practice.",
    why: "Playwright's default sharding distributes *tests*, not *time* — if it splits purely by file/test count without knowledge of actual historical duration, a shard that happens to receive several genuinely slow tests (heavy visual regression suites, long E2E flows) will take much longer than a shard full of fast unit-style checks, even though both shards have a similar test *count*.",
    debug: "1. Compare the reported duration of each shard from recent CI runs — is it consistently the same shard index that's slow, or does it vary?\n2. Check whether the slow shard happens to contain a cluster of known-heavy tests (visual regression, cross-browser, long wizards).\n3. Check whether Playwright's blob-report-based shard merging is being used, which unlocks duration-aware shard planning in newer Playwright versions.",
    fix: "```yaml\n# .github/workflows/practice-suite.yml — shard by a stable total, and merge blob\n# reports afterward so flake/duration data informs *future* shard balancing.\nstrategy:\n  matrix:\n    shard: [1, 2, 3, 4]\nsteps:\n  - run: npx playwright test --shard=${{ matrix.shard }}/4 --reporter=blob\n  - uses: actions/upload-artifact@v4\n    with: { name: blob-report-${{ matrix.shard }}, path: blob-report }\n```\n```bash\n# Merge job: use the merged report's per-test durations to rebalance shard\n# groupings by moving known-slow specs into their own dedicated shard.\nnpx playwright merge-reports --reporter=html ./all-blob-reports\n```",
    bestPractice: "Periodically review merged-report duration data and deliberately group consistently-slow tests (visual regression, long E2E flows) so they're spread evenly across shards rather than left to chance file-based splitting — treat shard balance as something to actively tune, not a \"set once and forget\" default.",
    wrongFixes: "1. Adding more shards indiscriminately without addressing the imbalance — the same slow tests still cluster on one shard, now among fewer other tests, potentially making the imbalance proportionally worse.\n2. Manually moving individual test files between shard groups by trial and error with no duration data to guide it — a guess that can silently become wrong again as the suite evolves.\n3. Reducing coverage by removing the slow tests specifically to make shards even — trades real coverage for a scheduling convenience.",
    interviewAngle: "\"One CI shard consistently takes three times longer than the others — why, and how do you fix it?\" — senior answer: default sharding balances by test count, not duration; use merged-report duration data to deliberately redistribute known-slow tests (visual/E2E) evenly across shards instead of leaving it to chance.",
    related: "stuck-parallel-ci-passes-locally-fails-ci, stuck-login-auth-storage-expired",
  },
  {
    id: "stuck-parallel-ci-viewport-diff",
    title: "Viewport size differs between local and CI, breaking layout-dependent tests",
    category: "parallel-ci",
    severity: "common",
    symptom: "A test asserting on a responsive layout behavior (a hamburger menu appearing, a sidebar collapsing) passes locally and fails in CI, or vice versa, with the DOM structure looking otherwise identical.",
    why: "If `viewport` isn't explicitly configured, Playwright's default viewport size may differ from whatever a developer's local headed browser window happens to be sized to, or from an assumption baked into the test — responsive breakpoints are viewport-driven, so an unpinned viewport means the test's \"which layout am I even looking at\" is left to chance/defaults rather than an explicit decision.",
    debug: "1. Log the actual viewport size (`page.viewportSize()`) at the point of failure in both environments.\n2. Check `playwright.config.ts` and any `test.use({ viewport: ... })` overrides for whether the size is explicitly pinned or left to Playwright's default.\n3. Confirm which breakpoint the app's CSS switches layouts at, and whether the observed viewport sizes straddle that breakpoint differently.",
    fix: "```ts\n// playwright.config.ts — pin viewport explicitly per project rather than relying\n// on framework defaults that could change or differ across environments.\nexport default defineConfig({\n  projects: [\n    { name: 'desktop', use: { viewport: { width: 1280, height: 800 } } },\n    { name: 'mobile', use: { viewport: { width: 390, height: 844 } } },\n  ],\n});\n```",
    bestPractice: "Always pin `viewport` explicitly per project rather than depending on Playwright's default, and write responsive-layout tests as dedicated `desktop`/`mobile`-tagged projects that each assert the layout appropriate to their pinned size — never assume \"the current default\" viewport for anything layout-sensitive.",
    wrongFixes: "1. Adding `page.setViewportSize()` mid-test as an ad hoc patch in just the one failing test — inconsistent with the rest of the suite and easy to forget for the next similar test.\n2. Writing a layout assertion that tries to handle \"either\" viewport with an `.or()` fallback — masks which layout was actually intended to be tested at that point.\n3. Assuming CI's default viewport \"should just match\" a developer's local browser window without ever pinning either — leaves both to chance.",
    interviewAngle: "\"A responsive-layout test passes locally but fails in CI with no code change — what's the first thing to check?\" — senior answer: whether `viewport` is explicitly pinned or left to a framework default that may differ between environments — responsive tests should always run against an explicit, dedicated viewport per breakpoint, not an ambient default.",
    related: "stuck-parallel-ci-docker-font-rendering, stuck-locators-hidden-vs-visible",
  },
  {
    id: "stuck-parallel-ci-worker-race-shared-user",
    title: "Parallel workers race on a shared test user account",
    category: "parallel-ci",
    severity: "common",
    symptom: "Multiple specs that each log in as the same hardcoded test account intermittently fail with unexpected state (wrong balance, unexpected logged-out state, data left over from a different test) only when the suite runs with more than one worker.",
    why: "Test workers run genuinely concurrently; a shared account's state (session, balance, whatever a previous test mutated) is a single, mutable resource with no built-in isolation between two workers both acting on it \"at the same time\" from the account's perspective — one worker's action can and will interleave with another's mid-test.",
    debug: "1. Search the suite for a hardcoded username/account shared across more than one spec file.\n2. Reproduce with `--workers=1` (should pass reliably) versus the CI worker count (should show the race) to confirm the hypothesis.\n3. Check whether the failure's specific symptom (unexpected balance, unexpected logout) matches what a second worker's concurrent action on the same account would produce.",
    fix: "```ts\n// fixtures/personas.ts — provision one distinct account per worker\nexport const PERSONAS = {\n  apex_user_w0: { password: process.env.APEX_W0_PASS! },\n  apex_user_w1: { password: process.env.APEX_W1_PASS! },\n  // ...\n};\n\ntest('uses a per-worker persona, never a shared one', async ({ page }, testInfo) => {\n  const persona = Object.values(PERSONAS)[testInfo.workerIndex % Object.keys(PERSONAS).length];\n  // ...log in with `persona`, fully isolated from other workers...\n});\n```",
    bestPractice: "Provision (or dynamically create via API) one account per parallel worker, keyed by `testInfo.workerIndex`, so no two workers ever touch the same underlying account state — this is the single highest-leverage fix for this entire class of parallel-execution flake.",
    wrongFixes: "1. Serializing the affected specs with `test.describe.serial()` — fixes the race by removing parallelism for those tests, which slows the suite and doesn't scale as more shared-account tests are added.\n2. Adding a retry so the \"losing\" worker's test passes on a second attempt — non-deterministic, since the second attempt can race against a *different* worker instead.\n3. Adding a manual lock file/semaphore the tests wait on before touching the shared account — reimplements per-worker isolation by hand, worse, when per-worker accounts avoid the problem entirely.",
    interviewAngle: "\"Tests using the same test account fail intermittently, only under parallel execution — what's the fix?\" — senior answer: provision one account per worker so no two workers ever share mutable account state — this is a test-data-isolation problem, not something serialization or retries should paper over.",
    related: "stuck-login-auth-parallel-logout, stuck-files-data-worker-data-collision",
  },
  {
    id: "stuck-waits-timing-animation-covered-click",
    title: "An in-progress animation blocks a click that should just work",
    category: "waits-timing",
    severity: "common",
    symptom: "`click()` on an element mid-transition (a sliding panel, an expanding accordion, a modal fade-in) intermittently times out or clicks the wrong coordinates — but only occasionally, and never in a slow, manual walkthrough.",
    why: "Playwright's actionability checks require an element's bounding box to be *stable* across consecutive animation frames before clicking — this correctly refuses to click a target that's still moving, since a real click at the \"wrong\" moment could land on the wrong pixel. If a test happens to run its click call right as an unrelated animation is mid-flight, it can hit this stability check and either wait it out (usually fine) or, on tighter timeouts, time out.",
    debug: "1. Confirm from the trace whether the target (or something overlapping it) was mid-CSS-transition at the click moment — screenshots a few frames apart around the failure will show visible movement.\n2. Check the animation's duration against the action timeout — a slow/janky CI runner can make an animation that's \"fast enough\" locally take noticeably longer under load.\n3. Distinguish this from a permanently-stuck overlay (see the hidden-vs-visible entry) — here the element *does* eventually stabilize, it just takes longer than expected sometimes.",
    fix: "```ts\n// Let Playwright's own actionability/stability check do its job — usually no\n// change is needed beyond a slightly more generous action timeout for CI:\nawait page.getByRole('button', { name: 'Confirm' }).click({ timeout: 10_000 });\n\n// If the animation is purely decorative, respecting reduced-motion in the test\n// environment removes the whole class of flake at the source:\nawait page.emulateMedia({ reducedMotion: 'reduce' });\n```",
    bestPractice: "Where the animation is purely cosmetic, run the test environment with `prefers-reduced-motion: reduce` emulated — this removes an entire category of animation-timing flakiness without weakening any real assertion, since the test still verifies the same end state.",
    wrongFixes: "1. Reaching for `{ force: true }` to click through the instability check — clicks at possibly-wrong coordinates mid-animation, which can silently interact with the wrong element or miss entirely on a still-moving target.\n2. Adding a `waitForTimeout` tuned to \"roughly the animation duration\" — brittle to any future duration change and adds latency to every run.\n3. Disabling all CSS animations globally via a stylesheet override injected only in tests — can be a reasonable choice for pure performance, but doing it silently without deciding to (rather than as a deliberate `prefers-reduced-motion` policy) hides a signal you might want for other tests.",
    interviewAngle: "\"A click on an animating element flakes only under CI load, never locally — what's happening and what's the right fix?\" — senior answer: Playwright's actionability check requires bounding-box stability across frames; a slower runner extends how long an animation takes to settle. The right fix is emulating `prefers-reduced-motion: reduce` for cosmetic animations, not `force: true`.",
    related: "stuck-locators-hidden-vs-visible, stuck-flaky-debug-visual-flaky-animation",
  },
  {
    id: "stuck-waits-timing-clock-dependent-otp",
    title: "An OTP code expires before the test can submit it",
    category: "waits-timing",
    severity: "tricky",
    symptom: "A 2FA/OTP test that reads a short-lived code (e.g., a 60-second expiry) and submits it fails intermittently specifically on a slower CI run or a slower worker, with an \"OTP expired\" error the test never intended to trigger.",
    why: "Real wall-clock time keeps advancing while the test does its normal (variable-speed) work between reading the code and submitting it — a code with a short TTL leaves very little margin, and CI environments are exactly where execution speed varies most run to run. Relying on real time for something this time-sensitive makes the test's pass/fail depend on incidental execution speed rather than actual application behavior.",
    debug: "1. Confirm the OTP's actual TTL and measure how long the test typically takes between reading and submitting the code — is the margin genuinely thin?\n2. Check whether failures correlate with CI load/slowness rather than a specific test change.\n3. Check whether the app exposes (or could expose, for its own testability) a controllable clock via `page.clock` instead of relying on real elapsed time.",
    fix: "```ts\n// Control time explicitly instead of racing the real clock — see the Bank Demo's\n// own Clock-API-driven OTP tests for a working example of this pattern.\ntest('OTP submitted well within its TTL using a controlled clock', async ({ page }) => {\n  await page.clock.install();\n  await page.goto('/index.html#bank-demo');\n  // ...trigger OTP...\n  await page.clock.fastForward('00:00:10'); // advance a known, small amount\n  // ...submit the still-valid code...\n});\n\ntest('OTP correctly rejected once its TTL has passed', async ({ page }) => {\n  await page.clock.install();\n  await page.goto('/index.html#bank-demo');\n  // ...trigger OTP...\n  await page.clock.fastForward('00:01:05'); // deliberately past a 60s TTL\n  // ...assert the app now rejects it...\n});\n```",
    bestPractice: "Use `page.clock` to make time-dependent behavior deterministic in both directions — test the valid-code path with a small, controlled advance, and explicitly test the expiry path by advancing past the TTL — rather than letting either path depend on how fast the test happens to execute.",
    wrongFixes: "1. Raising the OTP's TTL specifically for test environments — tests a materially different (more forgiving) timing behavior than production actually has.\n2. Adding retries so an occasional \"OTP expired\" failure passes on a faster second attempt — non-deterministic, and never actually tests the expiry path on purpose.\n3. Speeding up the test's own code (fewer awaits, tighter polling) to \"beat the clock\" — an arms race against CI variability instead of removing the dependency on real time.",
    interviewAngle: "\"A 60-second OTP expiry test flakes only on slow CI runs — what's the fix?\" — senior answer: control time explicitly with `page.clock` rather than racing real wall-clock time — advance a small, known amount for the valid-code path, and deliberately advance past the TTL to test expiry, so neither path depends on incidental execution speed.",
    related: "stuck-login-auth-mfa-otp, stuck-login-auth-session-expired-midform",
  },
  {
    id: "stuck-waits-timing-networkidle-trap",
    title: "`networkidle` \"waits forever\" or resolves too early",
    category: "waits-timing",
    severity: "common",
    symptom: "Using `page.waitForLoadState('networkidle')` either hangs well past when the page is visibly ready (a page with a persistent polling request, websocket, or analytics beacon never truly goes idle), or resolves before the actual content a test needs has rendered.",
    why: "`networkidle` waits for \"no network connections for 500ms\" — it says nothing about whether the *specific element or data* your test cares about has arrived. Modern apps routinely have long-lived connections (websockets, SSE, polling analytics) that mean the network is *never* truly idle, or conversely finish an initial burst of requests before the JS that renders your target has even run.",
    debug: "1. Check the network panel for anything long-lived (websocket upgrade, SSE `EventSource`, repeating poll) — if present, `networkidle` is fundamentally the wrong tool here.\n2. Time how long the hang actually is: does it match roughly \"polling interval + 500ms,\" confirming the theory?\n3. Identify the actual UI signal that means \"ready\" — a specific element appearing, a loading spinner disappearing — and wait on that instead.",
    fix: "```ts\n// Before: fights a page with a persistent poll/websocket\nawait page.goto('/dashboard');\nawait page.waitForLoadState('networkidle'); // may hang or resolve too early\n\n// After: wait on the actual signal the test cares about\nawait page.goto('/dashboard');\nawait expect(page.getByTestId('dashboard-ready')).toBeVisible();\n```",
    bestPractice: "Avoid `networkidle` entirely for anything other than very simple static pages — prefer a web-first assertion on the specific element/state the test needs, which is both faster (doesn't wait for an arbitrary idle window) and more accurate (doesn't get fooled by unrelated background traffic).",
    wrongFixes: "1. Combining `networkidle` with a follow-up `waitForTimeout` \"just in case\" — stacks two unreliable waits instead of replacing them with one accurate one.\n2. Increasing the idle window's implicit timeout repeatedly — doesn't fix a page that will never go idle by design.\n3. Disabling the app's polling/websocket specifically in the test environment — tests a materially different runtime behavior than production.",
    interviewAngle: "\"Why is `networkidle` considered an anti-pattern in modern Playwright tests?\" — senior answer: it measures absence of network activity, not readiness of the specific content under test, and long-lived connections (websockets, polling) can make it hang indefinitely or resolve early — a targeted web-first assertion on the actual ready-state is both faster and more accurate.",
    related: "stuck-waits-timing-spinner-never-detaches, stuck-network-api-graphql-assertion",
  },
  {
    id: "stuck-waits-timing-response-race",
    title: "`waitForResponse` registered after the click misses the request",
    category: "waits-timing",
    severity: "common",
    symptom: "`page.waitForResponse(...)` times out even though the network panel in a manual run clearly shows the expected request firing and succeeding — it just doesn't seem to be \"seen\" by the test.",
    why: "This is a classic ordering race: if you `click()` first and *then* call `waitForResponse()`, the response may already have arrived (the request can be very fast) before the listener was registered, so the wait genuinely misses an event that already happened. `waitForResponse` doesn't retroactively see already-completed network activity.",
    debug: "1. Read the code order literally: is `waitForResponse` called before or after the action that triggers the request?\n2. Check the trace's network panel timestamp for the request versus the timestamp the waitForResponse call started — if the request completed first, that confirms the race.\n3. Confirm this reproduces more often on faster environments/mocked responses (which complete request+response almost instantly), consistent with a race rather than a genuine absence of the request.",
    fix: "```ts\n// Before: click first, wait second — a race the click can win\nawait page.getByRole('button', { name: 'Save' }).click();\nconst response = await page.waitForResponse('**/api/save'); // may already be too late\n\n// After: start waiting before triggering the action\nconst [response] = await Promise.all([\n  page.waitForResponse((r) => r.url().includes('/api/save') && r.ok()),\n  page.getByRole('button', { name: 'Save' }).click(),\n]);\n```",
    bestPractice: "Always pair `waitForResponse`/`waitForRequest`/`waitForEvent` with the triggering action inside `Promise.all`, registering the wait first — never call the wait after the action that might complete before the listener exists.",
    wrongFixes: "1. Adding a `waitForTimeout` before calling `click()` \"to slow things down\" so the wait has time to register — doesn't fix an ordering bug, and adds latency to every run regardless of the race actually occurring.\n2. Wrapping the whole sequence in a retry loop — sometimes \"passes\" by luck (slow environment happens to lose the race less often) while leaving the actual ordering bug in place.\n3. Switching to a generic `waitForLoadState('networkidle')` instead — trades a specific, correct wait for an imprecise one with its own problems (see the networkidle entry).",
    interviewAngle: "\"`waitForResponse` sometimes times out even though the network tab shows the request succeeded — why?\" — senior answer: the wait was likely registered after triggering the action, so a fast request/response cycle already completed before the listener existed; always wrap the wait and the trigger together in `Promise.all`, wait first.",
    related: "stuck-network-api-route-not-intercepting, stuck-waits-timing-timeout-30000-triage",
  },
  {
    id: "stuck-waits-timing-slow-page-load",
    title: "A page takes six minutes to load and the default timeout isn't enough",
    category: "waits-timing",
    severity: "rare",
    symptom: "A specific page (a heavy analytics dashboard, a large report) genuinely, legitimately takes minutes to become interactive — not a bug, just a slow page — and `page.goto()`'s default timeout fails the test before it ever gets a chance to load.",
    why: "Playwright's default navigation timeout (30s) assumes a normal page; a small number of legitimately heavy pages exceed that by design (large data payloads, expensive server-side rendering, big client-side computation). The fix is scoping a longer timeout specifically to that navigation, not changing global defaults for the whole suite.",
    debug: "1. Confirm the slowness is real and expected (check with product/eng whether 6 minutes is \"working as intended\" or itself a bug worth filing) before writing a test that treats it as normal forever.\n2. Measure the actual typical load time with a few manual/trace runs to pick a sane, generous-but-bounded timeout rather than an arbitrary huge number.\n3. Check whether the page has an earlier \"good enough\" ready-state (e.g., the shell renders quickly, only one heavy chart is slow) that a more targeted wait could use instead of waiting on full load.",
    fix: "```ts\ntest('heavy analytics dashboard eventually loads', async ({ page }) => {\n  await page.goto('/reports/annual', { timeout: 120_000 }); // scoped, not global\n  await expect(page.getByTestId('report-ready')).toBeVisible({ timeout: 120_000 });\n});\n```",
    bestPractice: "Scope generous timeouts to the *specific* slow operation via a per-call `timeout` option, keep the global default tight so genuine hangs elsewhere still fail fast, and consider tagging genuinely-slow tests separately so they don't block a fast PR-gate suite.",
    wrongFixes: "1. Raising the global `timeout`/`actionTimeout`/`navigationTimeout` in `playwright.config.ts` for the whole suite — every other test's real hangs now take minutes to fail instead of seconds.\n2. Polling in a manual loop with `waitForTimeout` checking `document.readyState` — reimplements what a scoped timeout + web-first assertion already does, worse.\n3. Skipping the slow page from automated coverage entirely — silently drops real coverage of a page that (being slow) is exactly the kind of page worth regression-testing for performance.",
    interviewAngle: "\"One specific page in your app legitimately takes minutes to load — how do you test it without slowing down the whole suite?\" — senior answer: scope a generous timeout to that specific navigation/assertion only, keep the suite-wide default tight, and consider separating genuinely-slow tests into their own tagged group.",
    related: "stuck-waits-timing-timeout-30000-triage, stuck-network-api-slow-3g-simulation",
  },
  {
    id: "stuck-waits-timing-spinner-never-detaches",
    title: "A loading spinner never detaches, so the wait never resolves",
    category: "waits-timing",
    severity: "tricky",
    symptom: "`await expect(page.getByTestId('spinner')).toBeHidden()` (or `.toHaveCount(0)`) times out — the spinner element is still technically present in the DOM (just visually transitioned to `opacity: 0` and hidden via animation) rather than actually removed or `display:none`.",
    why: "Some spinner implementations animate out (CSS transition/opacity fade) but leave the element attached with computed styles that don't cross Playwright's actual \"hidden\" threshold at the moment you check, or only reach `display:none` after an animation-end event that fires later than expected; others use a `visibility: hidden` on a wrapping element that your locator doesn't target.",
    debug: "1. Take a screenshot and dump computed styles (`opacity`, `display`, `visibility`) of the spinner element at the moment of the timeout to see its actual state.\n2. Check whether the spinner is removed from the DOM entirely on completion (in which case wait for it to detach) versus staying present but styled hidden (in which case `toBeHidden()` should already work, and something else is off — check you're targeting the right element, not a wrapper).\n3. Check for a CSS transition duration much longer than expected as a possible root cause on its own (a genuine, separate front-end bug worth reporting).",
    fix: "```ts\n// If the spinner element is removed from the DOM on completion:\nawait expect(page.getByTestId('spinner')).toHaveCount(0);\n\n// If it stays in the DOM but should become hidden:\nawait expect(page.getByTestId('spinner')).toBeHidden();\n\n// If completion is really signaled by content appearing, prefer waiting on that instead:\nawait expect(page.getByTestId('results-list')).toBeVisible();\n```",
    bestPractice: "Prefer asserting on the *positive* signal (the real content appearing) over the *negative* signal (a loading indicator disappearing) wherever both exist — a spinner's exact hide mechanics are an implementation detail that can change, while \"the data the user wanted is now visible\" is the thing that actually matters.",
    wrongFixes: "1. Adding a fixed sleep timed to \"roughly how long the fade takes\" — brittle to any future animation-duration change, and slower than necessary on faster runs.\n2. Asserting `opacity: 0` via `page.evaluate()` computed-style reads in a manual poll loop — reimplements what a web-first assertion already does, with more code and less reliability.\n3. Removing/disabling the spinner's fade animation \"for tests only\" via a test-mode CSS override — tests a different visual behavior than what real users see.",
    interviewAngle: "\"A loading-spinner wait never resolves even though the page is visibly ready — what do you check?\" — senior answer: whether the spinner actually leaves the DOM/becomes truly hidden by Playwright's definition versus just visually fading via CSS transition with the element still present in an ambiguous state — and consider asserting on the positive 'content appeared' signal instead.",
    related: "stuck-waits-timing-networkidle-trap, stuck-locators-hidden-vs-visible",
  },
  {
    id: "stuck-waits-timing-timeout-30000-triage",
    title: "Triaging a generic \"Timeout 30000ms exceeded\" error",
    category: "waits-timing",
    severity: "common",
    symptom: "`Error: Timeout 30000ms exceeded` with no further context beyond which call it happened on — the single most common Playwright error message, and the least specific on its own.",
    why: "This message means exactly one thing precisely: *the thing Playwright was polling for never became true within the timeout*. It says nothing yet about *why* — the element might never exist, might exist but stay hidden, might be covered by an overlay, or the whole app might have crashed. Treating this as one bug category instead of reading the specific action/assertion it decorates is the actual mistake.",
    debug: "1. Read the *rest* of the error, not just the headline — Playwright appends the specific actionability check that kept failing (e.g., \"waiting for element to be visible, enabled and stable\") or the locator's current match count.\n2. Open the trace and jump to the failing step — look at the DOM snapshot and screenshot at that exact moment.\n3. Check the console and network panels for errors around the same timestamp — a JS exception or a failed API call upstream often explains why the expected element never appeared.",
    fix: "```ts\n// Turn a generic timeout into a diagnosable one: assert on the specific\n// precondition first, so failures point at the right layer immediately.\nawait expect(page.getByRole('status')).toHaveText('Ready', { timeout: 10_000 });\nawait page.getByRole('button', { name: 'Continue' }).click();\n```",
    bestPractice: "Treat every 30-second timeout as an investigation, not a nuisance to silence — the trace almost always contains the real answer within the first 30 seconds of looking. Build tests with intermediate, specific assertions so a failure narrows down to the right step instead of one large multi-step action swallowing the signal.",
    wrongFixes: "1. Globally raising `timeout` to 60 or 90 seconds \"to be safe\" — masks real bugs and makes every genuinely-broken test take three times longer to fail.\n2. Adding `waitForTimeout(5000)` before the failing line \"to give it more time\" — doesn't fix a condition that was never going to become true, and adds latency to every passing run too.\n3. Wrapping the failing line in a retry loop — retries a symptom, not a cause; if the underlying condition is genuinely broken, retries just fail three more times instead of once.",
    interviewAngle: "\"You get a generic 30-second timeout — walk me through your triage.\" — senior answer: read the specific actionability detail Playwright appends, open the trace's DOM/screenshot/console/network at that moment, and diagnose from there — never treat the headline message alone as the diagnosis.",
    related: "stuck-flaky-debug-trace-viewer-timeout, stuck-locators-hidden-vs-visible",
  }
];

/* Scenario-based interview bank — rendered by app.js */
window.INTERVIEW_DATA = {
  hub: {
    title: "Scenario-based interview bank",
    lead: "117 scenario questions across four tiers (Beginner → Architect), plus the Interview essentials pack (HR, QA theory, BDD, Git, MCP). Read each question out loud, then check the ideal approach — the \"why they get stuck\" line is usually more useful than the answer.",
  },

  recommendations: {
    html: ` <strong>As a candidate (staged prep):</strong> 1. <em>Weeks 1–2:</em> Drill Tier A/B out loud until flakiness triage (reproduce → trace → categorize → fix), locator priority, storageState, and hybrid API+UI are automatic. Benchmark: you can explain a trace and fix a flaky test without saying "add a retry." 2. <em>Weeks 3–4:</em> For senior roles, build one real portfolio suite (POM + fixtures + sharded CI + merged report + visual + a11y) and rehearse Tier C answers out loud. 3. <em>For lead/architect roles:</em> Pick five Tier D questions at random and whiteboard each in five minutes, naming trade-offs and what you'd deliberately <em>not</em> build. If you can't name the thing you're choosing not to do, you're not done yet.<br><br><strong>As an interviewer (staged use):</strong> Screen in this order — locator strategy, flake diagnosis, maintenance/architecture economics. Use one Tier B scenario as a live "fix this flaky test" exercise; escalate to Tier D for lead/architect candidates, and prefer the ones with a concrete forcing function (a number, a deadline, a constraint from another team) over the abstract ones — they're harder to answer from memorized talking points. Red flags across every tier: "just add a retry," "always build/always buy," "POM for everything," treating a migration as a syntax swap, answering an organizational problem with a testing technique.<br><br><strong>Threshold to change approach:</strong> If a candidate nails mechanisms but can't reason about trade-offs or restraint, they're strong mid-level, not senior. If they lead with trade-offs and restraint before syntax, and have real war stories about flake, isolation, or migration, that's the senior/lead signal. `,
  },

  tierA: {
    id: 'interview-tier-a',
    title: "Tier A \u2014 Beginner / Fresher (0\u20132 years)",
    lead: "Scenario-based screening questions for 0\u20132 years. Read each scenario out loud, then compare to the ideal approach \u2014 the \"why they get stuck\" line tells you which instinct to override.",
    questions: [
      {
        id: 'a1',
        q: "A test passes locally but the element \"isn't found\" even though you can see it on the page. What do you check?",
        ideal: `<p>Confirm it isn't a timing issue first (Playwright auto-waits, but you may be reading state too early with a non-retrying check like <code>isVisible()</code> instead of <code>await expect(locator).toBeVisible()</code>). Then check: is the element inside an <strong>iframe</strong> (needs <code>frameLocator</code>) or a <strong>shadow root</strong> (open roots are pierced automatically; closed ones can't be reached)? Is the locator matching multiple elements (strict-mode violation)? Is it behind an overlay/modal (fails actionability "receives events")? Use the trace/Inspector "pick locator" to verify.</p>`,
        stuck: `<p>Freshers reach for <code>waitForTimeout</code> or <code>force:true</code> instead of diagnosing; they don't know iframes/shadow DOM change the lookup, and they confuse "in the DOM" with "actionable."</p>`,
      },
      {
        id: 'a2',
        q: "Your test uses `page.waitForTimeout(3000)` and still fails on the CI machine. Why, and what do you do?",
        ideal: `<p>Hard sleeps are both too short on slow CI and wasted time on fast machines. Replace with a <strong>web-first assertion</strong> on the observable end-state (<code>await expect(page.getByRole('alert')).toHaveText('Saved')</code>) or wait for the specific network response. Auto-waiting handles most cases.</p>`,
        stuck: `<p>They treat <code>waitForTimeout</code> as the fix rather than the cause; they don't know assertions auto-retry until <code>expect.timeout</code>.</p>`,
      },
      {
        id: 'a3',
        q: "The login form has fields with no stable IDs and auto-generated class names. How do you locate them reliably?",
        ideal: `<p>Prefer user-facing locators: <code>getByLabel('Email')</code>, <code>getByRole('textbox', {name: 'Email'})</code>, <code>getByPlaceholder(...)</code>. If none exist, ask devs to add <code>data-testid</code> (or configure <code>testIdAttribute</code>). Anchor CSS to a stable parent as a last resort; avoid brittle structural XPath like <code>//div[2]/form/button[1]</code>.</p>`,
        stuck: `<p>They default to copied CSS/XPath from DevTools and don't know the recommended locator priority order.</p>`,
      },
      {
        id: 'a4',
        q: "You need to test a \"Delete account?\" browser confirm dialog. How do you handle it?",
        ideal: `<p>Register <code>page.on('dialog', async d =&gt; { …; await d.accept(); })</code> <strong>before</strong> the click that triggers it; for prompts pass text to <code>accept('...')</code>. If no handler is registered, Playwright auto-dismisses. Distinguish native dialogs (<code>alert/confirm/prompt</code>) from in-page modals, which are just DOM elements clicked with normal locators.</p>`,
        stuck: `<p>They register the handler after the click (race), or try to locate a native alert with a selector.</p>`,
      },
      {
        id: 'a5',
        q: "A \"Sign in\" button appears twice on the page and your click throws a \"strict mode violation.\" How do you fix it?",
        ideal: `<p>Narrow the locator rather than disable strictness: scope to a parent (<code>page.getByRole('dialog').getByRole('button', {name:'Sign in'})</code>), <code>.filter({hasText})</code>, or explicitly <code>.first()/.nth()</code> only when position is truly what you want. Strict mode is a feature that catches ambiguous selectors early.</p>`,
        stuck: `<p>They reach for <code>.first()</code> reflexively (hiding a real ambiguity) or think strict mode is a bug.</p>`,
      },
      {
        id: 'a6',
        q: "How do you upload a file when the visible \"Upload\" control is a styled button, not a real file input?",
        ideal: `<p>Two options — call <code>setInputFiles()</code> directly on the hidden <code>input[type=file]</code>, or use the <code>filechooser</code> event: set up <code>page.waitForEvent('filechooser')</code>, click the styled button, then <code>chooser.setFiles(path)</code>. Buffers work for synthetic in-memory files.</p>`,
        stuck: `<p>They try to click the styled button and "type" a path; they don't know about the filechooser event or targeting the hidden input.</p>`,
      },
      {
        id: 'a7',
        q: "Clicking \"Export CSV\" triggers a download. How do you capture and verify it?",
        ideal: `<p>Set up <code>const downloadPromise = page.waitForEvent('download')</code> <strong>before</strong> clicking, then <code>const download = await downloadPromise; await download.saveAs(...)</code>. Verify <code>suggestedFilename()</code> or read the stream contents. It's a one-shot wait.</p>`,
        stuck: `<p>They click first then wait (missing the event), or expect a popup/dialog instead of a download event.</p>`,
      },
      {
        id: 'a8',
        q: "Clicking a link opens a new tab. How do you interact with the new tab?",
        ideal: `<p>Capture it via the context/page event before the click: <code>const [popup] = await Promise.all([context.waitForEvent('page'), link.click()])</code>; then <code>await popup.waitForLoadState()</code> and treat it as a normal Page. Note tabs in the same context share cookies.</p>`,
        stuck: `<p>They don't set up the event listener before the action; they confuse a native dialog with a new page.</p>`,
      },
      {
        id: 'a9',
        q: "Login takes 5+ seconds and every one of your tests logs in through the UI. What's the problem and the simplest first improvement?",
        ideal: `<p>Repeated UI login is slow and fragile. Log in once and reuse <strong>storageState</strong> (cookies + localStorage) across tests, injected via config <code>use.storageState</code> or a setup project. This is the single biggest speed win in most suites.</p>`,
        stuck: `<p>They know storageState exists but can't explain the setup-project/dependency wiring, or don't connect it to speed/stability.</p>`,
      },
      {
        id: 'a10',
        q: "Your test needs the app running at localhost:3000. How should the suite start it?",
        ideal: `<p>Use the <code>webServer</code> config block (command + url + <code>reuseExistingServer: !process.env.CI</code>) so the app boots before the suite and is reused locally but freshly started in CI. Removes "did you start the server?" as a human failure mode.</p>`,
        stuck: `<p>They start it manually or shell out in package.json; they don't know <code>webServer</code> exists.</p>`,
      },
      {
        id: 'a11',
        q: "How do you run the same test across Chromium, Firefox, and WebKit, and why would you bother?",
        ideal: `<p>Define <code>projects[]</code> in the config, one per browser/device; run all with <code>npx playwright test</code> or one with <code>--project=webkit</code>. Rationale: engines differ (WebKit = Safari's engine; catches Safari-only layout/focus/date bugs). Note bindings: same concept in Java (<code>projects</code> in config or parameterized runners).</p>`,
        stuck: `<p>They've only ever run Chromium and don't know projects are also how you split roles/environments.</p>`,
      },
      {
        id: 'a12',
        q: "A colleague wrote `page.click('#save')` without `await`. What happens?",
        ideal: `<p>A floating promise — the action fires but the test may move on before it resolves, causing intermittent failures. This is one of the most common flakiness sources. Enable the ESLint rule <code>@typescript-eslint/no-floating-promises</code> to catch it. (Node/TS-specific; in Java every call is synchronous so this class of bug doesn't exist — a good contrast point.)</p>`,
        stuck: `<p>Freshers don't grasp that every Playwright JS/TS call is async, and don't know lint can catch it.</p>`,
      },
      {
        id: 'a13',
        q: "A login test suddenly shows a CAPTCHA/verification challenge that wasn't there before. What's your first move?",
        ideal: `<p>Treat it as an environment/configuration signal, not something to defeat programmatically — CAPTCHA and bot-challenge systems exist specifically to tell automation apart from humans, so "solving" them inside a legitimate test suite is the wrong instinct. Get the challenge disabled or issued a test-only bypass key in non-production environments, coordinated with dev/security teams. If a challenge does appear in CI, the test should detect it and fail fast so the environment issue gets investigated — not route around it with solver services or stealth plugins.</p>`,
        stuck: `<p>Under pressure, candidates often jump to "I'd use a CAPTCHA-solving service or a stealth plugin," which is both fragile (constant upkeep as detection evolves) and, in a real org, a security/legal red flag. The strong answer redirects to environment ownership instead.</p>`,
      },
      {
        id: 'a14',
        q: "A delivery app should show \"no restaurants near you\" when location access is denied, and real results when it's granted for a specific city. How do you test both paths?",
        ideal: `<p>Grant or deny the <code>geolocation</code> permission at the context level, and separately set coordinates with <code>context.setGeolocation(...)</code> (or the <code>geolocation</code> config option). Test the denied-permission fallback UI and at least one granted-with-coordinates happy path. Permission and coordinates are two independent settings, and they apply to the whole context, not a single page.</p>`,
        stuck: `<p>They know geolocation can be mocked but forget it's two separate switches — granting permission without setting coordinates (or the reverse) silently produces the wrong test.</p>`,
      },
      {
        id: 'a15',
        q: "The same confirmation page must show \"$10.00\" in the US and \"10,00 \u20ac\" in Germany, with the German date shown day-first. How do you cover this?",
        ideal: `<p>Set <code>locale</code> (and usually <code>timezoneId</code>) per test — via <code>test.use({locale: 'de-DE', timezoneId: 'Europe/Berlin'})</code> or globally in config — then assert on the rendered currency/date strings for each locale. Keep it to a smoke check on formatting logic; testing every translated string belongs to localization/linguistic QA, not E2E.</p>`,
        stuck: `<p>Either they don't know <code>locale</code>/<code>timezoneId</code> are first-class context options, or they overcorrect and try to E2E-test every translated string, bloating the suite for no reliability gain.</p>`,
      },
      {
        id: 'a16',
        q: "The suite passes on desktop Chromium, but tapping a menu button does nothing when you emulate a Pixel device. Why?",
        ideal: `<p>Mobile emulation profiles set <code>hasTouch: true</code>, and touch-only controls may listen for <code>touchstart</code>/<code>touchend</code> rather than a mouse <code>click</code>. Use <code>locator.tap()</code> — which dispatches real touch events — instead of <code>.click()</code>, and confirm the emulated context truly has <code>hasTouch</code> enabled (<code>tap()</code> throws if it doesn't).</p>`,
        stuck: `<p>They treat <code>click()</code> as a universal stand-in for "user interaction" and don't consider that some UI code paths are genuinely touch-event-only.</p>`,
      },
      {
        id: 'a17',
        q: "A \"country\" field isn't a native `<select>` \u2014 it's a text input that opens a custom div-based list when focused. How do you pick \"Germany\"?",
        ideal: `<p>Treat it like ordinary UI, not <code>selectOption()</code> (which only targets real <code>&lt;select&gt;</code> elements): focus/click the input to open the list, then locate and click the "Germany" option by role/text within the listbox, and assert the list closed with the input now showing "Germany."</p>`,
        stuck: `<p>They reach for <code>selectOption()</code> out of habit and hit a confusing error, or type the value directly without confirming a real selection event actually fired.</p>`,
      },
      {
        id: 'a18',
        q: "Clicking \"Enable notifications\" triggers the real browser permission prompt. How do you test both accept and block?",
        ideal: `<p>Native permission prompts (notifications, camera, mic) are controlled the same way as geolocation: grant or deny at the context level before the triggering action, rather than trying to click a native OS-level dialog — which isn't part of the page DOM and can't be located.</p>`,
        stuck: `<p>They confuse this with the in-page <code>dialog</code> event used for <code>alert/confirm/prompt</code>, and try (and fail) to locate a native permission prompt as if it were a web element.</p>`,
      },
      {
        id: 'a19',
        q: "A \"You're offline\" banner should appear when connectivity drops and disappear when it's restored. How do you simulate that?",
        ideal: `<p><code>context.setOffline(true)</code> flips the emulated network to offline — triggering the app's own online/offline listeners — and <code>setOffline(false)</code> restores it. Assert the banner's visibility transitions in both directions, not just the offline state alone.</p>`,
        stuck: `<p>They think about killing the dev server or physically disconnecting, instead of realizing the test itself can flip this state.</p>`,
      },
      {
        id: 'a20',
        q: "Before the full page exists, design wants a test verifying a new `<Button>` component renders its label and fires `onClick` \u2014 without the whole app running. Possible?",
        ideal: `<p>Yes — Playwright's component-testing mode mounts the component in isolation via a <code>mount()</code> fixture, letting you assert on rendered output and simulate interaction without booting the full application. It's a distinct test type from E2E: faster feedback on component-level logic, not a replacement for user-journey tests.</p>`,
        stuck: `<p>Candidates who've only done E2E don't know Playwright has a component-testing mode at all, or assume "component test" must mean a Jest/Vitest unit test.</p>`,
      },
      {
        id: 'a21',
        q: "In a multi-step signup form, data entered on step 1 should still be there if the user clicks back after step 2. How do you verify this?",
        ideal: `<p>Fill and submit step 1, advance to step 2, then navigate back and assert the step-1 field still shows the value you entered — using a web-first assertion rather than reading the value once. This tests the form's actual state-persistence behavior (in memory, local storage, or a resubmitted draft), not just that step 1's fields exist.</p>`,
        stuck: `<p>They test that each step's fields exist and can be filled, but don't think to test the round trip — forward, then back — which is where multi-step forms actually lose data.</p>`,
      },
      {
        id: 'a22',
        q: "A teammate says \"the logo loads correctly,\" but an `<img>` tag existing in the DOM doesn't prove the image file itself loaded. How do you actually verify this?",
        ideal: `<p>An <code>&lt;img&gt;</code> element can exist — and even report as visible — while its <code>src</code> 404s or the file is corrupt. Check that the image actually rendered: assert <code>naturalWidth</code> is greater than zero via a page evaluation, or capture the network response for that specific asset request and assert its status is 200, rather than only asserting the element is present.</p>`,
        stuck: `<p>They equate "the element is in the DOM" or "visible" with "the resource behind it loaded successfully," missing that a broken image can still occupy layout space and pass a naive visibility check.</p>`,
      },
      {
        id: 'a23',
        q: "Test 1 logs in and lands on the dashboard. Test 2 assumes the user is still logged in and clicks a dashboard button \u2014 but test 2 fails, saying the button doesn't exist. Why?",
        ideal: `<p>By default, each test gets its own isolated page and browser context — nothing from test 1 carries over, including login state. Tests should never assume implicit continuation from whatever ran before them; a shared starting state has to be created explicitly (a <code>beforeEach</code> that logs in, or shared storageState), not assumed from file order.</p>`,
        stuck: `<p>Coming from tools where the same browser session persists across an entire script, candidates assume "one test after another" means "continuing the same session" — and are confused when Playwright's default isolation contradicts that.</p>`,
      },
      {
        id: 'a24',
        q: "A browser engine upgrade lands and 2,000 tests fail overnight, with no application code changes. What do you do?",
        ideal: `<p>First confirm it's really the browser update and not a coincident deploy — Playwright ships specific browser builds per version, so pinning the previous Playwright version temporarily rolls back the browser too, isolating the variable. Then triage by pattern, not one test at a time: a mass failure from a single root cause (a changed default, a stricter security policy, a rendering change) usually clusters around one code path — fix that path or add a compatibility shim, rather than touching 2,000 tests individually. Going forward, run new browser versions against the suite in a non-blocking job before adopting them, so this kind of jump gets caught before it's forced on everyone at once.</p>`,
        stuck: `<p>They start "fixing" tests one by one before confirming a single shared root cause, turning an hours-long investigation into a days-long slog.</p>`,
      },
      {
        id: 'a25',
        q: "Two different API calls that should return the same customer record return slightly different data \u2014 a test using each endpoint gives inconsistent results. How do you approach this?",
        ideal: `<p>This is a product data-consistency question wearing a test-flakiness costume — don't just pick whichever endpoint "usually passes." Confirm which endpoint is the actual source of truth for the field in question, and if both are meant to agree, treat the discrepancy as a bug to report (with both raw responses attached as evidence), not a test to adjust. Only change the test's expectations if product confirms the two endpoints are intentionally allowed to diverge (e.g., one is cached or eventually consistent).</p>`,
        stuck: `<p>They treat it as "the test is flaky" and adjust the assertion to whichever value shows up, silently hiding a genuine backend consistency bug.</p><p>---</p>`,
      },
    ],
  },

  tierB: {
    id: 'interview-tier-b',
    title: "Tier B \u2014 Intermediate / Mid-level (2\u20135 years)",
    lead: "Fixtures, auth, mocking, CI, and reliability scenarios for 2\u20135 years. Panels push past syntax here \u2014 diagnosis order matters more than memorised snippets.",
    questions: [
      {
        id: 'b1',
        q: "A test is green 9 runs out of 10 in CI but you can't reproduce it locally. Walk me through your process.",
        ideal: `<p>Reproduce under stress: <code>npx playwright test file:line --repeat-each=100</code> (and throttle CPU / raise <code>--workers</code> to mimic contended CI). Disable retries while investigating so flakiness isn't hidden. Open the <strong>trace</strong> (<code>trace: 'on-first-retry'</code>): inspect the DOM snapshot at the failing action, the network panel (did an API never resolve?), the console, and the longest bar on the timeline. Categorize: async/wait, resource contention, selector, order-dependency, environment drift, or test bug. Fix root cause, then re-run <code>--repeat-each</code> to confirm.</p>`,
        stuck: `<p>They "just add a retry" or bump timeouts; they don't know <code>--repeat-each</code>/CPU throttling to reproduce, or how to read a trace to localize the failing action.</p>`,
      },
      {
        id: 'b2',
        q: "Under heavy parallel load, a test times out waiting for a backend response that's fine locally. What's happening and how do you stabilize it?",
        ideal: `<p>The backend is slower because many workers hammer it simultaneously; it's a resource/timing issue, not a code bug. Options: start the wait before the trigger (<code>waitForResponse</code> set up before the click), route-intercept known-slow non-critical endpoints, seed state via API instead of clicking through prerequisites, reduce worker count against a capacity-limited staging env, or add capacity. Assert on the user-visible end-state.</p>`,
        stuck: `<p>They raise the timeout globally instead of recognizing shared-environment capacity; they don't know network-first wait patterns.</p>`,
      },
      {
        id: 'b3',
        q: "You need an authenticated session for admin, editor, and viewer roles across many tests. Design it.",
        ideal: `<p>A setup project (or worker-scoped fixture) logs in once per role and saves <code>auth/&lt;role&gt;.json</code> via <code>storageState()</code>. Dependent projects load the right file with <code>use.storageState</code>; per-test override with <code>test.use({storageState:'auth/admin.json'})</code>. For token-based apps, hit the auth API and inject the token (<code>addInitScript</code> to localStorage or <code>extraHTTPHeaders</code>) — faster than UI login. Address MFA, expiry, and per-role variation.</p>`,
        stuck: `<p>They save one shared state and mutate it across tests, or can't articulate token-injection vs UI-login trade-offs, or forget MFA/expiry.</p>`,
      },
      {
        id: 'b4',
        q: "A test must set up a specific order, then verify it in the UI. How do you make it fast and reliable?",
        ideal: `<p>Hybrid API+UI: create the order via <code>request.post('/api/orders', …)</code> (the fast, stable door), capture the returned id, then <code>page.goto</code> and assert the order appears in the UI. Two wins: much faster than clicking prerequisites, and it isolates the order-history UI regression from the order-placement UI.</p>`,
        stuck: `<p>They drive the entire setup through the UI (slow, flaky), or don't realize the <code>request</code> fixture can share/seed state.</p>`,
      },
      {
        id: 'b5',
        q: "You want deterministic tests of a loading spinner, an error banner, and an empty state. How?",
        ideal: `<p>Use <code>page.route</code> to control the boundary: fulfill a fake 200 with a fixed body for the happy path, <code>route.fulfill({status:500})</code> for errors, an empty array for empty state, and add an artificial delay (<code>await new Promise(r=&gt;setTimeout(r,2000)); route.continue()</code>) to exercise the spinner. Keep at least one happy-path test against the real backend so you don't mock away the integration.</p>`,
        stuck: `<p>They can't produce backend edge states on demand and either skip them or make them flaky; or they over-mock and stop testing the real integration.</p>`,
      },
      {
        id: 'b6',
        q: "Third-party analytics/tag-manager calls are slowing and destabilizing your runs. What do you do?",
        ideal: `<p>Abort noisy hosts with <code>page.route(/(analytics|googletagmanager|hotjar)\\./, r=&gt;r.abort())</code>. Legitimate speed/determinism tactic since those aren't under test. Be careful not to abort anything the flow depends on.</p>`,
        stuck: `<p>They don't know request interception can abort, or they block too broadly and break the app.</p>`,
      },
      {
        id: 'b7',
        q: "Payment fields live inside a Stripe iframe, and a custom web component uses shadow DOM. How do you automate both?",
        ideal: `<p>iframe → <code>page.frameLocator('iframe[name="stripe-card"]').getByLabel('Card number').fill(...)</code> (chain for nested frames). Shadow DOM → role/text/label locators pierce <strong>open</strong> shadow roots automatically, so usually no special syntax; closed shadow roots can't be reached by Playwright or Selenium. Java binding uses <code>page.frameLocator(...)</code> identically.</p>`,
        stuck: `<p>They think shadow DOM can't be tested or reach for <code>evaluate</code> hacks; they try to locate iframe contents with <code>page.locator</code> without <code>frameLocator</code>.</p>`,
      },
      {
        id: 'b8',
        q: "Implement the Page Object Model, then explain how fixtures relate to it.",
        ideal: `<p>POM = a class per page/component holding <code>readonly</code> Locators initialized from the injected <code>Page</code>, plus intent methods (<code>login()</code>, <code>addToCart()</code>). Fixtures provide <em>state</em> (logged-in user, seeded data, API client) and inject the page objects so specs stay short. They compose: fixture builds the POM + preconditions, POM exposes actions, test reads like a user story. Keep business assertions visible in tests (or in explicit <code>expectX()</code> methods), not buried everywhere in POMs.</p>`,
        stuck: `<p>They conflate POM with fixtures, put <code>test()</code> or heavy assertions inside page objects, or expose raw selector strings instead of Locators.</p>`,
      },
      {
        id: 'b9',
        q: "Your suite takes 45 minutes in CI. How do you cut wall-clock time?",
        ideal: `<p>First enable <code>fullyParallel: true</code> and tune <code>workers</code> to saturate one machine. Then <strong>shard</strong> across machines (<code>--shard=i/n</code>) via a CI matrix, emit <code>blob</code> reports per shard, and <code>merge-reports</code> into one HTML report. Also: seed via API + storageState to cut per-test time, mock unreliable third parties, and tier smoke vs regression. Sharding only helps after parallelism saturates a machine — and not if a shared staging app is the bottleneck.</p>`,
        stuck: `<p>They confuse workers (one machine) with shards (many machines), forget to merge blob reports, or shard before parallelizing.</p>`,
      },
      {
        id: 'b10',
        q: "Set up visual regression for a dashboard that includes a live clock and user avatars.",
        ideal: `<p><code>await expect(page).toHaveScreenshot('dashboard.png', {mask:[…dynamic regions…], animations:'disabled', maxDiffPixelRatio:…})</code>. Mask timestamps/avatars/ads, disable animations, hide caret. Generate baselines in the <strong>same Docker image CI uses</strong> because fonts/OS rendering differ, else you'll chase phantom diffs. Visual tests complement — don't replace — functional E2E.</p>`,
        stuck: `<p>They don't mask volatile regions (noisy diffs), generate baselines on a laptop but run CI in Linux Docker (font diffs), or confuse visual with functional testing.</p>`,
      },
      {
        id: 'b11',
        q: "Integrate the suite into CI (GitHub Actions / Jenkins / Azure DevOps). What are the must-haves?",
        ideal: `<p>Node setup → <code>npm ci</code> → <code>npx playwright install --with-deps</code> (or the official <code>mcr.microsoft.com/playwright</code> image with a version-matched tag) → run with sharding → upload trace/HTML reports as artifacts (<code>if: always()</code>). CI-only: <code>retries: 2</code>, <code>forbidOnly: true</code>, <code>trace: 'on-first-retry'</code>. Cache browser binaries or use the Docker image. Secrets in the CI secret store. Jenkins: a Jenkinsfile stage running the same commands; Azure DevOps: a YAML pipeline with NodeTool + scripts (browsers install headless without Docker on hosted agents).</p>`,
        stuck: `<p>They forget <code>--with-deps</code>/binary caching (startup balloons), don't upload artifacts, or hardcode secrets.</p>`,
      },
      {
        id: 'b12',
        q: "A test relies on data another test created and fails when run alone or out of order. How do you fix the class of problem?",
        ideal: `<p>Make each test self-seeding and independent: no shared mutable globals, unique data per test (faker/UUID) or per-worker pools, storageState instead of a UI-login chain, cleanup in fixture teardown. Verify with <code>--workers=4 --repeat-each=3</code> and randomized order. Use <code>test.describe.serial</code> only for genuinely sequential journeys.</p>`,
        stuck: `<p>They fix the one test instead of the ordering dependency; they don't know how to prove independence.</p>`,
      },
      {
        id: 'b13',
        q: "When would you choose to mock a network call versus hit the real backend?",
        ideal: `<p>Mock to isolate the UI (rendering/error/empty states) and to remove flaky third parties; hit the real service when the journey <em>is</em> the integration (e.g., "checkout talks to payments"). Hybrid is usually right: mock at the boundary, keep one happy-path test against the real backend. HAR replay for hermetic CI, refreshed when APIs change.</p>`,
        stuck: `<p>They over-mock (green tests, broken integration) or under-mock (can't reproduce edge states).</p>`,
      },
      {
        id: 'b14',
        q: "Drag-and-drop and a hover-only menu both fail intermittently. How do you handle each?",
        ideal: `<p>Try <code>source.dragTo(target)</code> first; for custom HTML5 DnD do it manually: <code>hover() → mouse.down() → hover(target) → mouse.up()</code>, sometimes with intermediate moves. Hover menus: <code>hover()</code> then assert the submenu is visible before clicking. Avoid <code>force:true</code> (it can silently "succeed" while nothing happens).</p>`,
        stuck: `<p>They rely on <code>dragTo</code> for finicky DnD, or use <code>force:true</code> and get false greens.</p>`,
      },
      {
        id: 'b15',
        q: "A chat \"Send\" button should show \"Delivered\" only after the server acknowledges receipt over the WebSocket \u2014 not just after the click. How do you assert that reliably?",
        ideal: `<p>Don't assert right after the click or use a fixed sleep. Listen for the specific WebSocket frame the server sends back, resolve a promise when an ack frame of the right type arrives, <code>await</code> it, then assert the UI shows "Delivered." This ties the assertion to the actual event the UI reacts to, not a guessed elapsed time.</p>`,
        stuck: `<p>They either poll the DOM in a loop (slow, still occasionally flaky) or use <code>waitForTimeout</code> — both are indirect proxies for "did the ack arrive" instead of checking it directly.</p>`,
      },
      {
        id: 'b16',
        q: "A pricing feature is live behind a flag for 20% of users. How do you test both the on and off states without needing five separate real accounts?",
        ideal: `<p>Toggle the flag programmatically for the test — via the flag provider's API/SDK for a dedicated test user, or by intercepting the flag-evaluation network call — then parameterize one test body over both states so both run predictably in every CI run, independent of the live rollout percentage. Reset the flag afterward so state doesn't leak into other tests.</p>`,
        stuck: `<p>They either hardcode against whatever the flag happens to be at run time (non-deterministic) or maintain two separate, drifting test files instead of one parameterized test.</p>`,
      },
      {
        id: 'b17',
        q: "Login goes through Google OAuth via a popup with its own consent screen. Do you automate the real Google login in CI?",
        ideal: `<p>Mock the identity-provider response for the bulk of tests — intercept the OAuth callback and return a pre-crafted token/assertion, exercising your app's auth handling without depending on Google's UI, rate limits, or bot detection. Reserve a small separate suite that exercises a real IdP login, since some regressions only show up in the actual redirect/popup/consent flow — don't make every test pay that cost.</p>`,
        stuck: `<p>They either try to automate the real provider everywhere (slow, rate-limited, breaks on UI changes) or mock so completely that a real integration bug would never surface anywhere.</p>`,
      },
      {
        id: 'b18',
        q: "QA needs to test \"a session that expired 2 minutes ago\" without waiting 2 real minutes. How?",
        ideal: `<p>Since the app trusts whatever token is present, craft a token with an already-past expiry (or a malformed claim) and inject it directly via storageState/cookies rather than logging in and waiting — the same token-injection technique used for role-based auth, applied to an edge case that would otherwise be slow or flaky to reproduce naturally.</p>`,
        stuck: `<p>They try to reproduce expiry by actually waiting, or assume expired/malformed-token states can only be tested manually.</p>`,
      },
      {
        id: 'b19',
        q: "Marketing wants the confirmation page screenshot-tested in English (LTR) and Arabic (RTL). What's different about the RTL screenshot test?",
        ideal: `<p>Set locale as with any localization test, but pay specific attention to layout mirroring — element order, icon direction, and alignment flip in RTL, so the Arabic baseline is a genuinely different image, not translated text in the same layout. Keep masks for dynamic content consistent across both baselines.</p>`,
        stuck: `<p>They screenshot-test the translated text but don't expect the whole layout to mirror, so real RTL bugs (icons still pointing the LTR way) never get caught because nobody set an RTL-aware baseline.</p>`,
      },
      {
        id: 'b20',
        q: "Product wants proof a \"saving\u2026\" spinner behaves correctly on a slow 3G connection, not just on your dev machine's fast one. How, without a slow network in the room?",
        ideal: `<p>Throttle the network at the DevTools Protocol (CDP session) level — set download/upload throughput and latency to slow-3G-like values for the test — so requests genuinely take longer and the spinner's real behavior gets exercised, then assert loading and success states in sequence.</p>`,
        stuck: `<p>They either treat this as "not really automatable" or fake it with <code>page.route</code> delays, without knowing CDP throttling exists and behaves closer to real degraded-network conditions.</p>`,
      },
      {
        id: 'b21',
        q: "The exact same hCaptcha challenge appears only when tests run against staging, never locally. What's different?",
        ideal: `<p>Same environment-drift triage muscle as any CI-only flake: bot-detection scores requests on signals like IP reputation, ASN, and traffic patterns that differ between a developer's home network and shared CI infrastructure, so staging can trigger a challenge local never sees. The fix lives at the environment level — a test-key bypass or allowlisted CI IP range coordinated with security — not in test code.</p>`,
        stuck: `<p>They assume it's a flaky test to fix with retries, when it's actually a signal that staging's bot-detection posture needs an explicit test exception.</p>`,
      },
      {
        id: 'b22',
        q: "A live-updating dashboard must reconnect and catch up if the WebSocket drops mid-session. How do you test that?",
        ideal: `<p>Combine offline emulation with the WebSocket-aware wait pattern: drop the connection mid-test with <code>context.setOffline(true)</code>, assert a "reconnecting" state appears, restore with <code>setOffline(false)</code>, then assert the dashboard catches up to the latest data — not merely that the socket reopened.</p>`,
        stuck: `<p>They verify the socket reconnects but stop there, missing whether the app actually resyncs the data it missed while disconnected — the more common real bug.</p>`,
      },
      {
        id: 'b23',
        q: "Signup requires a one-time code sent by SMS/email, and you don't control that provider in CI. How do you test the flow?",
        ideal: `<p>Intercept and mock the OTP verification endpoint the app calls, returning success/failure bodies for the cases you need (correct code, wrong code, expired code) rather than trying to receive a real SMS or email in CI. This exercises the app's handling of the verification response without a slow, non-deterministic delivery channel in the loop; reserve any real-provider test for a small, separate, non-blocking suite.</p>`,
        stuck: `<p>They either wire a real test phone number/inbox into CI (slow, flaky, costly) or skip testing the OTP step entirely, missing that the verification endpoint is just another API to mock.</p>`,
      },
      {
        id: 'b24',
        q: "A form has five independent validation rules. QA wants one test run to report all five failures on empty submit, not stop at the first. How?",
        ideal: `<p>Use a soft assertion for each field-level check instead of a normal one. Soft assertions don't halt the test on failure — they're collected, and the test is marked failed at the end with every failure reported, which is far more useful than a hard assertion that stops at field one and leaves fields two through five unverified.</p>`,
        stuck: `<p>They either write five separate tests (more setup duplication, slower) or don't know soft assertions exist and settle for only ever seeing the first failing field per run.</p>`,
      },
      {
        id: 'b25',
        q: "A route mock makes a \"create user\" test pass, but in production the same form sends malformed request bodies the real API rejects. How does a mock hide a bug like this?",
        ideal: `<p>A route handler that unconditionally returns success proves the frontend can handle a success response — it proves nothing about whether the frontend sent a valid request. Guard against this by asserting on the intercepted request's method, path, and body shape inside the handler before fulfilling, so the mock only returns success when the outgoing request actually looks right; an unexpected shape fails the test instead of silently passing.</p>`,
        stuck: `<p>They treat mocking as one-directional — controlling what comes back — and don't think to validate what went out, so a mock can pass while masking a client-side bug that would fail against the real backend.</p>`,
      },
      {
        id: 'b26',
        q: "Two people edit the same record; the second save should be rejected with a conflict, not silently overwrite the first. How do you test that, and what should the UI preserve?",
        ideal: `<p>Mock the save endpoint to return a version-conflict response after submission, and assert two things: the conflict message appears, and the user's own unsaved edits are still visible in the form rather than wiped out by the failed save. Losing someone's typed input on a conflict is usually the more damaging bug than the conflict itself.</p>`,
        stuck: `<p>They test that a conflict is detected but forget to assert the input-preservation half — which is usually the actual source of user complaints in production.</p>`,
      },
      {
        id: 'b27',
        q: "A teammate commits `playwright/.auth/admin.json` to the repo so CI can reuse it. What's wrong, and what should happen instead?",
        ideal: `<p>A storageState file contains live cookies and tokens — it's a credential, not a fixture. Committing it leaks real session access to anyone with repo read access, and it will silently go stale. Generate it fresh in a protected CI-only setup step, write it to a gitignored output directory, and handle it exactly like any other secret in the pipeline.</p>`,
        stuck: `<p>Because it looks like "just a JSON file with cookies in it" rather than a labeled password, candidates don't immediately categorize it as something needing the same handling as an API key.</p>`,
      },
      {
        id: 'b28',
        q: "A developer refactors a form and, not realizing tests depend on it, removes the `data-testid` attributes \"because they're not used by the app.\" What's the fix \u2014 for this instance, and going forward?",
        ideal: `<p>The immediate fix is mechanical — restore the attributes, or migrate those locators to role/label-based selectors if that's actually more robust long-term. The real fix is process: a test-only attribute is a contract between test code and product code, and needs to be visible to whoever could break it — a lint or code-owners rule flagging <code>data-testid</code> removal in PRs touching tested components, or, better, migrating to role/label locators wherever a real accessible name exists, so the "contract" is the same accessibility attributes the app needs anyway rather than a test-only add-on nobody outside QA has a reason to preserve.</p>`,
        stuck: `<p>They fix the immediate breakage and move on, without addressing that a test-only attribute with no other purpose in the app will keep getting stripped by anyone who doesn't know it's load-bearing.</p>`,
      },
      {
        id: 'b29',
        q: "Your product exposes a GraphQL API instead of REST. How does testing change?",
        ideal: `<p>The mechanics are similar to REST via the <code>request</code> fixture (POST a query/variables body, assert on the JSON shape), but interception differs: nearly all GraphQL traffic hits a single endpoint, so <code>page.route()</code> matching by URL alone can't distinguish operations — match the URL, then inspect the parsed request body's operation name to route only the specific operation you want to mock, letting everything else through. Errors look different too: a GraphQL response can return HTTP 200 with an <code>errors</code> array in the body instead of a non-2xx status, so status-code-based error assertions silently miss GraphQL-level failures — assert on the response body's <code>errors</code> field explicitly.</p>`,
        stuck: `<p>They apply REST assumptions wholesale — routing purely by URL, checking only HTTP status for success/failure — and don't realize GraphQL's single-endpoint, always-200 conventions break both.</p>`,
      },
      {
        id: 'b30',
        q: "The product is mid-redesign: some users see the legacy UI, others the new one, based on account creation date \u2014 not a flag you control in a test. How do you keep one suite covering both?",
        ideal: `<p>Treat "which UI version" as just another piece of context the test needs, the same way role or locale is: parameterize page objects or locators by UI version, and drive version selection from a test account whose creation date is deliberately set to fall on the side you want, rather than hoping a random real account lands on the right variant. Run the core journeys against both versions; retire the legacy-version tests on the same day the legacy UI is retired, not before.</p>`,
        stuck: `<p>They either maintain two entirely separate, drifting test suites (double the maintenance) or only test whichever version the CI test accounts happen to have gotten, silently losing coverage of the other.</p><p>---</p>`,
      },
    ],
  },

  tierC: {
    id: 'interview-tier-c',
    title: "Tier C \u2014 Senior (5\u20139 years)",
    lead: "Architecture, flake governance, hybrid design, and trade-off reasoning for 5\u20139 years. Senior signal = restraint and naming what you deliberately will not build.",
    questions: [
      {
        id: 'c1',
        q: "Design a Playwright test architecture for a team of ~30 engineers. What's in it, and what do you deliberately NOT build?",
        ideal: `<p>Cover POM or component-object hybrid, shared/custom fixtures, a stable locator policy, a parallel-safe test-data strategy, environment-specific config via <code>projects</code>/env files, CI sharding, and reporting. Feature-grouped <code>tests/</code>, page objects with locators+actions (no assertions), fixtures for preconditions. Crucially name restraint: <strong>avoid premature abstractions, do not write a custom framework on top of Playwright, do not hand-roll parallelism.</strong></p>`,
        stuck: `<p>They over-engineer, default to "POM for everything" without critiquing it, and can't name what <em>not</em> to build — the senior signal is restraint.</p>`,
      },
      {
        id: 'c2',
        q: "Your flaky-test list keeps growing. Turn \"we have flaky tests\" into \"we manage flaky tests.\"",
        ideal: `<p>Diagnose-first via traces, then categorize and fix by class (locator→role-based; timing→web-first assertions/remove sleeps; state→isolation; environment→infra). Retries as a <em>diagnostic</em>: <code>retries:2</code> + <code>trace:'on-first-retry'</code> + <code>--fail-on-flaky-tests</code> on PRs so retries surface rather than hide. Track flakiness across history (stability score), fix worst offenders first. Quarantine known-flaky tests behind a tag with a ticket + owner + review date; never let the list grow silently.</p>`,
        stuck: `<p>They lean on retries as a fix, have no historical tracking, and no quarantine governance (ownership/expiry).</p>`,
      },
      {
        id: 'c3',
        q: "Handle test data across a highly parallel, sharded suite.",
        ideal: `<p>Know the three patterns — per-test data via API setup; per-worker isolation via user pools; DB snapshots restored per shard. Playwright isolates cookies/localStorage/session via fresh BrowserContext per test, but <strong>DB records, server state, and filesystem artifacts are your responsibility.</strong> Create shared datasets once per worker where safe; use factories/builders and namespacing; clean up in teardown. Senior insight: "shared seed data" causes most parallel-run flake.</p>`,
        stuck: `<p>They assume context isolation covers everything, rely on shared seed data, and can't distinguish per-test vs per-worker vs snapshot trade-offs.</p>`,
      },
      {
        id: 'c4',
        q: "Multi-user real-time scenario: a customer places an order while an admin approves it in the same test. How do you structure it?",
        ideal: `<p>Two isolated contexts in one test (<code>browser.newContext({storageState:'customer.json'})</code> and <code>…'admin.json'</code>), a page in each. Drive both; assert the customer page reflects the admin's action in real time. Contexts can't see each other's cookies but can observe the app changing. Close contexts in teardown.</p>`,
        stuck: `<p>They try to do it with one context/page, or don't know contexts give true session isolation without new browsers.</p>`,
      },
      {
        id: 'c5',
        q: "A test passes on Chromium and Firefox but fails only on WebKit. How do you approach it?",
        ideal: `<p>First determine if it's a <strong>product bug</strong> (real Safari/WebKit divergence — layout, sub-pixel rounding, sticky positioning, focus/date handling, event timing, unsupported web API) or a <strong>test issue</strong> (assertions on exact bounding boxes, hit-testing near edges, headless-vs-headed differences). Use the WebKit trace. If it's a real Safari behavior, it's a valuable catch; if it's brittle assertions, make them less pixel-exact. Remember WebKit ≈ Safari's engine but not byte-identical to the shipping Safari app.</p>`,
        stuck: `<p>They assume "Playwright is broken," skip the product-vs-test triage, and write assertions too tightly coupled to Chromium rendering.</p>`,
      },
      {
        id: 'c6',
        q: "Make accessibility a first-class outcome of the framework rather than a separate effort.",
        ideal: `<p>Role-based locators (<code>getByRole</code>, <code>getByLabel</code>) force tests to depend on accessible names/roles, so passing tests tend to run against an accessible app; when a control is hard to locate by role, that's often a real a11y bug to raise. Layer explicit <code>@axe-core/playwright</code> audits gating serious/critical violations. Frame a11y as a byproduct of locator policy, not a bolt-on suite.</p>`,
        stuck: `<p>They treat a11y as a separate checklist and miss that locator strategy already drives it.</p>`,
      },
      {
        id: 'c7',
        q: "Design a \"self-healing\" locator strategy. What does real self-healing require \u2014 and what are its limits?",
        ideal: `<p>The lightweight built-in version: chain fallbacks with <code>locator.or(...)</code> so an alternative resolves if the primary breaks. True self-healing = tries genuinely different strategies (role/text/attribute/position), <strong>completes</strong> the run, and <strong>reports</strong> which locator failed and what worked so the root cause gets fixed. Advanced setups use an LLM to propose a locator from the DOM, gated by a confidence threshold, cached, and — critically — the heal is only accepted if downstream assertions still pass (a repair, not a cover-up). Treat healing as a backup; refactor fragile locators with stable <code>data-testid</code>/role selectors; audit every heal event as tech debt.</p>`,
        stuck: `<p>They think try/catch-and-continue or retrying the same XPath is self-healing; they don't gate on confidence/assertions, so healing hides bugs.</p>`,
      },
      {
        id: 'c8',
        q: "What should NOT be automated in Playwright E2E, and where should those checks live instead?",
        ideal: `<p>Keep E2E a thin layer over business-critical journeys (login, search, checkout, payment). Push field-level validation, edge cases, and error branches down to unit/component/API tests (faster, more stable). Also name Playwright's scope limits: no native mobile apps (use Appium/Maestro/Detox), not for pure unit tests, not IE11. Apply the test pyramid.</p>`,
        stuck: `<p>They chase 100% E2E coverage and claim Playwright fits everything — losing credibility.</p>`,
      },
      {
        id: 'c9',
        q: "Your CI report answers \"what failed in this run\" but not \"which test has been flaky all month.\" How do you get observability?",
        ideal: `<p>The built-in HTML report is per-run and local; for trends aggregate across runs/branches/PRs. Options: <code>blob</code> + <code>merge-reports</code>, <code>junit</code> for CI test tabs, <code>allure-playwright</code> dashboards, or a test-intelligence platform that indexes runs and links traces to PRs. Track flake rate, duration, and stability score over time; attach traces/screenshots to failures.</p>`,
        stuck: `<p>They stop at the HTML report and manually diff CI artifacts run-by-run.</p>`,
      },
      {
        id: 'c10',
        q: "How do you keep tests reliable when the app is a constantly re-rendering SPA (React/Vue/Svelte)?",
        ideal: `<p>Lean on Locators (lazy, re-queried each use, immune to stale references) and web-first assertions that retry; avoid <code>ElementHandle</code>. Locate by role/label/text so re-renders and class changes don't break tests. Wait on observable outcomes/network responses, never fixed sleeps. Understand actionability checks (attached/visible/stable/enabled/receives-events) to reason about "why did this time out."</p>`,
        stuck: `<p>They cache ElementHandles or read state with non-retrying getters and get stale-element/timing flakes.</p>`,
      },
      {
        id: 'c11',
        q: "Your automated axe-core scan reports zero violations on a modal, but a screen-reader user says it's unusable. What's the scan missing, and how do you test for it?",
        ideal: `<p>Automated scanners like axe-core check static/structural rules (missing labels, contrast, ARIA attributes) but generally can't verify dynamic keyboard behavior: that focus actually moves into the modal on open, that Tab doesn't escape its boundary, that Escape closes it, and that focus returns to the triggering control on close. These map to real WCAG criteria but need scripted keyboard-interaction tests alongside the axe scan, not instead of it.</p>`,
        stuck: `<p>Senior candidates often treat "the axe scan is green" as "accessible," missing that a large share of WCAG success criteria need behavioral or human judgment that static analysis structurally can't verify.</p>`,
      },
      {
        id: 'c12',
        q: "Leadership wants a CI gate that fails a PR if Core Web Vitals regress \u2014 but your suite runs Chromium, Firefox, and WebKit. Design it.",
        ideal: `<p>Lighthouse (and CDP-based tracing generally) only works against Chromium, so a dedicated Chromium-only project carries the performance gate while cross-browser E2E projects run unrelated to it. For metrics captured directly in-browser (LCP, CLS, INP), a <code>PerformanceObserver</code>-based approach gives more control over test conditions than a Lighthouse audit. Gate on regressions against a budget, and remember Lighthouse measures initial load well but is blind to post-load interactions (search, filtering) that a Playwright journey can trigger it against.</p>`,
        stuck: `<p>They try to force Lighthouse onto Firefox/WebKit (no CDP support there), or don't distinguish a Lighthouse synthetic audit from Core Web Vitals measured live via the Performance API — related but not identical.</p>`,
      },
      {
        id: 'c13',
        q: "Design the test strategy for a live trading dashboard where price updates must appear within strict latency bounds across concurrent users.",
        ideal: `<p>Separate the concerns. Playwright is well suited to verify <em>correctness</em> — the right price update reaches the right element after a matching WebSocket frame arrives, exercised across two-plus contexts simulating concurrent users, using frame-event-driven waits rather than fixed sleeps. It is the wrong tool for proving strict latency/throughput SLAs under real load — that's dedicated WebSocket or load-testing tooling.</p>`,
        stuck: `<p>They try to make Playwright the source of truth for latency numbers, conflating "my assertion resolved quickly" with "the system meets its SLA" — the former is an artifact of test design, not a load measurement.</p>`,
      },
      {
        id: 'c14',
        q: "A checkout page has three independent feature flags live at once. A naive matrix is eight combinations, and QA can't realistically own 8\u00d7 the checkout suite. What do you actually test?",
        ideal: `<p>Don't test the full combinatorial matrix by default — test each flag's effect in isolation against a fixed baseline of the others (catches most real regressions at a fraction of the cost), and reserve a small number of deliberately-chosen "known risky" combinations, flagged by product/eng as likely to interact, for explicit combination tests.</p>`,
        stuck: `<p>They either brute-force every combination (suite explodes) or ignore combinations entirely (misses real flag-interaction bugs) — the senior signal is a deliberate, risk-based middle path.</p>`,
      },
      {
        id: 'c15',
        q: "Auth tests pass individually but fail intermittently only when the full suite runs in parallel against shared staging. Investigation steps?",
        ideal: `<p>Playwright's context isolation covers client-side state (cookies, localStorage) but not server-side session state — if parallel tests authenticate as the same shared account, one test's token rotation or logout can invalidate another's session mid-run. Fix by giving each worker its own dedicated account/tenant, and keep auth-flow tests (login, logout, expiry) in a separate suite from feature tests that merely assume auth already works.</p>`,
        stuck: `<p>They debug the individual failing feature tests instead of recognizing shared-account server-side coupling as the root cause — the same "shared state" flakiness class as parallel test-data isolation, on the auth axis instead of the data axis.</p>`,
      },
      {
        id: 'c16',
        q: "A product ships in 15 languages. Do you E2E-test all 15 fully?",
        ideal: `<p>No — full E2E coverage per locale mostly re-tests the translation vendor's copy, not your code. Run the full functional suite against one or two representative locales (including one RTL and one with longer strings, like German), and run a much thinner smoke test — page loads, key strings render, date/currency formatting is correct, no layout overflow — across the rest. This targets what actually breaks per locale rather than re-verifying business logic 15 times.</p>`,
        stuck: `<p>They either propose testing everything in every locale (unsustainable) or drop localization testing to unstructured spot checks, missing the tiered middle ground.</p>`,
      },
      {
        id: 'c17',
        q: "Playwright ships a new major version with breaking changes. You own a 500-test suite. How do you plan the upgrade?",
        ideal: `<p>Trial the upcoming version via Playwright's canary release channel against a subset of the suite (or a shadow CI job) before it's generally available, so breaking changes surface early. Read the migration notes, get the full suite green in a branch before merging, and treat "our suite can't upgrade easily" as itself a signal of overly-coupled, version-fragile test code worth fixing regardless.</p>`,
        stuck: `<p>They plan to "just upgrade and fix what breaks" with no staging step, turning routine maintenance into a multi-day firefight that blocks the whole team's CI.</p>`,
      },
      {
        id: 'c18',
        q: "Fifteen product teams each maintain their own copy of the same design-system `<Button>`/`<Modal>` test logic, and it's drifted out of sync. Fix it with component testing.",
        ideal: `<p>Centralize component tests for the shared design-system library itself, using component-testing mode to mount and assert on each shared component in isolation, and publish it as the source of truth for "how do you test our Button." Product teams import/extend those fixtures instead of re-authoring locator/assertion logic per team, turning a design-system regression into one failing suite instead of fifteen silently-drifting ones.</p>`,
        stuck: `<p>They propose "better documentation" or a shared style guide, missing that component testing lets the design-system team ship an executable, always-current contract instead of a document that drifts.</p>`,
      },
      {
        id: 'c19',
        q: "Walk through `route.fulfill()`, `route.continue()`, `route.fetch()` + `fulfill()`, and `route.abort()` \u2014 and when multiple handlers match one request, what decides which runs?",
        ideal: `<p><code>fulfill()</code> returns a fully controlled fake response without hitting the network. <code>continue()</code> lets the real request through, optionally with overridden headers/method/body. <code>fetch()</code> followed by <code>fulfill()</code> makes the real request and lets you patch the real response before returning it — useful for injecting one bad field into an otherwise-real payload. <code>abort()</code> simulates a transport-level failure (offline, DNS failure, connection reset), not an HTTP error status. When multiple handlers match, they run in reverse registration order, and a handler can call <code>fallback()</code> to pass the request to the next matching handler instead of finishing it itself — the basis for layered, composable routing (a global logging handler plus a test-specific override, for instance).</p>`,
        stuck: `<p>Most candidates know <code>fulfill()</code> and stop there; the <code>fetch()+fulfill()</code> patch pattern and <code>fallback()</code>-based chaining are rarely needed day to day, so candidates who've actually built layered routing stand out from those who've only ever used the simplest mock.</p>`,
      },
      {
        id: 'c20',
        q: "Your locator matches two \"Save\" buttons with the identical accessible name. Beyond scoping your locator to fix the test, what should this make you ask about the product?",
        ideal: `<p>Fix the immediate test problem by scoping to the correct region or filtering — but flag it further: two controls sharing one accessible name is frequently a real accessibility defect, not just a locator inconvenience, since a screen-reader user hears "Save, Save" with no way to tell them apart either. Treat a strict-mode collision as a possible signal worth raising with product or design, not only an obstacle to script around.</p>`,
        stuck: `<p>Mid-level candidates fix the ambiguity and move on; the senior signal is noticing that the same ambiguity confusing your locator is also confusing an actual screen-reader user.</p>`,
      },
      {
        id: 'c21',
        q: "A user is halfway through a long form when their session expires in the background. What should the product do, and how do you test it \u2014 distinct from testing a session that's already expired before the test starts?",
        ideal: `<p>This differs from starting a test with an already-expired token, which only tests "reject an old token." Here, expire the session while the user is actively interacting — via a test API, or by making the next request return the real 401 the backend would send — then assert on what happens to their in-progress input: ideally it's preserved (saved to a draft, or resubmitted after re-authentication) rather than silently lost, and the user is routed to re-authenticate rather than shown a confusing generic error.</p>`,
        stuck: `<p>They only test the "arrive already logged out" case, missing the more damaging and more common-in-production mid-session expiry, where the interesting question is whether unsaved work survives.</p>`,
      },
      {
        id: 'c22',
        q: "Design the test coverage for a checkout flow that hands off to a third-party payment provider.",
        ideal: `<p>Define the boundary explicitly with the team first. The bulk of functional tests mock the application's own server-facing payment result (success, decline, specific error codes) to exercise every branch deterministically without touching real money; a small number of tests run against the provider's official sandbox/test-card environment to validate the actual redirect/iframe/webhook integration; and no test — mocked or sandboxed — should ever be able to create a real charge. Provider test credentials live in the CI secret store like any other credential, never in test code.</p>`,
        stuck: `<p>They either drive the real provider's UI for every test (slow, and sandbox outages block unrelated PRs) or mock so completely that a real break in the provider's actual redirect flow would go unnoticed by anything in the suite.</p>`,
      },
      {
        id: 'c23',
        q: "A page registers a Service Worker that intercepts and answers requests itself \u2014 and your `page.route()` mocks stop taking effect for those requests. What's happening?",
        ideal: `<p>A Service Worker can intercept fetches at the browser network layer before Playwright's routing gets a chance to act on them, since the Service Worker effectively becomes its own network layer for requests it claims. Options: unregister or bypass the Service Worker for the test context (Playwright can disable Service Workers via a context option), or, if the Service Worker's caching/offline behavior is itself under test, mock at a layer the Service Worker can't shadow — the actual backend response it would fetch, via <code>route.fetch()</code>, or a dedicated test-only backend endpoint.</p>`,
        stuck: `<p>They assume <code>page.route()</code> is always the outermost layer intercepting network traffic and don't know a Service Worker can sit in front of it for the requests it owns.</p>`,
      },
      {
        id: 'c24',
        q: "Your payment provider's sandbox is unreliable, and their terms of service prohibit automated interaction with it entirely \u2014 you can't drive it and aren't permitted to script against it. How do you get any coverage of the integration at all?",
        ideal: `<p>When you can't touch the third party at all — not even a sandbox — the integration boundary has to move entirely to your own backend: build and maintain a stub of the provider's webhook/callback contract that your own team owns and controls (validated periodically, manually or via a low-frequency scheduled check, against the provider's actual current contract so it doesn't quietly drift), and test against that stub for everything automated. This goes a step further than "mock the app's own server-facing result" — here, zero automated coverage ever touches the provider, by requirement, so contract-drift risk is managed by manual periodic verification instead.</p>`,
        stuck: `<p>They either assume there's always some sandbox to fall back to, or give up on automated coverage of the integration entirely, missing that a team-owned contract stub still catches most regressions even with zero real-provider contact.</p>`,
      },
      {
        id: 'c25',
        q: "Your Playwright trace shows a completely clean run \u2014 every action succeeded, every assertion passed \u2014 but the CI job is still reported as failed. How is that possible, and where do you look?",
        ideal: `<p>A trace only captures what happened inside the browser; it can't see the CI runner itself. A clean trace with a failed job points outside the test: disk full while writing artifacts, the process running out of memory and getting killed after the test itself finished, a container crash during artifact upload, or the test runner's own timeout firing on a step after the last recorded action. Check the raw CI job logs and resource metrics (memory/disk) for the run, not just the Playwright report, whenever the trace and the job status disagree.</p>`,
        stuck: `<p>They keep re-examining the trace looking for something they missed, because "the trace shows success" feels like it should be authoritative — not realizing the trace's scope stops at the browser boundary.</p>`,
      },
      {
        id: 'c26',
        q: "Your application publishes an event to a message queue (Kafka, SQS, etc.) after a user action, and a consumer processes it asynchronously before the UI reflects the result. How do you test the whole path, not just the UI trigger?",
        ideal: `<p>Don't assert on the UI alone and call it covered — the UI update is the last of several hops (publish → consume → process → persist → UI refresh), and a UI-only test can pass even if the consumer silently fails, because the UI might poll or the test might get lucky on timing. Where you own the infrastructure, verify intermediate state directly: that the event was actually published (via a test consumer or the queue's own inspection API) and that the downstream side effect landed (a database row, a follow-up API becoming available) — then assert the UI reflects it, waiting on the real end condition rather than a fixed delay. Where you don't own the queue in the test environment, at minimum assert on the API/database state the consumer is supposed to produce, not only the UI.</p>`,
        stuck: `<p>They test the trigger (the user action) and the eventual UI state, and treat that as full coverage of an asynchronous pipeline where any of several intermediate hops could be the thing that's actually broken.</p>`,
      },
      {
        id: 'c27',
        q: "\"Place order\" fails somewhere behind the UI \u2014 could be the API gateway, payment, inventory, notifications, or shipping. Where do you start, and what should you never do first?",
        ideal: `<p>Never start by re-running the UI test and staring at the browser — in a microservices system the UI is the last hop, not the first place to look. Start from what the request actually did behind the scenes: pull the correlation/trace ID for that request, follow it through the API gateway and each service's logs, check response times and error rates per service, and confirm which service actually returned the failure before touching the frontend at all. Only once the failing service is identified does it make sense to reproduce and debug at the UI layer, if the UI layer is even where the fix belongs.</p>`,
        stuck: `<p>They debug from the browser inward (checking selectors, checking the click) on a failure that has nothing to do with the UI, burning time before ever looking at the actual failing service.</p><p>---</p>`,
      },
    ],
  },

  tierD: {
    id: 'interview-tier-d',
    title: "Tier D \u2014 Lead / Solutions Architect (9+ years)",
    lead: "Platform economics, migration, governance, and org-scale decisions for 9+ years. Prefer scenarios with a forcing function \u2014 a number, deadline, or cross-team constraint.",
    questions: [
      {
        id: 'd1',
        q: "You join a company with zero automation. Design a Playwright framework (TS) to scale to thousands of tests across a growing team. Folder structure, fixtures, POM vs component-object, config/env layering \u2014 and what you deliberately won't build.",
        ideal: `<p>Clear conventions (<code>tests/</code> by feature, <code>pages/</code> or <code>components/</code>, <code>fixtures/</code>, <code>utils/</code>, <code>config/</code>) so it's obvious where new tests go. <strong>Fixtures over manual instantiation</strong> (<code>base.extend</code>) to inject POMs + shared auth. Critique POM: for large SPAs a <strong>component-object/hybrid</strong> (objects per reusable component, not per whole page) scales better. Config/env layering: <code>projects[]</code> for browsers/environments, env vars per stage, centralized retries/timeouts/trace policy, storageState auth. Restraint: no premature abstractions, no custom framework atop Playwright, no hand-rolled parallelism.</p>`,
        stuck: `<p>They over-abstract on day one, default to "POM for everything," scatter config per-test, and can't name what not to build.</p>`,
      },
      {
        id: 'd2',
        q: "Cross-browser at scale: build-vs-buy. Self-hosted grid vs managed cloud (BrowserStack / LambdaTest / Microsoft Playwright Testing). Decide and justify to leadership.",
        ideal: `<p>Frame as <strong>total cost of ownership</strong>, not license price — "build" carries engineer-time + infra + perpetual maintenance. (Autonoma AI's June 2026 cost model estimates an in-house framework for a 5-engineer team at roughly $340,000 over three years vs. a managed platform at $60,000–$90,000 at a $160k fully-loaded engineer cost; its companion in-house-grid model puts annual grid TCO at $70,000 to $147,000 — treat these as vendor estimates, but the <em>structure</em> of the argument is the point.) Playwright-specific insight: context isolation + on-machine parallelism make a classic self-hosted Selenium Grid largely unnecessary. <strong>Buy/rent</strong> when you need real-device/wide OS-browser matrices, compliance, or speed-to-coverage. <strong>Build/self-host</strong> when data-control/cost dominate and the matrix is mostly Chromium+Firefox with someone owning container infra. Name trade-offs; "always build/always buy" is a red flag.</p>`,
        stuck: `<p>They anchor on sticker price, miss that Playwright undercuts the grid rationale, and answer dogmatically.</p>`,
      },
      {
        id: 'd3',
        q: "The org regression run is a 45-minute nightly blocking releases across teams. Scale execution \u2014 and say when native sharding stops being enough.",
        ideal: `<p>Distinguish <strong>workers</strong> (vertical, one machine) from <strong>shards</strong> (horizontal, many machines); tune workers first, add sharding when a machine is maxed. Native sharding limits: static pre-assignment, blocking (slowest shard gates the job), and splitting by file-path lexical order → unbalanced load. Next tier: an <strong>orchestrator</strong> that load-balances by historical timing. Add execution-time budgets, smoke/regression tiering, cross-machine report aggregation, and <code>trace:'on-first-retry'</code>. Hard prerequisite: test isolation — "a discipline problem, not a config problem."</p>`,
        stuck: `<p>They conflate workers with shards, don't know sharding is static/lexically unbalanced, and "add machines" without addressing isolation/balancing.</p>`,
      },
      {
        id: 'd4',
        q: "50+ engineers write Playwright. Enforce a locator policy, review standards, and lint rules so the suite doesn't rot.",
        ideal: `<p>Locator policy: role/user-facing first, <code>getByTestId</code> sparingly, CSS fallback, <strong>avoid XPath</strong>. Enforce via ESLint (<code>@typescript-eslint/no-floating-promises</code>/<code>await-thenable</code> to catch missing awaits; <code>eslint-plugin-playwright</code> rules on test globs) run in CI, plus PR quality gates and review standards. Ownership: shared fixtures with named owners; feature teams review their Playwright PRs (break the QA silo). It's "a discipline question, not a tooling question — pick a stable strategy on day one."</p>`,
        stuck: `<p>They treat locators as personal preference, don't know lint catches missing-await races, and have no enforcement mechanism (gate/ownership) — only aspirations.</p>`,
      },
      {
        id: 'd5',
        q: "A few flaky tests break the build for every team. Design a quarantine policy that doesn't just paper over the problem.",
        ideal: `<p>Diagnose-before-quarantine via traces; categorize (locator/wait/data/environment/product). Set a named threshold (e.g., "flaky if it fails N of M runs"). Mechanics: tag <code>@flaky</code>, exclude from the release gate but keep running it in a nightly quarantine job with retries. <strong>Every quarantined test needs a ticket ID, an owner, and a review/expiry date, and the quarantine count must be visible in CI.</strong> Retries: 0 local, 1–2 CI; >2 usually hides a deeper problem. If the list only grows, you're "archiving pain," not managing quality.</p>`,
        stuck: `<p>They mask with retries/timeouts, set no threshold, and have no ownership/expiry so the list grows unbounded.</p>`,
      },
      {
        id: 'd6',
        q: "Design the org-wide CI/CD test strategy: smoke vs regression tiers, nightly runs, quality gates, reporting/observability \u2014 across GitHub Actions / Azure DevOps / Jenkins.",
        ideal: `<p>Tier by feedback speed: fast smoke on every PR (blocks merge); full sharded regression nightly. Mechanics: official Docker image, matrix sharding, retries, trace+HTML artifacts. Deterministic <strong>quality gate</strong>: typecheck + lint + Playwright + traces gate the merge; a green build with 12 flaky tests must not look like a clean green build. Observability: aggregate across shards/machines. Watch the binary-caching gotcha (large downloads balloon startup).</p>`,
        stuck: `<p>They run the full suite on every PR, skip binary caching, treat reporting as an afterthought, and can't articulate a deterministic gate.</p>`,
      },
      {
        id: 'd7',
        q: "You own a 3,000-test Java Selenium suite on Selenium Grid. Leadership wants Playwright. Big-bang or incremental? Give the strategy and the traps.",
        ideal: `<p>Reject big-bang (silent coverage gap indefensible at a prod incident), naive parallel-maintenance (burnout, no ROI), and lift-and-shift. Use <strong>incremental strangler-fig</strong>: all <em>new</em> tests in Playwright from day one; run both in CI during transition; migrate highest-value/flakiest Selenium tests opportunistically. Analyze→Optimize→Migrate: don't port garbage — delete dead tests, push UI checks that belong at API layer down. Governance during coexistence: "No New Selenium" rule. Grid retirement is an explicit phase once <code>projects</code> cover the browser matrix. Prove ROI with KPIs. It's "an operating model change, not a rewrite."</p>`,
        stuck: `<p>They pitch a big-bang rewrite or lift-and-shift, underestimate the ownership/CI/data operating-model change, and treat it as a framework swap.</p>`,
      },
      {
        id: 'd8',
        q: "Handle the WebDriver\u2192Playwright API mapping mechanically. What must be rewritten rather than wrapped?",
        ideal: `<p>Core mapping: <code>driver.get()</code>→<code>page.goto()</code>; <code>findElement(By.id)</code>→<code>page.locator("#id")</code> or better <code>getByRole/getByLabel</code>; <code>sendKeys</code>→<code>fill()</code>; <code>WebDriverWait</code>+<code>ExpectedConditions</code>→deleted (auto-waiting); <code>Assert.assertEquals</code>→<code>expect(locator).toHaveText()</code>; <code>@BeforeMethod/@AfterMethod</code>→removed; <code>Thread.sleep()</code>→removed. Infra: Grid/Hub→built-in <code>--shard</code>; TestNG XML→<code>projects[]</code>; <code>ThreadLocal&lt;WebDriver&gt;</code>→browser contexts. <strong>Rewrite (not wrap):</strong> the locator+assertion+wait layer is the heart of a Selenium test and doesn't port — "you're adopting a different execution model." A first refactor pass removes every <code>waitForTimeout</code> left over from Selenium habits.</p>`,
        stuck: `<p>They treat it as syntax translation and wrap everything, don't grasp the wait/locator/assertion model change, and carry Selenium's sleep/explicit-wait habits into Playwright.</p>`,
      },
      {
        id: 'd9',
        q: "How do you handle authentication architecture across a very large suite (roles, MFA, expiry, rate limits)?",
        ideal: `<p>Capture <code>storageState</code> once per role (setup project or worker-scoped fixture) and reuse; never log in per test. Prefer API/token login where possible. Handle MFA (test-only bypass/seeded TOTP in non-prod), session expiry (refresh strategy), and per-test variations of the logged-in state. Keep <code>.auth/</code> gitignored; credentials in a secret manager.</p>`,
        stuck: `<p>They log in in every test/<code>beforeEach</code>, hit rate limits, and have no MFA/expiry story.</p>`,
      },
      {
        id: 'd10',
        q: "Governance of AI-assisted test authoring (codegen, MCP, LLM self-healing) at org scale \u2014 how do you adopt it without losing control or leaking data?",
        ideal: `<p>Freeze the target architecture first via a rules file so AI migrates <em>into</em> your architecture. Separate autonomous low-risk read-only tasks from approval-required state-mutating/generative tasks with a human checkpoint. Data privacy: enterprise LLM endpoints with zero retention, DOM redaction of PII/tokens before sending to an MCP server, agents sandboxed to isolated contexts with synthetic data — never production. Treat AI output like any code: review, locator discipline, same flaky-test management.</p>`,
        stuck: `<p>They let AI invent inconsistent structure per file, don't separate risk levels, and ignore PII/token leakage and sandboxing.</p>`,
      },
      {
        id: 'd11',
        q: "Six months in, someone notices your shared CI artifact bucket \u2014 browsable by most of engineering \u2014 holds Playwright traces and HTML reports containing plaintext passwords typed via `fill()` and auth tokens visible in the network panel. What's the fix, and what are today's real limits?",
        ideal: `<p>Be direct that this is a known, currently-open gap rather than a config flag you're missing — there is no built-in way yet to redact arbitrary sensitive values from traces, reports, or console logs. Mitigate at the layers that do exist: element-level screenshot masking for visual captures, environment variables plus your CI's secret store instead of literal credentials in test code, and — the real fix today — restricting who can access the artifact bucket, plus treating test-environment credentials as disposable and rotatable. Name artifact access control, not the browser layer, as the actual mitigation until native redaction ships.</p>`,
        stuck: `<p>They assume a built-in redaction option must already exist because the need is so obvious, and confidently describe a feature that isn't there yet instead of naming the access-control mitigation that is.</p>`,
      },
      {
        id: 'd12',
        q: "It's 2026 and your org wants AI agents for test authoring/maintenance. MCP server, Playwright-as-CLI-skill, or Playwright's built-in Test Agents \u2014 and why?",
        ideal: `<p>These solve different problems, not competing options for the same job. MCP gives an agent a live, structured connection to a real browser grounded in the accessibility tree — valuable when the agent must iteratively explore and reason about page state — but reported benchmarks put it at roughly four times the token cost of a CLI-skill approach for comparable coverage, and long MCP sessions carry a context-staleness risk: a stale accessibility tree can make the agent confidently act on the wrong element, producing consistent-not-flaky failures easy to misdiagnose as ordinary flakiness. CLI-skill invocation is cheaper when the agent just needs to execute a known test. Built-in Test Agents are a role-specialized plan/generate/heal pipeline for authoring and maintaining suites at scale, layered on a mature framework rather than replacing one.</p>`,
        stuck: `<p>Candidates who've only absorbed the marketing layer treat "AI plus Playwright" as one undifferentiated thing and can't articulate the cost/reliability trade-off between a stateful MCP session and a stateless CLI call.</p>`,
      },
      {
        id: 'd13',
        q: "A SaaS product must show different pricing, currency, and legally-required disclaimers per country, and EU user data must never leave the EU. Design the test coverage.",
        ideal: `<p>Separate three concerns that get conflated. Presentation: geolocation/locale emulation verifies the right pricing/currency/disclaimer renders per region — a client-side, easily-parallelized E2E concern. Data residency: verifying EU data is actually served from and stored in EU infrastructure is a backend/infrastructure assertion Playwright can help trigger but shouldn't be the system of record for proving. Compliance sign-off: legally-mandated disclaimer text needs legal review of the copy itself, not just automated presence-checking. Keep these three explicitly separate and owned.</p>`,
        stuck: `<p>They treat "test internationally" as solved by geolocation emulation alone, missing that data residency is an infrastructure claim the browser can't verify.</p>`,
      },
      {
        id: 'd14',
        q: "Product plans a 4-week canary rollout (1% \u2192 100%) for a risky rewrite and asks whether the Playwright regression suite can be thinner for this release as a result. How do you respond?",
        ideal: `<p>Push back on the framing: canaries and automated regression tests catch different failure classes and aren't substitutes. A canary catches what shows up in aggregate metrics once real traffic hits it, and doesn't catch a broken user journey unless that journey is instrumented and someone is watching; "traffic looked normal at 10%" isn't a statistically rigorous check. Keep the regression suite at full strength as the pre-release gate, and treat the canary as an additional post-release net for unknown-unknowns.</p>`,
        stuck: `<p>Leadership conflates "we have a gradual rollout" with "we've de-risked this," missing that the two catch different things.</p>`,
      },
      {
        id: 'd15',
        q: "Design the testing/observability boundary for a real-time collaborative editor \u2014 what does Playwright own, and what doesn't it?",
        ideal: `<p>Playwright owns correctness of merge/sync behavior as observed in the UI: two-plus contexts representing concurrent editors, driving conflicting edits, asserting the eventual UI state converges correctly, using waits tied to the actual sync messages. It does not own proving the underlying conflict-resolution algorithm holds at scale, or that the system survives hundreds of concurrent real editors. Stating this boundary explicitly stops the suite from being asked to prove things it structurally can't.</p>`,
        stuck: `<p>Without a stated boundary, either the suite balloons trying to simulate load it was never built for, or a real incident happens under load nobody had assigned as anyone's job.</p>`,
      },
      {
        id: 'd16',
        q: "An audit finds 340 feature flags, many unowned and years old, with CI time creeping up as flag-conditional paths multiplied. What's the governance fix?",
        ideal: `<p>This is a flag lifecycle problem, not a testing problem to solve one test at a time: require an owner and an explicit removal date on every new flag at creation, run periodic audits flagging any toggle stuck at 0% or 100% for months as a removal candidate, and default new coverage to the flag's current default state only. Tie cleanup to the same PR process as any other tech debt, with a visible count leadership can see.</p>`,
        stuck: `<p>Candidates answer with a testing technique when the real failure is organizational — nobody owns deleting flags once they've served their purpose.</p>`,
      },
      {
        id: 'd17',
        q: "Your platform integrates with 20+ enterprise customers' own SSO/SAML identity providers, each its own sandbox with its own rate limits. Release velocity keeps getting blocked by a flaky third-party IdP sandbox. Redesign the architecture.",
        ideal: `<p>Decouple release velocity from any single IdP's availability: the release-gating suite authenticates via a mocked/stubbed SAML assertion or OAuth response your app trusts identically to a real one, while real-IdP integration tests run separately, on a schedule rather than per-PR, against a curated subset of representative sandboxes.</p>`,
        stuck: `<p>They try to keep every IdP integration in the release-blocking path "for confidence," not recognizing that confidence in your own code and confidence in a third party's sandbox uptime are different guarantees.</p>`,
      },
      {
        id: 'd18',
        q: "Legal mandates an accessibility compliance program, and your axe-core gate already runs on every PR. Is that gate enough to tell legal \"we're compliant\"?",
        ideal: `<p>No, and say so directly: automated tools are estimated to catch only a fraction of WCAG success criteria — meaningful reading order, whether alt text is actually descriptive, real screen-reader usability, and cognitive-load criteria all need human and assistive-technology testing no scanner performs. A defensible program pairs the automated PR gate with a periodic manual/AT audit, and reports the automated gate to legal as a floor, not the ceiling.</p>`,
        stuck: `<p>Under pressure to show a green dashboard, teams over-represent "the axe-core gate is green" as "we are WCAG compliant."</p>`,
      },
      {
        id: 'd19',
        q: "A fixture creates a test record via API and deletes it in teardown. What happens when teardown itself fails, and how do you stop orphaned data from accumulating for months?",
        ideal: `<p>Don't let the happy-path teardown be the only cleanup mechanism — a crashed test, a killed CI job, or a teardown call that itself 404s all skip client-side cleanup. Two layers: make the fixture's teardown tolerant of "already deleted" as a success case, and back it with an environment-level sweep — a scheduled job that deletes anything matching the test-data naming convention past an age threshold — as the real backstop. Tag all test-created data with a consistent, greppable prefix specifically so that backstop can find it.</p>`,
        stuck: `<p>They design the happy-path fixture teardown and stop there, not accounting for the CI-crash and already-deleted edge cases that guarantee some test data never hits that code path.</p>`,
      },
      {
        id: 'd20',
        q: "Your team is leaving Selenium+TestNG+Cucumber for Playwright. Some stakeholders \u2014 including non-technical ones who review Gherkin scenarios \u2014 want to keep the BDD layer; others want plain TypeScript tests. How do you decide, and what does the architecture look like either way?",
        ideal: `<p>Treat this as a stakeholder-value question before a technical one. Gherkin's value is a shared, readable specification non-engineers can review — if that readership genuinely exists and uses it, keep Cucumber-over-Playwright (step definitions calling Playwright page objects, sharing browser/context through Cucumber's World object). If only engineers ever actually open the feature files, the BDD layer is pure translation overhead, and plain TypeScript tests with well-named describe/test blocks give most of the readability without it. Either way, name the ongoing cost: Cucumber adds a step-definition-matching layer that becomes its own maintenance burden.</p>`,
        stuck: `<p>They treat "keep BDD or not" as a technical preference rather than checking whether the specific value BDD provides — non-engineer readership — still applies to this team.</p>`,
      },
      {
        id: 'd21',
        q: "Engineers routinely see red Playwright runs, shrug, and merge anyway \u2014 automation has stopped being trusted. You're asked to fix that. Where do you actually start?",
        ideal: `<p>Don't start by writing more tests — start by making the existing signal trustworthy again, since trust was lost to false failures, not insufficient coverage. Audit and eliminate concrete sources of false-positive red builds (static waits, shared mutable test data, brittle structural locators, retry-masked root causes), then make the <em>evidence</em> for real failures cheap to access (trace, video, screenshots attached automatically, not something someone reproduces locally to understand). Trust is rebuilt incrementally by a visible run of genuinely-actionable red builds, not an announcement that things are fixed now.</p>`,
        stuck: `<p>They respond to "developers don't trust automation" by proposing more automation, missing that the fix is reliability and evidence quality, not volume.</p>`,
      },
      {
        id: 'd22',
        q: "One codebase serves 100 different customers, each with their own logo, language, permission set, and enabled features \u2014 but it's genuinely one application, not 100 forks. Design the test coverage.",
        ideal: `<p>Never write or generate a test per tenant — an N-times maintenance multiplier for zero added confidence, since the application code is shared. Make tenant identity a configuration input the same way environment or locale already are: a small config per tenant (or a handful of representative tenant archetypes covering the meaningfully different combinations, not all 100 literally) that the suite reads to know what to expect, with test logic identical across tenants. Run the full suite against a couple of real configurations and a lighter smoke pass across the rest, weighted toward the riskiest combinations.</p>`,
        stuck: `<p>They either propose duplicating suites per customer (unmaintainable) or ignore per-tenant differences entirely, instead of recognizing tenant config as just another input to parameterize over.</p>`,
      },
      {
        id: 'd23',
        q: "Security mandates that no authentication cookies or tokens may be persisted to disk anywhere in the pipeline \u2014 ruling out the standard storageState-on-disk pattern. Redesign authentication for the suite.",
        ideal: `<p>Keep the goal (skip repeated UI logins) while dropping the specific mechanism security objected to. Options: authenticate via API at the start of each worker and hold the token/cookies only in memory for that worker's lifetime, regenerating rather than persisting between runs; or use short-lived, test-scoped credentials issued fresh per run and never written to disk. The requirement forces an explicit conversation with security about what "acceptable" looks like rather than assuming the standard pattern is the only option.</p>`,
        stuck: `<p>They treat the standard storageState-file pattern as the only way to reuse auth and either fight the requirement or quietly ignore it.</p>`,
      },
      {
        id: 'd24',
        q: "One framework needs to drive web (Playwright), native mobile (a different tool entirely), and API tests, and leadership wants \"one framework\" so business logic isn't duplicated three times. Design it.",
        ideal: `<p>"One framework" should mean one shared core — test data builders, environment config, reporting, business-level assertions/domain language — with platform-specific <em>drivers</em> underneath (Playwright for web, a mobile automation tool for native, an HTTP client for API), not literally one tool doing all three, since Playwright doesn't drive native mobile apps. A shared abstraction (e.g., a login-flow concept implemented once per platform driver, exposing the same interface to test authors) lets a test author write "log in as admin" once conceptually while the mechanics differ per platform.</p>`,
        stuck: `<p>They either try to make Playwright itself drive mobile (it can't) or build three fully separate frameworks and call the shared folder structure "unified," missing that real unification happens at the business-logic layer, not the driver layer.</p>`,
      },
      {
        id: 'd25',
        q: "A hundred teams depend on your shared framework. You need to ship a breaking change (e.g., a fixture signature change) without freezing every team's work until they all migrate simultaneously. How?",
        ideal: `<p>Publish and version the framework like the internal product it is — semantic versioning, with the breaking change landing behind a major-version bump teams opt into on their own schedule. Ship a deprecation period where old and new signatures both work (with a warning on the old path), give a concrete removal date, and provide a migration guide or codemod for the mechanical part of the change.</p>`,
        stuck: `<p>They plan a single coordinated flag-day migration across 100 teams — which either never happens on schedule or causes exactly the blast-radius incident the versioning discipline exists to prevent.</p>`,
      },
      {
        id: 'd26',
        q: "Fifteen product teams use your shared framework, and each has organically adopted a different design pattern \u2014 Page Object Model, Component Objects, and Screenplay all coexist. New engineers can't move between team codebases without relearning the architecture each time. Fix it without a rewrite.",
        ideal: `<p>A shared framework's job is to define standards, not remain neutral among however many patterns teams happen to gravitate to — competing architectural styles under one framework name destroys the portability a shared framework was supposed to provide. Pick one sanctioned pattern, publish it with a reference implementation and lint rules that flag deviation, and migrate incrementally as teams touch existing code rather than mandating a stop-the-world rewrite. Leave clearly-scoped extension points for legitimate team-specific needs, but not architectural pattern choice itself.</p>`,
        stuck: `<p>They try to accommodate all three patterns simultaneously with adapters, treating flexibility as the goal, when the actual problem (portability between teams) requires convergence.</p>`,
      },
      {
        id: 'd27',
        q: "A shared utility function three hundred tests depend on gets modified by one team for their own needs, and five hundred unrelated tests across other teams break the next morning. How do you prevent this class of incident?",
        ideal: `<p>Treat the framework's own code as a product with its own quality bar: unit and integration tests for shared utilities and fixtures themselves, semantic versioning with a real deprecation policy instead of silent breaking changes, mandatory review from framework owners on any PR touching shared code, and a compatibility test run against a representative sample of consuming teams' suites before a shared-code change merges.</p>`,
        stuck: `<p>They fix the specific utility function and move on, without addressing that shared code with no independent test coverage or review gate will keep producing this exact incident.</p>`,
      },
      {
        id: 'd28',
        q: "Test execution itself is fast, but the cloud bill for running it has tripled, and leadership wants costs down without losing coverage. Where do you look?",
        ideal: `<p>Separate "fast" from "cheap" — they're not the same axis. Audit what's driving spend: expensive tiers (full cross-browser, visual regression) running on every commit when they only need to run nightly; worker/machine sizing over-provisioned by default; browser binaries re-downloaded every run instead of cached; sharding used even where a single machine's parallelism was never saturated. Tier execution by risk and frequency, and measure cost per pipeline run as an explicit, tracked metric — not just wall-clock time.</p>`,
        stuck: `<p>They optimize for wall-clock speed (more parallelism, more machines) without recognizing that's often exactly what inflated the bill, and don't track cost-per-run at all.</p>`,
      },
      {
        id: 'd29',
        q: "Fifteen different backend systems (Salesforce, SAP, Oracle, ServiceNow, and others) each need their own test integration, and every new system currently means bolting more special-case code onto the core framework. Redesign it.",
        ideal: `<p>Invert the dependency: define a plugin interface the core framework exposes — how a system-specific plugin registers itself, provides its own fixtures/setup, and reports results back — and implement each backend integration as a plugin behind that interface, rather than the core accumulating direct knowledge of each system. The core should get simpler over time as integrations grow, not more complex.</p>`,
        stuck: `<p>They keep extending the core with system-specific special cases, and it becomes an unmaintainable switchboard everyone is afraid to touch.</p>`,
      },
      {
        id: 'd30',
        q: "Production runs blue-green: half of live traffic is on the previous version, half on the new one, during every deployment window \u2014 and your suite becomes unreliable specifically during those windows. Fix the architecture.",
        ideal: `<p>Make version-awareness explicit test setup: have the suite detect (via a response header, a version endpoint, or an explicit parameter) which version it's actually exercising before asserting on version-specific behavior, and load the matching expected-behavior configuration for that run. This is the same "read the actual variable, don't assume a constant" fix that resolves feature-flag and canary testing.</p>`,
        stuck: `<p>They treat the intermittent failures during deploy windows as flakiness to retry past, rather than recognizing "two versions live simultaneously" as the system's actual steady state during that window.</p>`,
      },
      {
        id: 'd31',
        q: "Your team merges roughly fifty pull requests an hour, and running the full regression suite on every one is no longer physically possible within a reasonable feedback window. Redesign the CI strategy.",
        ideal: `<p>Move from "run everything, every time" to test impact analysis: map which tests exercise which parts of the codebase (via coverage data or a dependency graph), and on each PR run only the tests whose mapped area overlaps the diff, with the full regression suite still running on a schedule as a backstop against mapping gaps. This trades a small, bounded risk (a regression outside the mapped area, caught later by the nightly run) for a PR feedback loop people actually watch.</p>`,
        stuck: `<p>They try to make the full suite faster through raw parallelism alone, hitting a ceiling, instead of questioning whether every PR needs the full suite at all.</p>`,
      },
      {
        id: 'd32',
        q: "Your primary CI provider has an extended outage, and testing needs to continue regardless. What does \"designed for this\" look like, and is it worth building before an outage actually happens?",
        ideal: `<p>Keep the actual test invocation (install, run, report) in a plain script any CI system calls the same way, so switching providers — or running locally, or in a fallback environment — is pointing a different orchestrator at the same script, not rewriting pipeline logic. Whether to build this proactively is a real cost/benefit call: weigh the usually-low probability and usually-short duration of a total outage against the ongoing cost of maintaining vendor-agnostic abstraction. For most teams, documenting a manual fallback is proportionate; full multi-provider redundancy is only worth it if release cadence genuinely can't tolerate any pause.</p>`,
        stuck: `<p>They either dismiss the question ("just wait for it to come back") or over-engineer permanent multi-cloud redundancy, without weighing the cost of each against how critical zero-pause execution really is.</p>`,
      },
      {
        id: 'd33',
        q: "The company has four separate products, each in its own repository, and each has independently reinvented its own reporting, fixtures, and utilities. Leadership wants shared infrastructure without forcing everything into one monolithic repo. Design it.",
        ideal: `<p>Extract the genuinely shared pieces — reporting, common fixtures, shared utilities, core configuration patterns — into a versioned internal package that each product repository depends on and upgrades independently, rather than merging repositories or copy-pasting the same code four times. Product-specific tests and page objects stay in their own repositories, owned by that product's team; only genuinely cross-cutting infrastructure centralizes.</p>`,
        stuck: `<p>They default to "put it all in one monorepo," without weighing that a monorepo trades duplication for cross-team coordination overhead — a versioned shared package usually gets most of the benefit with much less coupling.</p>`,
      },
      {
        id: 'd34',
        q: "Leadership keeps hearing \"5,000 tests, 98% pass rate\" and visibly doesn't care \u2014 they want to know if automation is actually working. Redesign what gets reported upward, and make the case for continued investment.",
        ideal: `<p>Report outcomes leadership can act on, not volume: defect leakage, mean time to detect and repair a real regression, flaky-test percentage (a proxy for how much the pass rate can be trusted), pipeline duration against release cadence, and — the metric that actually justifies budget — production incidents the suite would have caught versus ones it structurally couldn't, framed as risk avoided in terms leadership already tracks (incident cost, downtime). Pair any coverage number with which business-critical journeys are and aren't covered. The ROI case is this reporting shift itself.</p>`,
        stuck: `<p>They keep making the volume/pass-rate case more emphatically instead of switching to the risk-and-outcome framing that's actually persuasive to a non-technical audience holding the budget.</p>`,
      },
      {
        id: 'd35',
        q: "Your company acquires two other companies with completely different tech stacks (one has no automation at all, one has a mature but different framework), and separately the product is moving toward independently-deployed micro-frontends owned by different teams. Design the automation strategy across both kinds of heterogeneity.",
        ideal: `<p>Don't force convergence on day one in either case — converge on outcomes and interfaces, not implementation. For the acquisitions: define what "adequately tested" means as an outcome (critical-journey coverage, a flakiness ceiling, a CI gate) and let each acquired team evolve their existing tooling toward that bar on a realistic timeline, rather than mandating an immediate rewrite that stalls their product work. For micro-frontends: since each is independently deployed, each should own its own test suite scoped to itself, with a thin, centrally-owned integration/contract layer verifying the seams (shared navigation, cross-fragment data contracts), so no team's release is gated on every other team's suite.</p>`,
        stuck: `<p>They reach for "everyone standardizes on our framework immediately" as the default answer to any heterogeneity, without weighing the cost forced convergence imposes on teams whose priority is shipping, not migrating test tooling.</p>`,
      },
    ],
  },

};
